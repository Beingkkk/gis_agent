{# @id gdal_vector_explode_collections #}
{# @name 分解几何集合 #}
{# @description 将矢量数据中的几何集合（MultiPoint、MultiLineString、MultiPolygon等）展开为独立要素 #}
{# @keyword explode #}
{# @keyword collection #}
{# @keyword geometry #}
{# @keyword multi #}
{# @keyword 分解 #}
{# @keyword 集合 #}
{# @keyword gdal vector #}
{# @concept "几何集合展开是将MultiPoint、MultiLineString、MultiPolygon等复合几何拆分为独立的简单几何要素，例如MULTIPOINT(1 2,3 4)会被拆分为两个独立的POINT要素" #}
{# @concept "展开操作不会递归处理嵌套的几何集合，GEOMETRYCOLLECTION(GEOMETRYCOLLECTION(...))只展开一层" #}
{# @note 此工具可作为gdal vector pipeline的一个处理步骤使用 #}
{# @note 输出格式默认从文件扩展名自动判断，可用-f参数显式指定 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或无法访问，请检查文件是否存在且格式正确 #}
{# @common_error "Feature write failed" — 要素写入失败，可能是输出格式不支持该几何类型或磁盘空间不足 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML #}
{# @param layer string optional 输入图层名称（可重复指定多个图层） #}
{# @param output_layer string optional 输出图层名称 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 追加到现有图层（与--upsert互斥） #}
{# @param skip_errors boolean optional 写入要素时跳过错误 #}
{# @param quiet boolean optional 静默模式（不显示进度条或警告信息） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_explode_collections

gdal vector explode-collections{% if of %} -f {{ of | quote }}{% endif %}{% if layer %} -l {{ layer | quote }}{% endif %}{% if quiet %} -q{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if skip_errors %} --skip-errors{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
