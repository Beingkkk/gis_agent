{# @id gdal_vector_rename_layer #}
{# @name 矢量图层重命名 #}
{# @description 重命名矢量数据集的图层名称，支持 ASCII 转换和文件名兼容处理 #}
{# @concept "gdal vector rename-layer 支持两种模式：直接重命名为指定名称，或通过 ASCII 转换、小写化、文件名兼容处理等方式批量规范化图层名称" #}
{# @concept "工具会自动处理重名冲突，通过在名称后追加 _N 序号确保唯一性" #}
{# @note 当数据集包含多个图层时，必须使用 -l 指定源图层名称 #}
{# @note 使用 --filename-compatible 或 --reserved-characters 时，不兼容字符会被移除，可通过 --replacement-character 指定替代字符 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Layer does not exist" — 指定的输入图层名称不存在，或数据集为空 #}
{# @common_error "Attempt to create a layer that already exists" — 输出图层名称与已存在图层冲突，需使用 --overwrite 或更改输出名称 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param input_layer string optional 指定要重命名的源图层名称（多图层时必填） #}
{# @param output_layer string optional 重命名后的目标图层名称 #}
{# @param ascii boolean optional 强制图层名称使用 ASCII 字符集 #}
{# @param lower_case boolean optional 强制图层名称转换为小写（仅作用于 ASCII 字符） #}
{# @param filename_compatible boolean optional 强制图层名称符合文件名规范 #}
{# @param reserved_characters string optional 指定需要移除的保留字符列表 #}
{# @param replacement_character string optional ASCII 转换时替换非法字符的替代字符 #}
{# @param max_length integer optional 图层名称的最大字符长度限制 #}
{# @param update boolean optional 以更新模式打开现有数据集 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_rename_layer

gdal vector rename-layer{% if input_layer %} -l {{ input_layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if ascii %} --ascii{% endif %}{% if lower_case %} --lower-case{% endif %}{% if filename_compatible %} --filename-compatible{% endif %}{% if reserved_characters %} --reserved-characters {{ reserved_characters | quote }}{% endif %}{% if replacement_character %} --replacement-character {{ replacement_character | quote }}{% endif %}{% if max_length %} --max-length {{ max_length }}{% endif %}{% if update %} --update{% endif %}{% if overwrite %} --overwrite{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
