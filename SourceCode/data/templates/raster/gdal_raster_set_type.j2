{# @id gdal_raster_set_type #}
{# @name 栅格数据类型转换 #}
{# @description 修改栅格数据波段的数据类型，支持将高精度数据类型降为低精度或进行格式转换 #}
{# @keyword gdal #}
{# @keyword raster #}
{# @keyword datatype #}
{# @keyword 数据类型 #}
{# @keyword gtiff #}
{# @keyword tiff #}
{# @keyword 转换 #}
{# @keyword 波段 #}
{# @concept "栅格数据类型决定像素值存储的精度和范围，例如Float32可存储小数，Int16适合存储带符号的整数值" #}
{# @concept "将高精度类型（如Float64）转为低精度类型（如UInt8）时会发生数据截断或舍入，可能导致信息丢失" #}
{# @note 选择输出数据类型时需考虑数据范围和精度需求，避免因类型过小导致数据失真 #}
{# @note 复杂数据类型（如CInt16、CFloat32）用于表示复数数据，常见于雷达或干涉测量数据 #}
{# @seealso gdal_raster_translate #}
{# @seealso gdal_raster_convert #}
{# @common_error "Data loss will occur because output data type is smaller" — 输出数据类型范围小于输入数据，数据将被截断或舍入，如需保留全部信息应选择更大范围的数据类型 #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在、格式不支持或文件已损坏 #}
{# @param input file_path required 输入栅格数据集路径 #}
{# @param output file_path required 输出栅格数据集路径 #}
{# @param ot enum required 输出数据类型 options=UInt8,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,CInt16,CInt32,Float16,Float32,Float64,CFloat32,CFloat64 #}
{# @param of format optional 输出格式 default=GTiff options=GTiff,COG,GPKG,JP2OpenJPEG,HDF5,netCDF,ENVI,EHdr,HFA #}
{# @param co text optional 创建选项（KEY=VALUE格式，可重复） #}
{# @param overwrite boolean optional 覆盖已存在的输出文件（与--append互斥） #}
{# @param append boolean optional 作为子数据集追加到现有文件（与--overwrite互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_set_type

gdal raster set-type{% if ot %} --datatype {{ ot }}{% endif %}{% if of %} --format {{ of | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if co %} --co {{ co | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
