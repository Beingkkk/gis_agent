{# @id gdal_mdim #}
{# @name 多维数据管理 #}
{# @description GDAL 多维数组数据处理工具，支持信息查看、格式转换和数据镶嵌 #}
{# @keyword multidimensional #}
{# @keyword netcdf #}
{# @keyword hdf5 #}
{# @keyword zarr #}
{# @keyword mosaic #}
{# @keyword convert #}
{# @keyword 多维数据 #}
{# @keyword 镶嵌 #}
{# @keyword nc #}
{# @concept "gdal mdim 是 GDAL 处理多维数组数据的统一工具，支持 NetCDF、HDF5、Zarr 等格式，用于管理气候、海洋等时间序列或深度维度数据" #}
{# @concept "子命令包括：info（查看元数据和变量信息）、convert（格式转换）、mosaic（多数据集镶嵌）" #}
{# @note 多维数据支持变量、维度（如时间、经度、纬度、深度等）的查看和操作 #}
{# @note convert 子命令可指定输出格式，如 NetCDF、Zarr 等 #}
{# @seealso gdal_translate #}
{# @seealso gdalinfo #}
{# @common_error "No such file or directory" — 输入文件路径不存在或路径错误 #}
{# @common_error "Unsupported data model" — 输入数据格式不支持多维操作，可能需要先进行格式转换 #}
{# @param subcommand enum required 子命令类型 options=convert,info,mosaic #}
{# @param input file_path required 输入多维数据文件路径 #}
{# @param output file_path optional 输出文件路径（convert和mosaic子命令需要） #}
{# @param config text optional 配置选项（KEY=VALUE格式，可重复使用） #}
{# @param drivers boolean optional 显示支持的多维驱动列表（JSON格式） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_mdim

gdal mdim {% if subcommand %}{{ subcommand }}{% endif %}{% if config %} --config {{ config | quote }}{% endif %}{% if drivers %} --drivers{% endif %} {{ input | safe_path | quote }}{% if output %} {{ output | safe_path | quote }}{% endif %}

REM Done
