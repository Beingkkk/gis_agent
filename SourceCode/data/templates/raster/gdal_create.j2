{# @id gdal_create #}
{# @name 创建栅格文件 #}
{# @description 使用 gdal_create 创建新的栅格数据集，支持设置尺寸、波段数、坐标系统、仿射变换等参数 #}
{# @keyword raster #}
{# @keyword tiff #}
{# @keyword gtiff #}
{# @keyword create #}
{# @keyword new #}
{# @keyword 创建 #}
{# @keyword 新建 #}
{# @keyword 栅格 #}
{# @concept "gdal_create 用于初始化新的栅格文件，可以设置尺寸、波段数、数据类型、空间参考、仿射变换等参数" #}
{# @concept "通过 -if 参数可以基于现有栅格作为模板创建新文件，元信息会自动复制" #}
{# @note 输出格式默认从文件扩展名自动判断，也可用 -of 显式指定 #}
{# @note -outsize 和 -bands 默认值取决于是否指定了 -if 模板文件 #}
{# @common_error "ERROR 1: No valid data pixels in destination" — 创建的栅格文件尺寸或波段数设置错误，请检查 -outsize 和 -bands 参数 #}
{# @common_error "ERROR 1: Creation option not supported by this driver" — 指定的创建选项（-co）不被当前输出格式驱动支持 #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param of format optional 输出格式名称，如 GTiff、COG 等 default=GTiff options=GTiff,COG,GPKG,JPEG,PNG,HFA,ENVI,MEM #}
{# @param ot enum optional 输出数据类型 options=Byte,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,Float32,Float64,CInt16,CInt32,CFloat32,CFloat64 #}
{# @param outsize_x integer optional 输出图像宽度（像素） #}
{# @param outsize_y integer optional 输出图像高度（像素） #}
{# @param bands integer optional 波段数量，默认为1（当指定-outsize时）或0（其他情况） #}
{# @param burn float optional 填充到所有波段的固定值 #}
{# @param a_srs crs optional 覆盖输出的空间参考系统定义（如 EPSG:4326） #}
{# @param a_ullr string optional 输出文件的地理范围（左上x 左上y 右下x 右下y） #}
{# @param a_nodata float optional 设置无数据值 #}
{# @param mo string optional 元数据键值对（格式：KEY=VALUE） #}
{# @param co string optional 驱动创建选项（如 COMPRESS=LZW） #}
{# @param input file_path optional 用作模板的输入栅格数据集（会复制尺寸、波段、数据类型等元信息但不复制像素值） #}
{# @param quiet boolean optional 静默模式，不显示进度信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_create

gdal_create{% if ot %} -ot {{ ot | quote }}{% endif %}{% if of %} -of {{ of | quote }}{% endif %}{% if outsize_x and outsize_y %} -outsize {{ outsize_x | quote }} {{ outsize_y | quote }}{% endif %}{% if bands %} -bands {{ bands | quote }}{% endif %}{% if burn %} -burn {{ burn | quote }}{% endif %}{% if a_srs %} -a_srs {{ a_srs | quote }}{% endif %}{% if a_ullr %} -a_ullr {{ a_ullr | quote }}{% endif %}{% if a_nodata %} -a_nodata {{ a_nodata | quote }}{% endif %}{% if mo %} -mo {{ mo | quote }}{% endif %}{% if co %} -co {{ co | quote }}{% endif %}{% if input %} -if {{ input | safe_path | quote }}{% endif %}{% if quiet %} -q{% endif %} {{ output | safe_path | quote }}

REM Done
