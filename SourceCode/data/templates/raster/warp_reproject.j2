{# @id warp_reproject #}
{# @name 栅格重投影 #}
{# @description 将栅格数据重投影到指定坐标系 #}
{# @concept "栅格重投影" — 改变栅格数据的像素网格坐标系，同时重采样像素值 #}
{# @concept "gdalwarp" — GDAL 栅格变换工具，支持坐标系转换、裁剪、拼接 #}
{# @note 默认使用最近邻重采样，大尺度重投影建议使用双线性或三次卷积 #}
{# @seealso vector/reproject — 矢量数据重投影 #}
{# @common_error "ERROR 6: No coordinate system" — 输入栅格缺少坐标系信息 #}
{# @param input file_path required 输入栅格文件路径 #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param t_srs crs required 目标坐标系 EPSG 代码 #}

@echo off
REM Generated by GIS Agent
REM Template: warp_reproject

gdalwarp -t_srs {{ t_srs | quote }} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
