{# @id pct2rgb #}
{# @name 伪彩色转RGB #}
{# @description 将栅格数据的伪彩色（调色板）波段转换为RGB或RGBA彩色格式 #}
{# @keyword pct2rgb #}
{# @keyword 调色板 #}
{# @keyword 伪彩色 #}
{# @keyword palette #}
{# @keyword RGB #}
{# @keyword RGBA #}
{# @keyword 彩色转换 #}
{# @concept "pct2rgb是GDAL的Python工具，用于将单波段伪彩色图像（基于调色板的值映射到颜色）转换为多波段RGB颜色表示" #}
{# @concept "转换后每个像素由R、G、B三个（或RGBA四个）通道值表示，不再依赖调色板" #}
{# @note 此工具依赖GDAL Python绑定，若不可用可使用gdal_translate的-expand rgb选项代替 #}
{# @note 调色板文件可以是GDAL支持的栅格格式（有调色板）或颜色配置文件（txt、qml、qlr） #}
{# @common_error "RuntimeError: Unable to find code that handles" — 输入文件没有调色板波段，或者指定的波段不存在 #}
{# @common_error "Not a valid palette file" — -pct指定的调色板文件格式不受支持或文件损坏 #}
{# @param input file_path required 输入栅格文件路径（包含伪彩色调色板的单波段数据） #}
{# @param output file_path required 输出RGB/RGBA文件路径 #}
{# @param of format optional 输出格式，默认为GeoTIFF options=GTiff,PNG,JPEG,HFA,MEM,GIF #}
{# @param band integer optional 要转换的波段编号 default=1 #}
{# @param rgba boolean optional 生成RGBA四通道文件，默认为RGB三通道 #}
{# @param pct file_path optional 从指定文件读取调色板，而非从源文件获取（可用于多个文件使用统一调色板） #}

@echo off
REM Generated by GIS Agent
REM Template: pct2rgb

pct2rgb{% if of %} -of {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if rgba %} -rgba{% endif %}{% if pct %} -pct {{ pct | safe_path | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
