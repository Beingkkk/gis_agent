{# @id pct2rgb_palette_to_rgb #}
{# @name 伪彩色转RGB #}
{# @description 将栅格数据的伪彩色（调色板）波段转换为RGB或RGBA彩色图像 #}
{# @keyword palette #}
{# @keyword pseudocolor #}
{# @keyword rgb #}
{# @keyword rgba #}
{# @keyword color table #}
{# @keyword 栅格 #}
{# @keyword 调色板 #}
{# @keyword 转换 #}
{# @keyword 调色板转RGB #}
{# @concept "伪彩色（调色板）栅格数据使用颜色索引表存储颜色信息，每个像素值对应颜色表中一个颜色条目" #}
{# @concept "此工具将颜色索引展开为完整的RGB或RGBA值，直接编码到输出文件中" #}
{# @note gdal_translate 的 -expand rgb|rgba 选项提供了相同功能，对于新工作流程推荐使用该选项 #}
{# @note 该工具为 Python 脚本，需要 GDAL Python 绑定才能使用 #}
{# @seealso gdal_translate_convert #}
{# @common_error "Unable to open" — 输入文件不存在或格式不支持，请检查文件路径是否正确 #}
{# @common_error "band is not color indexed" — 指定的波段没有调色板，无法进行伪彩色到RGB的转换 #}
{# @param source_file file_path required 输入的伪彩色栅格文件路径 #}
{# @param dest_file file_path required 输出的RGB/RGBA文件路径 #}
{# @param of format optional 输出格式名称（如 GTiff、PNG 等） default=从文件扩展名自动判断 options=GTiff,PNG,JPEG,HFA,KEA #}
{# @param band integer optional 要转换的波段编号，默认值为1 default=1 #}
{# @param rgba boolean optional 生成RGBA文件（4通道）而不是RGB文件（3通道） #}
{# @param pct file_path optional 调色板文件路径，用于从外部文件获取颜色表实现多文件颜色统一 #}

@echo off
REM Generated by GIS Agent
REM Template: pct2rgb_palette_to_rgb

pct2rgb{% if of %} -of {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if rgba %} -rgba{% endif %}{% if pct %} -pct {{ pct | quote }}{% endif %} {{ source_file | safe_path | quote }} {{ dest_file | safe_path | quote }}

REM Done
