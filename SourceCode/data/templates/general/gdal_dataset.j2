{# @id gdal_dataset #}
{# @name 栅格数据集管理 #}
{# @description gdal dataset 是 GDAL 提供的栅格数据集管理工具，支持检查、复制、删除、重命名和识别数据集 #}
{# @keyword dataset #}
{# @keyword gdal #}
{# @keyword raster #}
{# @keyword 管理 #}
{# @keyword 复制 #}
{# @keyword 删除 #}
{# @keyword 重命名 #}
{# @keyword cp #}
{# @keyword rm #}
{# @concept "gdal dataset 是 GDAL 3.6+ 新增的子命令工具，专门用于栅格数据集的文件级管理操作" #}
{# @concept "支持多种栅格格式的检查、复制、删除、重命名和驱动识别操作" #}
{# @note copy 子命令支持 cp 别名，delete 支持 rm/remove 别名，rename 支持 ren/mv 别名 #}
{# @note 该工具主要面向栅格数据集管理，矢量数据管理请使用 ogr2ogr 或相关工具 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Permission denied" — 目标路径没有写入权限或文件被占用 #}
{# @common_error "No such file or directory" — 指定的数据集文件不存在 #}
{# @param subcommand enum required 数据集操作子命令 options=check,copy,delete,identify,rename #}
{# @param dataset file_path required 目标栅格数据集路径（GeoTIFF、ENVI、HDF、NetCDF 等） #}
{# @param output file_path optional 复制操作的目标路径（copy 子命令使用） #}
{# @param new_name string optional 重命名后的新名称（rename 子命令使用） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_dataset

gdal dataset {{ subcommand }}{% if dataset %} {{ dataset | safe_path | quote }}{% endif %}{% if output %} {{ output | safe_path | quote }}{% endif %}{% if new_name %} {{ new_name | quote }}{% endif %}

REM Done
