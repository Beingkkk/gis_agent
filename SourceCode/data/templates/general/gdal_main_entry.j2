{# @id gdal_main_entry #}
{# @name GDAL 命令行主入口 #}
{# @description GDAL 命令行工具的主入口，通过子命令执行栅格、矢量、数据集管理等多种操作 #}
{# @keyword gdal #}
{# @keyword raster #}
{# @keyword vector #}
{# @keyword convert #}
{# @keyword info #}
{# @keyword driver #}
{# @keyword dataset #}
{# @keyword pipeline #}
{# @concept "gdal 是 GDAL 工具套件的主入口程序，通过子命令（如 raster、vector、info）调用具体功能模块" #}
{# @concept "支持快捷方式：gdal <文件> 等效于 gdal info <文件>，gdal read <文件> 等效于 gdal pipeline <文件>" #}
{# @note 实际操作通过子命令完成，如 raster 转换用 'gdal raster convert'，矢量转换用 'gdal vector convert' #}
{# @note 可通过 --config 设置 GDAL 配置选项，如掩码值、缓存大小等运行时参数 #}
{# @seealso gdal_raster_convert #}
{# @seealso gdal_vector_convert #}
{# @common_error "Too many command line arguments" — 未指定子命令时传入过多参数，需明确使用 raster/vector 等子命令 #}
{# @common_error "Unrecognized command" — 指定的子命令不存在，检查是否使用了有效的子命令名称 #}
{# @param command enum optional GDAL 子命令类型 options=raster,vector,convert,info,dataset,driver,mdim,pipeline,vsi #}
{# @param input file_path optional 输入文件路径（作为 gdal info 的快捷方式） #}
{# @param json_usage boolean optional 以 JSON 格式显示帮助信息 #}
{# @param version boolean optional 显示 GDAL 版本信息 #}
{# @param drivers boolean optional 以 JSON 格式列出所有 GDAL 驱动 #}
{# @param config text optional 配置选项，格式为 KEY=VALUE（可重复） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_main_entry

gdal{% if command %} {{ command }}{% endif %}{% if json_usage %} --json-usage{% endif %}{% if version %} --version{% endif %}{% if drivers %} --drivers{% endif %}{% if config %} --config {{ config | quote }}{% endif %}{% if input %} {{ input | safe_path | quote }}{% endif %}

REM Done
