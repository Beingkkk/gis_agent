{# @id gdal_vsi_sozip #}
{# @name SOZip压缩文件管理 #}
{# @description 创建、列出、优化和验证SOZip（Seek-Optimized ZIP）压缩文件，用于高效的地理空间数据访问 #}
{# @keyword sozip #}
{# @keyword zip #}
{# @keyword seek-optimized #}
{# @keyword 压缩 #}
{# @keyword 优化 #}
{# @keyword gdal #}
{# @concept "SOZip（Seek-Optimized ZIP）是一种针对随机访问优化的ZIP压缩格式，特别适合大文件的高效解压读取" #}
{# @concept "支持四种操作：create（创建）、list（列出内容）、optimize（优化现有ZIP）、validate（验证文件）" #}
{# @note optimize命令可将普通ZIP文件转换为SOZip格式，提升随机访问性能 #}
{# @note list命令会显示ZIP文件内容及SOZip相关的优化信息 #}
{# @common_error "Unable to open" — 指定的ZIP文件不存在或无法读取 #}
{# @common_error "Invalid subcommand" — 子命令只能是 create、list、optimize 或 validate 之一 #}
{# @param subcommand enum required 操作子命令 options=create,list,optimize,validate #}
{# @param input file_path optional 输入文件路径（list/validate/optimize命令需要） #}
{# @param output file_path optional 输出文件路径（create/optimize命令需要） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vsi_sozip

gdal vsi sozip {{ subcommand }}{% if input %} {{ input | quote }}{% endif %}{% if output %} {{ output | quote }}{% endif %}

REM Done
