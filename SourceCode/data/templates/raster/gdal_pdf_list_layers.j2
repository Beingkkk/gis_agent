{# @id gdal_pdf_list_layers #}
{# @name PDF图层列表 #}
{# @description 列出PDF文件中的地理图层信息，支持JSON和文本格式输出 #}
{# @concept "GDAL PDF驱动可以读取嵌入了地理空间信息的PDF文件中的矢量图层和栅格图层" #}
{# @concept "该工具常用于检查PDF地图文件中包含的图层数量和名称" #}
{# @note 仅支持Geospatial PDF文件，非地理编码的普通PDF可能无法列出图层 #}
{# @note JSON格式输出便于在脚本中进一步处理图层信息 #}
{# @common_error "Unable to open datasource" — 文件路径不存在或文件不是有效的PDF格式 #}
{# @common_error "no layers found" — PDF文件中不包含地理图层信息，可能是普通扫描件PDF #}
{# @param input file_path required 输入PDF文件路径 #}
{# @param of string optional 输出格式，支持json或text格式 default=json #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_pdf_list_layers

gdal driver pdf list-layers{% if of %} -f {{ of | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
