{# @id gdal_vector_make_point #}
{# @name 属性字段转点要素 #}
{# @description 从属性表中包含坐标值的字段创建点几何要素 #}
{# @concept "gdal vector make-point 将表中的坐标字段转换为几何点要素，常用于将经纬度文本字段或XY坐标列转换为真正的空间矢量数据" #}
{# @concept "支持同时指定X、Y、Z（高程）、M（测量值）四个坐标维度字段，满足三维点或带测量值点的创建需求" #}
{# @note X和Y字段为必需参数，Z和M字段为可选项；确保指定字段存在且包含有效数值 #}
{# @note 输出坐标系默认继承源数据，可通过 --output-crs 指定新的坐标系进行坐标转换 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Invalid field name for X/Y/Z/M" — 指定的坐标字段名称在输入数据中不存在，或字段值不是有效的数值类型 #}
{# @common_error "Unable to create output dataset" — 输出路径无效、目录不存在，或指定的输出格式不受支持 #}
{# @param input file_path required 输入矢量数据集路径（包含坐标字段的源数据） #}
{# @param output file_path required 输出矢量数据集路径（生成的点要素数据） #}
{# @param x string required 包含X坐标的属性字段名称 #}
{# @param y string required 包含Y坐标的属性字段名称 #}
{# @param z string optional 包含Z坐标（高程）的属性字段名称 #}
{# @param m string optional 包含M值（测量值）的属性字段名称 #}
{# @param output_crs crs optional 输出坐标系统定义（如 EPSG:4326），不指定则继承源数据坐标系 #}
{# @param overwrite boolean optional 是否允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 是否追加到现有图层（与upsert互斥） #}
{# @param skip_errors boolean optional 写入要素时是否跳过错误继续处理 #}
{# @param input_layer string optional 输入图层名称（多层数据源时指定） #}
{# @param output_format string optional 输出格式名称，如 GeoJSON、ESRI Shapefile、GML 等 #}
{# @param output_layer string optional 输出图层名称 #}
{# @param creation_option string optional 数据集创建选项（格式特定参数） #}
{# @param layer_creation_option string optional 图层创建选项 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_make_point

gdal vector make-point --x {{ x | quote }} --y {{ y | quote }}{% if z %} --z {{ z | quote }}{% endif %}{% if m %} --m {{ m | quote }}{% endif %}{% if output_crs %} --output-crs {{ output_crs | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if skip_errors %} --skip-errors{% endif %}{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if output_format %} --output-format {{ output_format | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %}{% if layer_creation_option %} --layer-creation-option {{ layer_creation_option | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
