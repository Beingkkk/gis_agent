{# @id gdalsrsinfo_srs_query #}
{# @name 空间参考信息查询 #}
{# @description 查询并提取坐标系的多种表达格式，包括WKT、PROJ.4、EPSG等 #}
{# @keyword srs #}
{# @keyword crs #}
{# @keyword epsg #}
{# @keyword wkt #}
{# @keyword proj4 #}
{# @keyword 坐标系 #}
{# @keyword 投影 #}
{# @keyword 查询 #}
{# @concept "SRS（空间参考系统）定义了地理数据的坐标系统和投影方式，gdalsrsinfo可以将一种SRS表达转换为多种格式" #}
{# @concept "支持多种SRS输入格式：WKT、PROJ.4、EPSG编号、数据集文件等常用表达方式" #}
{# @note 输入可以是EPSG:4326这样的编码，也可以是完整的WKT字符串或PROJ.4定义 #}
{# @note 使用-V参数可以验证SRS定义是否有效，对于处理用户输入的数据很有帮助 #}
{# @common_error "Unable to parse SRS definition" — SRS定义格式不正确或不受支持，需检查WKT、PROJ.4字符串或EPSG编码是否有效 #}
{# @common_error "Cannot find EPSG" — 指定的EPSG编号不存在或无法找到对应的空间参考系统定义 #}
{# @param srs_def string required 空间参考系统定义，可以是数据集文件名、WKT字符串、PROJ.4字符串、EPSG编号(如EPSG:4326)或包含SRS信息的文件路径 #}
{# @param o enum optional 输出格式类型 default=default options=default,all,PROJJSON,proj4,wkt1,wkt_simple,wkt_noct,wkt_esri,wkt2,wkt2_2015,wkt2_2019,mapinfo,xml #}
{# @param V boolean optional 验证SRS定义的有效性 #}
{# @param e boolean optional 搜索与SRS对应的EPSG编号 #}
{# @param single_line boolean optional 输出WKT时使用单行格式（不换行） #}

@echo off
REM Generated by GIS Agent
REM Template: gdalsrsinfo_srs_query

gdalsrsinfo{% if single_line %} --single-line{% endif %}{% if V %} -V{% endif %}{% if e %} -e{% endif %}{% if o %} -o {{ o | quote }}{% endif %} {{ srs_def | quote }}

REM Done
