{# @id gdal_raster_unscale #}
{# @name 栅格值反缩放 #}
{# @description 将栅格数据中的缩放值（包含scale/offset元数据）转换回原始未缩放的实际值 #}
{# @keyword raster #}
{# @keyword unscale #}
{# @keyword scale #}
{# @keyword offset #}
{# @keyword 栅格 #}
{# @keyword 缩放 #}
{# @keyword 偏移 #}
{# @keyword 转换 #}
{# @concept "gdal raster unscale 将缩放的栅格值转换回原始值，使用公式：unscaled_value = scaled_value × scale + offset" #}
{# @concept "当输入波段没有scale/offset元数据时，值保持不变不做处理" #}
{# @note 默认输出数据类型根据输入类型决定：Byte/UInt16/Int16/Float32等默认转为Float32，Float64保持Float64 #}
{# @note 此命令是 gdal raster scale 的反向操作 #}
{# @seealso gdal_raster_scale #}
{# @common_error "No such file or directory" — 输入文件路径不存在或无法访问 #}
{# @common_error "Scale or offset metadata not found" — 输入栅格数据中不包含scale/offset元数据，无法进行反缩放操作 #}
{# @param input file_path required 输入栅格数据集路径（包含scale/offset元数据） #}
{# @param output file_path required 输出栅格数据集路径（存放反缩放后的值） #}
{# @param of format optional 输出格式名称 default=GTiff options=GTiff,COG,GRIB,HDF5,NetCDF,JPEG,PNG #}
{# @param ot enum optional 输出数据类型 options=UInt8,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,Float16,Float32,Float64,CInt16,CInt32,CFloat32,CFloat64 #}
{# @param co string optional 输出格式的创建选项（KEY=VALUE格式） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到已存在的输出文件中（与overwrite互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_unscale

gdal raster unscale{% if of %} --format {{ of | quote }}{% endif %}{% if ot %} --ot {{ ot | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
