{# @id gdal_vector_check_geometry #}
{# @name 几何有效性检查 #}
{# @description 检查矢量数据中几何要素的有效性和简单性，识别无效多边形、自相交线等几何错误 #}
{# @keyword geometry #}
{# @keyword validity #}
{# @keyword check #}
{# @keyword polygon #}
{# @keyword linestring #}
{# @keyword geos #}
{# @keyword 几何 #}
{# @keyword 有效性 #}
{# @keyword 检查 #}
{# @keyword 自相交 #}
{# @concept "该工具使用 GEOS 库执行 OGC Simple Features 标准中的几何有效性检查，与 PostGIS、QGIS、Shapely 等软件的结果一致" #}
{# @concept "多边形和线要素分别检查有效性和简单性：多边形检查自重叠、岛洞冲突等；线要素检查自相交（需 GEOS 3.14+）" #}
{# @note 默认只输出无效或不简单的几何要素，配合 --include-valid 可同时包含有效要素 #}
{# @note 几何集合（GeometryCollection）会递归检查其组成元素的有效性 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或格式不支持 #}
{# @common_error "Layer not found" — 指定的图层名称在数据源中不存在 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径（记录检查结果） #}
{# @param of format optional 输出格式名称 default=GeoJSON options=GeoJSON,GPKG,ESRI Shapefile,KML,GML #}
{# @param layer string optional 输入图层名称 #}
{# @param geometry_field string optional 要检查的几何字段名称 #}
{# @param include_field string optional 从输入复制到输出的字段名（可重复），特殊值：ALL 和 NONE #}
{# @param include_valid boolean optional 将有效几何要素也输出到结果中，几何为空 #}
{# @param skip_errors boolean optional 写入要素时跳过错误，继续处理后续要素 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_check_geometry

gdal vector check-geometry{% if of %} -f {{ of | quote }}{% endif %}{% if layer %} -l {{ layer | quote }}{% endif %}{% if geometry_field %} --geometry-field {{ geometry_field | quote }}{% endif %}{% if include_field %} --include-field {{ include_field | quote }}{% endif %}{% if include_valid %} --include-valid{% endif %}{% if skip_errors %} --skip-errors{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
