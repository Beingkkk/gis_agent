{# @id gdal_raster_convert #}
{# @name 栅格格式转换 #}
{# @description 使用 gdal raster convert 将栅格数据在不同格式之间转换，支持创建选项和覆盖模式 #}
{# @keyword tif #}
{# @keyword geotiff #}
{# @keyword asc #}
{# @keyword bil #}
{# @keyword hdf #}
{# @keyword nc #}
{# @keyword netcdf #}
{# @keyword png #}
{# @keyword jpg #}
{# @keyword raster #}
{# @keyword convert #}
{# @keyword format #}
{# @keyword gdal raster #}
{# @concept "gdal raster convert 是 GDAL 新命令行接口中的栅格格式转换工具，支持 GeoTIFF、ENVI、HFA、GRIB、netCDF 等多种栅格格式" #}
{# @concept "支持通过创建选项（--co）指定压缩方式、块大小、坐标系统等参数，通过打开选项（--oo）控制数据读取方式" #}
{# @note 目标格式默认从输出文件扩展名自动判断，可用 --of 显式指定 #}
{# @note 创建选项（--co）可以重复使用以指定多个参数，如 COMPRESS=LZW TILED=YES #}
{# @seealso ogr2ogr_convert #}
{# @common_error "无法识别该格式" — 指定的输出格式（--of）不受 GDAL 支持，或文件名缺少扩展名导致无法自动识别 #}
{# @common_error "Output dataset exists" — 输出文件已存在且未指定 --overwrite 或 --append 选项 #}
{# @param input file_path required 输入栅格文件路径或数据源（源数据集） #}
{# @param output file_path required 输出栅格文件路径或数据源（目标数据集） #}
{# @param of format optional 输出格式名称（如 GTiff、GeoJSON、ENVI 等） options=GTiff,JPEG,PNG,HFA,ENVI,EHdr,GPKG,netCDF,GRIB,MBTiles #}
{# @param co text optional 创建选项，格式为 KEY=VALUE（如 COMPRESS=LZW、TILED=YES 等，可重复使用） #}
{# @param input_format format optional 输入格式名称，覆盖自动检测 options=(see documentation) #}
{# @param oo text optional 打开选项，格式为 KEY=VALUE #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 追加为已有输出的子数据集（与 --overwrite 互斥） #}
{# @param quiet boolean optional 静默模式，不显示进度条或警告信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_convert

gdal raster convert{% if of %} --of {{ of | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if input_format %} --if {{ input_format | quote }}{% endif %}{% if oo %} --oo {{ oo | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if quiet %} -q{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
