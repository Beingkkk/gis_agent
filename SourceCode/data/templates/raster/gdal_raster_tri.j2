{# @id gdal_raster_tri #}
{# @name 地形崎岖度指数计算 #}
{# @description 基于 DEM 栅格数据计算 Terrain Ruggedness Index (TRI)，量化地表崎岖程度 #}
{# @keyword dem #}
{# @keyword elevation #}
{# @keyword tri #}
{# @keyword terrain #}
{# @keyword ruggedness #}
{# @keyword 地形 #}
{# @keyword 崎岖度 #}
{# @keyword 起伏度 #}
{# @concept "TRI（Terrain Ruggedness Index，地形崎岖度指数）通过计算中心像元与周围8个像元的高程差绝对值之和来量化地表崎岖程度" #}
{# @concept "Riley 算法是默认算法，逐像元计算 3x3 窗口内高程差异的总和；值越大表示地形越破碎崎岖" #}
{# @note 输入应为高程数据（如 DEM），输出为单波段浮点型栅格 #}
{# @note 图像边缘或附近存在 nodata 值时，默认会进行插值计算；使用 --no-edges 会在边缘生成 1 像素 nodata 边框 #}
{# @seealso gdal_raster_aspect #}
{# @seealso gdal_raster_slope #}
{# @seealso gdal_raster_hillshade #}
{# @common_error "Unable to open" — 输入文件路径不存在或格式不支持 #}
{# @common_error "File exists" — 输出文件已存在且未指定 --overwrite 或 --append 参数 #}
{# @param input file_path required 输入 DEM 栅格文件路径 #}
{# @param output file_path required 输出 TRI 结果栅格文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,HFA,ENVI,JPEG2000,COG #}
{# @param band integer optional 输入波段索引（从1开始） default=1 #}
{# @param algorithm enum optional TRI 计算算法 default=Riley options=Riley,Wilson #}
{# @param no_edges boolean optional 禁止在图像边缘插值，边缘像元设为 nodata #}
{# @param overwrite boolean optional 覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有文件 #}
{# @param co text optional 输出格式的创建选项（KEY=VALUE 格式，可多次指定） #}
{# @param option boolean optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_tri

gdal raster tri{% if of %} -f {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if algorithm %} --algorithm {{ algorithm | quote }}{% endif %}{% if no_edges %} --no-edges{% endif %}{% if co %}{% for option in co %} --co {{ option | quote }}{% endfor %}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
