{# @id gdal_driver_cog_validate #}
{# @name COG验证工具 #}
{# @description 验证GeoTIFF文件是否符合云优化GeoTIFF（COG）规范并输出详细验证报告 #}
{# @keyword cog #}
{# @keyword cloud optimized geotiff #}
{# @keyword tiff #}
{# @keyword validate #}
{# @keyword 云优化 #}
{# @keyword 验证 #}
{# @concept "云优化GeoTIFF（COG）是一种专为云端访问优化的GeoTIFF格式，要求文件结构支持远程块的高效读取" #}
{# @concept "COG验证工具会检查TIFF文件的结构是否符合COG规范，如GeoTIFF标签、外部块存储等特性" #}
{# @note 此工具依赖GDAL Python绑定，运行时需确保Python环境已安装GDAL库 #}
{# @note full_check设为auto时会自动判断执行层级，设为yes会执行最严格的完整检查 #}
{# @common_error "Unable to open" — 指定的文件不存在或无法被GDAL识别为有效的TIFF数据集 #}
{# @common_error "Python bindings not available" — 未正确安装GDAL Python绑定库，无法运行此验证工具 #}
{# @param input file_path required 待验证的TIFF文件路径（必须是GeoTIFF格式） #}
{# @param full_check enum optional 是否执行完整验证检查 default=auto options=auto,yes,no #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_driver_cog_validate

gdal driver cog validate{% if full_check %} --full-check {{ full_check | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
