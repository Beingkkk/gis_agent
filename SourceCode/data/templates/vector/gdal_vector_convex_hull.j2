{# @id gdal_vector_convex_hull #}
{# @name 矢量凸包计算 #}
{# @description 计算矢量数据几何对象的最小凸包多边形 #}
{# @keyword convex hull #}
{# @keyword 凸包 #}
{# @keyword 凸壳 #}
{# @keyword geometry #}
{# @keyword geometry operation #}
{# @keyword shp #}
{# @keyword geojson #}
{# @concept "凸包（Convex Hull）是包含输入几何所有点的最小凸多边形，是几何分析中的基础操作" #}
{# @concept "gdal vector convex-hull 适用于点、线、面等多种几何类型，输出结果通常是 Polygon，对于退化输入可能返回 LineString 或 Point" #}
{# @note 该命令可作为 gdal vector pipeline 管道的一个步骤使用 #}
{# @note --append 和 --upsert 互斥，不能同时使用 #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或格式不支持，请检查文件是否可读 #}
{# @common_error "Layer not found" — 指定的输入图层名称不存在，可用 --input-layer 参数确认图层名 #}
{# @param input file_path required 输入矢量文件路径或数据源 #}
{# @param output file_path required 输出矢量文件路径或数据源 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,GML,KML,GeoPackage #}
{# @param input_layer string optional 指定输入图层名称 #}
{# @param creation_option string optional 输出数据源创建选项（格式为 KEY=VALUE） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据源 #}
{# @param update boolean optional 以更新模式打开已存在的数据源 #}
{# @param append boolean optional 追加到已存在的图层 #}
{# @param output_layer string optional 输出图层名称 #}
{# @param active_geometry string optional 指定处理的几何字段名称 #}
{# @param skip_errors boolean optional 写入要素时跳过错误继续处理 #}
{# @param upsert boolean optional Upsert 模式更新或插入要素（隐含 append） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_convex_hull

gdal vector convex-hull{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if of %} --format {{ of | quote }}{% endif %}{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if append %} --append{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if active_geometry %} --active-geometry {{ active_geometry | quote }}{% endif %}{% if skip_errors %} --skip-errors{% endif %}{% if upsert %} --upsert{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
