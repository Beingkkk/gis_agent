{# @id gdal_pdf_list_layers #}
{# @name PDF图层列表 #}
{# @description 列出PDF文件中的图层信息，支持JSON和文本格式输出 #}
{# @keyword pdf #}
{# @keyword 图层 #}
{# @keyword layers #}
{# @keyword json #}
{# @keyword list #}
{# @keyword gdal #}
{# @concept "gdal driver pdf list-layers 用于提取PDF文件中的图层信息，支持GeoPDF等包含地理空间数据的PDF文件" #}
{# @concept "输出结果可以是JSON数组格式或文本列表格式，便于后续自动化处理" #}
{# @note PDF文件可能包含多个图层，图层可以是矢量要素或栅格数据 #}
{# @note 输出格式默认为JSON，适合程序化解析；text格式适合人工阅读 #}
{# @common_error "Unable to open" — 输入文件不是有效的PDF格式或文件路径不存在 #}
{# @common_error "PDF is not supported" — GDAL编译时未启用PDF支持，需要安装相关驱动库（如PDFium或Poppler） #}
{# @param input file_path required 输入的PDF文件路径或数据集 #}
{# @param of enum optional 输出格式 default=json options=json,text #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_pdf_list_layers

gdal driver pdf list-layers{% if of %} -f {{ of | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
