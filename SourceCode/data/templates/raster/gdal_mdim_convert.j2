{# @id gdal_mdim_convert #}
{# @name 多维栅格数据转换 #}
{# @description 将多维栅格数据集（如 NetCDF、Zarr、GRIB 等格式）在不同格式之间转换，支持数组筛选和维度子集提取 #}
{# @keyword netcdf #}
{# @keyword nc #}
{# @keyword zarr #}
{# @keyword multidim #}
{# @keyword 多维栅格 #}
{# @keyword HDF5 #}
{# @keyword grib #}
{# @keyword 多维数组 #}
{# @keyword 转换 #}
{# @keyword mdim #}
{# @concept "多维栅格数据（Multidimensional Raster）是指包含多个维度（如时间、高度、深度等）的数组数据，常见于气候、海洋、大气等科学领域" #}
{# @concept "gdal mdim convert 支持 NetCDF、Zarr、HDF5、GRIB 等科学数据格式的互转，可通过 --array、--group、--subset 参数选择性地转换数据的子集" #}
{# @note 对于常见格式如 NetCDF 和 Zarr，GDAL 通常能自动识别，无需显式指定 --if 或 --of 参数 #}
{# @note 使用 --subset 参数可以高效地提取数据的部分区域，避免转换完整数据集 #}
{# @common_error " subdatasets are not supported" — 输出格式不支持多维子数据集，可能需要使用其他格式如 netCDF 或 Zarr #}
{# @common_error "Dimension mismatch" — 子集范围（--subset）指定的维度大小与原始数据不匹配 #}
{# @param input file_path required 输入的多维栅格数据集路径（支持 NetCDF、Zarr、HDF5 等格式） #}
{# @param output file_path required 输出的多维栅格数据集路径 #}
{# @param of format optional 输出格式名称 options=netCDF,Zarr,GRIB,HDF5,GML,JSON #}
{# @param if format optional 输入格式名称（通常自动检测） options=netCDF,Zarr,GRIB,HDF5,GML,JSON #}
{# @param array string optional 指定要转换的单个数组名称（默认转换整个数据集） #}
{# @param group string optional 指定要转换的单个组路径 #}
{# @param subset string optional 指定数据子集范围，格式如 dim=start:end #}
{# @param scale_axes string optional 对指定维度应用整数缩放因子 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param strict boolean optional 将警告视为错误处理 #}
{# @param quiet boolean optional 静默模式，不显示进度条和警告信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_mdim_convert

gdal mdim convert{% if quiet %} -q{% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if if %} --if {{ if | quote }}{% endif %}{% if array %} --array {{ array | quote }}{% endif %}{% if group %} --group {{ group | quote }}{% endif %}{% if subset %} --subset {{ subset | quote }}{% endif %}{% if scale_axes %} --scale-axes {{ scale_axes | quote }}{% endif %}{% if strict %} --strict{% endif %}{% if overwrite %} --overwrite{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
