{# @id gdal_dataset_identify #}
{# @name 识别GDAL数据集格式 #}
{# @description 识别文件或目录中的GDAL数据集格式，返回打开数据集所需的驱动信息 #}
{# @keyword gdal #}
{# @keyword identify #}
{# @keyword driver #}
{# @keyword format #}
{# @keyword dataset #}
{# @keyword 识别 #}
{# @keyword 格式检测 #}
{# @keyword 元数据 #}
{# @concept "gdal dataset identify 用于检测文件是否被 GDAL 支持，并返回打开该数据集所需的驱动名称和参数" #}
{# @concept "该工具可递归扫描目录结构，适合批量识别多个数据集的格式信息" #}
{# @note 该命令不需要指定输入文件格式，GDAL会自动尝试各驱动识别文件类型 #}
{# @note 详细信息模式(--detailed)会报告文件是否包含地理参考信息及云优化状态 #}
{# @common_error "Unable to identify as a supported file format" — 指定的文件不是 GDAL 支持的格式，或文件已损坏 #}
{# @common_error "Permission denied" — 没有读取指定文件或目录的权限 #}
{# @param input file_path required 要识别的文件或目录路径（可重复指定多个） #}
{# @param recursive boolean optional 递归扫描目录中的所有文件 #}
{# @param force_recursive boolean optional 强制递归扫描所有子目录 #}
{# @param detailed boolean optional 输出详细信息，包括地理参考信息和云优化GeoTIFF状态 #}
{# @param report_failures boolean optional 报告无法识别的文件类型 #}
{# @param of format optional 输出格式 options=GML,GeoJSON,CSV,Memory #}
{# @param output file_path optional 输出矢量数据集路径 #}
{# @param l string optional 输出图层名称 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_dataset_identify

gdal dataset identify{% if input %} {{ input | safe_path | quote }}{% endif %}{% if recursive %} --recursive{% endif %}{% if force_recursive %} --force-recursive{% endif %}{% if detailed %} --detailed{% endif %}{% if report_failures %} --report-failures{% endif %}{% if of %} --of {{ of | quote }}{% endif %}{% if output %} -o {{ output | safe_path | quote }}{% endif %}{% if l %} -l {{ l | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}

REM Done
