{# @id gdal_mdim_convert #}
{# @name 多维栅格数据转换 #}
{# @description 使用 gdal mdim 将多维栅格数据集（如 NetCDF、GRIB 等格式）在不同格式之间转换 #}
{# @concept "gdal mdim 是 GDAL 3.1 引入的多维数据管理工具，用于处理多维栅格数据（多时相、多高度等维度数据）" #}
{# @concept "支持的数据格式包括 NetCDF、GRIB、ZARR、PCRaster 等多维科学数据格式" #}
{# @note gdal mdim 命令包含多个子命令：convert（转换）、info（查看信息）、mosaic（镶嵌） #}
{# @note 对于简单转换任务，也可以使用 gdal_translate 配合多维选项 #}
{# @seealso gdal_translate #}
{# @common_error "Unable to open dataset" — 输入文件不是有效的多维数据集，或指定的格式驱动不可用 #}
{# @common_error "Unsupported data model" — 目标格式不支持源数据的多维结构，需要选择其他输出格式 #}
{# @param input file_path required 输入多维数据集文件路径（如 NetCDF、GRIB 等） #}
{# @param output file_path required 输出多维数据集文件路径 #}
{# @param of string optional 输出格式名称（如 NetCDF、ZARR、GRIB 等多维格式） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_mdim_convert

gdal mdim convert{% if of %} --format={{ of | quote }}{% endif %}{% if input %} {{ input | safe_path | quote }}{% endif %}{% if output %} {{ output | safe_path | quote }}{% endif %}

REM Done
