{# @id gdalmdiminfo #}
{# @name 多维栅格信息查询 #}
{# @description 查询 GDAL 多维栅格数据集的结构信息、数组定义及统计值，以 JSON 格式输出 #}
{# @keyword netcdf #}
{# @keyword hdf #}
{# @keyword zarr #}
{# @keyword grib #}
{# @keyword 多维 #}
{# @keyword multidimensional #}
{# @keyword 数组 #}
{# @keyword 信息查询 #}
{# @keyword JSON #}
{# @concept "gdalmdiminfo 用于查询 NetCDF、HDF5、Zarr、GRIB 等多维栅格数据集中数组维度、变量类型、坐标轴定义及元数据信息" #}
{# @concept "输出遵循 gdalmdiminfo_output.schema.json 规范，返回标准的 JSON 结构便于程序解析" #}
{# @note 多维栅格（Multidimensional Raster）是支持时间序列、高光谱等高维度数据的格式，常见于气象、海洋、科学研究领域 #}
{# @note 详细模式 (-detailed) 会显示数组实际值，结合 -limit 可控制输出量；统计信息 (-stats) 会触发额外计算 #}
{# @seealso gdal_translate #}
{# @seealso gdalinfo #}
{# @common_error "ERROR 1: /path/to/file: No such file or directory" — 输入文件路径不存在或文件损坏，需检查文件是否被其他程序占用 #}
{# @common_error "Unable to find driver" — 无法识别指定格式，可能 GDAL 编译时未包含该驱动（如 HDF5、netCDF） #}
{# @param dataset_name file_path required 输入的多维栅格数据集路径（如 NetCDF、HDF5、Zarr 等格式） #}
{# @param detailed boolean optional 输出详细信息，包括属性数据类型和数组值 #}
{# @param summary boolean optional 输出摘要信息（默认模式） #}
{# @param nopretty boolean optional 输出单行紧凑格式，不带缩进 #}
{# @param array string optional 仅输出指定名称数组的信息 #}
{# @param limit integer optional 限制每个维度显示的值的数量（与 -detailed 配合使用） #}
{# @param stats boolean optional 读取并显示数组统计信息（若不存在则强制计算） #}
{# @param of format optional 指定数据集格式/驱动名称 options=netCDF,HDF5,Zarr,GRIB,JP2OpenJPEG #}
{# @param oo string optional 数据集打开选项（格式特定，格式为 NAME=VALUE） #}

@echo off
REM Generated by GIS Agent
REM Template: gdalmdiminfo

gdalmdiminfo{% if summary %} -summary{% endif %}{% if detailed %} -detailed{% endif %}{% if nopretty %} -nopretty{% endif %}{% if array %} -array {{ array | quote }}{% endif %}{% if limit %} -limit {{ limit | quote }}{% endif %}{% if stats %} -stats{% endif %}{% if of %} -if {{ of | quote }}{% endif %}{% if oo %} -oo {{ oo | quote }}{% endif %} {{ dataset_name | safe_path | quote }}

REM Done
