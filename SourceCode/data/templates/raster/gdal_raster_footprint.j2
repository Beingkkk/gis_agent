{# @id gdal_raster_footprint #}
{# @name 栅格Footprint提取 #}
{# @description 从栅格数据中提取有效像素区域的边界轮廓，生成矢量多边形 #}
{# @keyword footprint #}
{# @keyword mask #}
{# @keyword nodata #}
{# @keyword boundary #}
{# @keyword raster to vector #}
{# @keyword 轮廓提取 #}
{# @keyword 栅格转矢量 #}
{# @keyword 有效区域 #}
{# @concept "gdal raster footprint 通过读取栅格数据的nodata值或mask信息，将有效像素区域转换为矢量多边形边界" #}
{# @concept "支持多种后处理操作：多部分拆分、点加密、凸包计算、面积过滤、简化和点数限制" #}
{# @note 输入栅格的nodata值或alpha通道定义了哪些像素属于有效区域，nodata区域不会包含在footprint中 #}
{# @note 可同时输入多个栅格文件，生成的footprint会自动合并（取决于 --combine-bands 设置） #}
{# @common_error "Unable to open datasource" — 输入栅格文件路径不存在或文件已损坏 #}
{# @common_error "All pixels are nodata" — 输入栅格所有像素都是nodata值，导致无法生成有效的footprint多边形 #}
{# @param input file_path required 输入栅格文件路径（可指定多个文件合并footprint） #}
{# @param output file_path required 输出矢量文件路径（存储footprint多边形） #}
{# @param of format optional 输出矢量格式 default=GPKG options=GPKG,GeoJSON,ESRI Shapefile,KML,GML #}
{# @param output_layer string optional 输出图层名称 default=footprint #}
{# @param band integer optional 输入波段索引（1-based，多个波段可用 --band 重复指定） #}
{# @param combine_bands enum optional 多波段时如何合并mask default=union options=union,intersection #}
{# @param convex_hull boolean optional 是否计算footprint的凸包 #}
{# @param split_multipolygons boolean optional 是否将多部分几何拆分为多个单部分要素 #}
{# @param simplify_tolerance float optional 简化容差（用于简化输出几何） #}
{# @param output_crs crs optional 输出坐标系统（EPSG代码或PROJ字符串） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_footprint

gdal raster footprint{% if of %} --output-format {{ of | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if band %} --band {{ band }}{% endif %}{% if combine_bands %} --combine-bands {{ combine_bands }}{% endif %}{% if convex_hull %} --convex-hull{% endif %}{% if split_multipolygons %} --split-multipolygons{% endif %}{% if simplify_tolerance %} --simplify-tolerance {{ simplify_tolerance }}{% endif %}{% if output_crs %} --output-crs {{ output_crs | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
