{# @id gdal_mdim_mosaic #}
{# @name 多维栅格镶嵌 #}
{# @description 将多个多维栅格数据集（如NetCDF、Zarr格式）拼接成虚拟或实体镶嵌数据集 #}
{# @keyword netcdf #}
{# @keyword zarr #}
{# @keyword vrt #}
{# @keyword 多维 #}
{# @keyword 镶嵌 #}
{# @keyword mosaic #}
{# @keyword array #}
{# @keyword gdalmdim #}
{# @concept "gdal mdim mosaic 用于拼接按维度切片的多维栅格数组，可输出虚拟的VRT格式或实体化格式（NetCDF、Zarr）" #}
{# @concept "输入数组必须满足结构约束：相同的数组名称和数据类型、相同的维度数量和顺序、每个维度有对应的索引变量" #}
{# @note 输入文件路径可使用通配符（*、?）或以@开头的文本文件（每行列出一个输入路径） #}
{# @note 镶嵌区域中的空洞会填充nodata值（若已定义）或零值；重叠区域按输入列表顺序决定优先级，后面的文件优先 #}
{# @common_error "Array has not the same data type" — 参与镶嵌的数组数据类型不一致，需要确保所有输入数组使用相同的数据类型 #}
{# @common_error "Array has not the same number of dimensions" — 数组维度数量不匹配，参与镶嵌的所有数组必须具有相同的维度数量和顺序 #}
{# @param input file_path required 输入多维栅格数据集路径，支持多个文件或通配符（*、?） #}
{# @param output file_path required 输出多维栅格数据集路径 #}
{# @param of format optional 输出格式 default=VRT options=VRT,NetCDF,Zarr #}
{# @param creation_option text optional 输出格式的创建选项（KEY=VALUE形式，可重复使用） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param array string optional 指定要镶嵌的数组名称（可重复使用，默认处理所有二维及以上数组） #}
{# @param quiet boolean optional 静默模式，不显示进度条和警告信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_mdim_mosaic

gdal mdim mosaic{% if of %} --format {{ of | quote }}{% endif %}{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if array %} --array {{ array | quote }}{% endif %}{% if quiet %} -q{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
