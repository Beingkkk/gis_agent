{# @id gdal_vector_concave_hull #}
{# @name 矢量凹包计算 #}
{# @description 使用 concave-hull 算法计算覆盖输入几何图形的最小凹多边形 #}
{# @keyword concave hull #}
{# @keyword 凹包 #}
{# @keyword 多边形 #}
{# @keyword 几何计算 #}
{# @keyword gdal #}
{# @keyword ogr #}
{# @keyword polygon #}
{# @keyword hull #}
{# @concept "凹包（Concave Hull）是包围一组点或多边形的最小凹多边形，相比凸包能更精确地描述数据的实际边界形状" #}
{# @concept "ratio 参数控制凹包程度：值越接近1结果越接近凸包，值越小生成的边界越贴合原始几何但可能产生更多凹陷" #}
{# @note 该工具也可作为 gdal vector pipeline 的一个处理步骤使用 #}
{# @note 对于面状输入使用 ConcaveHullOfPolygons 方法，对其他几何类型使用通用 ConcaveHull 方法 #}
{# @common_error "Invalid value for --ratio" — ratio 参数必须在 0-1 范围内，超出范围会导致计算失败 #}
{# @common_error "No features found in input" — 输入数据集中不存在有效几何要素，请检查图层名称和几何字段是否正确 #}
{# @param input file_path required 输入矢量数据集路径，包含要计算凹包的几何要素 #}
{# @param output file_path required 输出矢量数据集路径，存储计算得到的凹多边形 #}
{# @param ratio float required 控制凹包程度的比例值（0-1之间，值越小越接近凹包，1表示凸包） #}
{# @param allow_holes boolean optional 允许输出多边形包含孔洞 #}
{# @param tight boolean optional 生成的凹包是否严格跟随输入多边形的外边界 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML,GeoPackage #}
{# @param input_layer string optional 输入图层名称 #}
{# @param output_layer string optional 输出图层名称 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 追加到已存在的图层 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_concave_hull

gdal vector concave-hull{% if of %} --output-format {{ of | quote }}{% endif %}{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if allow_holes %} --allow-holes{% endif %}{% if tight %} --tight{% endif %} --ratio {{ ratio }}{{ output | safe_path | quote }} {{ input | safe_path | quote }}

REM Done
