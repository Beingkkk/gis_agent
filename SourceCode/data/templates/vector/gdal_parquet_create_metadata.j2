{# @id gdal_parquet_create_metadata #}
{# @name 创建 Parquet 分区元数据文件 #}
{# @description 为 Parquet 分区数据集创建 _metadata 元数据文件 #}
{# @keyword parquet #}
{# @keyword metadata #}
{# @keyword _metadata #}
{# @keyword partitioned #}
{# @keyword gdal #}
{# @keyword 分区 #}
{# @concept "Parquet 分区数据集通常由多个 Parquet 文件组成，_metadata 文件记录了所有分区的 schema 和文件路径信息" #}
{# @concept "该工具主要用于优化 GDAL 读取分区 Parquet 数据集的读取性能" #}
{# @note 此工具通常由 gdal vector partition 命令自动调用，用户一般无需直接使用 #}
{# @note 输入的多个 Parquet 文件应具有相同的 schema 结构 #}
{# @common_error "File not found" — 指定的输入 Parquet 文件路径不存在 #}
{# @common_error "Schema mismatch" — 多个输入 Parquet 文件的 schema 不一致，无法合并元数据 #}
{# @param input file_path required 输入的 Parquet 数据集路径（支持多个文件） #}
{# @param output folder_path required 输出的 Parquet 数据集路径（将生成 _metadata 文件） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_parquet_create_metadata

gdal driver parquet create-metadata-file{% if overwrite %} --overwrite{% endif %} {{ input | quote }} {{ output | safe_path | quote }}

REM Done
