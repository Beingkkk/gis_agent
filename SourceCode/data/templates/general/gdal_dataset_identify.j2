{# @id gdal_dataset_identify #}
{# @name 数据集格式识别 #}
{# @description 识别给定文件或目录的数据集驱动格式和详细信息 #}
{# @keyword gdal #}
{# @keyword identify #}
{# @keyword driver #}
{# @keyword format #}
{# @keyword recognize #}
{# @keyword dataset #}
{# @keyword 识别 #}
{# @keyword 驱动 #}
{# @concept "gdal dataset identify 是 GDAL 提供的通用工具，用于自动识别文件或目录的数据集格式和打开驱动" #}
{# @concept "该工具支持栅格和矢量数据集识别，可递归扫描目录结构" #}
{# @note 当文件格式无法识别时，可使用 --report-failures 标记查看具体失败信息 #}
{# @note 使用 --detailed 参数可获取更详细的元数据信息，如坐标系统、块大小等 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 指定的文件路径不存在或文件格式 GDAL 无法识别 #}
{# @common_error "Permission denied" — 对指定目录没有读取权限，无法进行递归扫描 #}
{# @param filename file_path required 要识别的文件或目录路径 #}
{# @param recursive boolean optional 递归扫描文件夹寻找数据集 #}
{# @param force_recursive boolean optional 强制递归扫描所有子文件夹（包括有效格式识别的文件夹） #}
{# @param detailed boolean optional 输出详细信息，包括地理参考信息和云优化GeoTIFF状态 #}
{# @param report_failures boolean optional 报告无法识别的文件类型 #}
{# @param of format optional 输出格式 options=GeoJSON,GPKG,CSV #}
{# @param output file_path optional 输出文件路径 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_dataset_identify

gdal dataset identify{% if recursive %} --recursive{% endif %}{% if force_recursive %} --force-recursive{% endif %}{% if detailed %} --detailed{% endif %}{% if report_failures %} --report-failures{% endif %}{% if of %} --output-format {{ of | quote }}{% endif %}{% if output %} --output {{ output | safe_path | quote }}{% endif %} {{ filename | safe_path | quote }}

REM Done
