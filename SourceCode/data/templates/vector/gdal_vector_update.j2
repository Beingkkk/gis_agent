{# @id gdal_vector_update #}
{# @name 矢量数据更新 #}
{# @description 使用 gdal vector update 基于键值或特征ID将输入矢量数据合并更新到现有输出矢量数据集中 #}
{# @keyword gpkg #}
{# @keyword shp #}
{# @keyword shapefile #}
{# @keyword geojson #}
{# @keyword merge #}
{# @keyword update #}
{# @keyword sync #}
{# @keyword 属性更新 #}
{# @keyword 数据同步 #}
{# @concept "gdal vector update 默认通过特征ID匹配输入与输出中的要素，或通过 --key 指定一个或多个字段组成复合键进行匹配" #}
{# @concept "merge模式下会同时更新已有要素并创建输入中独有的新要素；update-only仅更新已匹配要素；append-only仅追加新要素" #}
{# @note 输入与输出图层的坐标系统应保持一致，工具不会自动进行投影转换 #}
{# @note 若输入图层中存在某字段而输出图层中不存在，该字段不会被新建到输出中 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入或输出文件路径不存在，或格式驱动不可用 #}
{# @common_error "Key column not found" — 通过 --key 指定的字段在输入或输出图层中不存在 #}
{# @param input file_path required 输入矢量数据集路径（作为更新来源） #}
{# @param output file_path required 输出矢量数据集路径（被更新的现有数据集） #}
{# @param input_layer string optional 输入数据集中要使用的图层名称 #}
{# @param output_layer string optional 输出数据集中要更新的图层名称 #}
{# @param mode enum optional 更新模式 default=merge options=merge,update-only,append-only #}
{# @param key string optional 用于标识匹配特征的字段名称（可指定多个字段作为复合键） #}
{# @param input_format format optional 输入数据集的格式 options=GPKG,GeoJSON,ESRI Shapefile,KML,GML,PostgreSQL #}
{# @param input_open_option string optional 输入数据集的打开选项（KEY=VALUE格式） #}
{# @param output_open_option string optional 输出数据集的打开选项（KEY=VALUE格式） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_update

gdal vector update{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if mode %} --mode {{ mode | quote }}{% endif %}{% if key %} --key {{ key | quote }}{% endif %}{% if input_format %} --input-format {{ input_format | quote }}{% endif %}{% if input_open_option %} --open-option {{ input_open_option | quote }}{% endif %}{% if output_open_option %} --output-open-option {{ output_open_option | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
