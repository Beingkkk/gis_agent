{# @id gdal_raster_compare #}
{# @name 栅格数据对比 #}
{# @description 比较两个栅格数据集之间的像素值、元数据、坐标系等差异，并返回差异数量 #}
{# @concept "gdal raster compare 用于对比两个GDAL支持的栅格数据集，检查像素值、元数据、坐标系等多种属性的差异" #}
{# @concept "第一个数据集作为参考/标准数据集，第二个数据集与参考数据进行比较，程序以退出值返回差异数量" #}
{# @note 字节级二进制比较算作一个差异，如果只关心GDAL可见数据是否相同，差异数量为1（二进制差异）可视为可接受 #}
{# @note 程序可作为栅格处理流水线（pipeline）的最后一步使用 #}
{# @common_error "Unable to open datasource" — 输入的栅格文件路径不存在、文件格式不支持或文件已损坏 #}
{# @common_error "No differences found" — 两个栅格数据集完全相同，没有检测到任何差异（这在验证数据一致性时实际上是正常结果） #}
{# @param reference file_path required 参考栅格数据集路径（作为标准/基准数据） #}
{# @param input file_path required 待比较的输入栅格数据集路径 #}
{# @param skip_all_optional boolean optional 跳过所有可选的比较项（仅保留二进制比较） #}
{# @param skip_binary boolean optional 跳过字节级二进制比较 #}
{# @param skip_crs boolean optional 跳过坐标系（CRS）比较 #}
{# @param skip_geotransform boolean optional 跳过仿射变换参数比较 #}
{# @param skip_overview boolean optional 跳过金字塔/概视图比较 #}
{# @param skip_metadata boolean optional 跳过元数据比较 #}
{# @param skip_rpc boolean optional 跳过RPC元数据比较 #}
{# @param skip_geolocation boolean optional 跳过地理位置元数据比较 #}
{# @param skip_subdataset boolean optional 跳过子数据集比较 #}
{# @param quiet boolean optional 安静模式，不显示进度条和警告信息 #}
{# @param input_format string optional (auto-completed) #}
{# @param open_option string optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_compare

gdal raster compare{% if skip_all_optional %} --skip-all-optional{% endif %}{% if skip_binary %} --skip-binary{% endif %}{% if skip_crs %} --skip-crs{% endif %}{% if skip_geotransform %} --skip-geotransform{% endif %}{% if skip_overview %} --skip-overview{% endif %}{% if skip_metadata %} --skip-metadata{% endif %}{% if skip_rpc %} --skip-rpc{% endif %}{% if skip_geolocation %} --skip-geolocation{% endif %}{% if skip_subdataset %} --skip-subdataset{% endif %}{% if quiet %} -q{% endif %}{% if input_format %} --input-format {{ input_format | quote }}{% endif %}{% if open_option %} --open-option {{ open_option | quote }}{% endif %} {{ reference | safe_path | quote }} {{ input | safe_path | quote }}

REM Done
