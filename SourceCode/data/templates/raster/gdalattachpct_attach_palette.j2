{# @id gdalattachpct_attach_palette #}
{# @name 栅格色彩表附加 #}
{# @description 将色彩表从一个栅格文件附加到另一个栅格文件，生成RGB输出 #}
{# @concept "gdalattachpct 是一个 Python 工具，用于将色彩表的颜色映射从一个栅格复制应用到另一个栅格，常用于将单波段假彩色影像转换为多波段RGB真彩色影像" #}
{# @note 此工具需要 GDAL Python 绑定支持，Windows 环境下建议使用 OSGeo4W 安装程序安装 #}
{# @note 输出文件将自动变为多波段RGB格式，原单波段数据中的像素值将根据色彩表映射为红绿蓝通道值 #}
{# @common_error "Error: Unable to open input file" — 指定的色彩表源文件不存在或格式不受 GDAL 支持，确保文件路径正确且带有有效色彩表 #}
{# @common_error "does not have a color table" — 色彩表源文件中不包含任何色彩表（调色板）信息 #}
{# @param palette_file file_path required 色彩表源文件路径，支持带色彩表的栅格文件或颜色配置文件（txt, qml, qlr） #}
{# @param source_file file_path required 输入栅格文件路径（将被应用色彩表） #}
{# @param dest_file file_path required 输出RGB文件路径（将创建为RGB影像） #}
{# @param of string optional 输出格式名称，如 GTiff 等 #}

@echo off
REM Generated by GIS Agent
REM Template: gdalattachpct_attach_palette

gdalattachpct{% if of %} -of {{ of | quote }}{% endif %} {{ palette_file | safe_path | quote }} {{ source_file | safe_path | quote }} {{ dest_file | safe_path | quote }}

REM Done
