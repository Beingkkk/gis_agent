{# @id gdal_main #}
{# @name GDAL 统一命令行入口 #}
{# @description GDAL 主命令入口，作为统一的命令行接口分发到栅格、矢量、信息查询等子命令 #}
{# @keyword gdal #}
{# @keyword 栅格 #}
{# @keyword 矢量 #}
{# @keyword raster #}
{# @keyword vector #}
{# @keyword 转换 #}
{# @keyword info #}
{# @keyword 统一入口 #}
{# @concept "gdal 是 GDAL 工具集的统一入口命令，通过子命令分发给 raster（栅格）、vector（矢量）、info（信息查询）等专门的工具" #}
{# @concept "所有子命令共享通用选项如 --help、--config、--json-usage，同时每个子命令有自己独立的参数体系" #}
{# @note 使用 gdal --version 可查看 GDAL 版本信息，使用 gdal --drivers 可列出所有支持的驱动 #}
{# @note gdal <FILENAME> 等效于 gdal info <FILENAME>，gdal read <FILENAME> 等效于 gdal pipeline <FILENAME> #}
{# @seealso gdal_raster_info #}
{# @seealso gdal_raster_convert #}
{# @seealso gdal_vector_info #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unrecognized command" — 指定的子命令名称不存在，请检查命令拼写或使用 gdal --help 查看可用子命令列表 #}
{# @common_error "Unable to open datasource" — 文件名参数指向的数据源无法打开，可能是文件不存在、格式不支持或权限不足 #}
{# @param command enum required GDAL 子命令类型，决定执行的具体操作 options=convert,dataset,driver,info,mdim,pipeline,raster,vector,vsi #}
{# @param config string optional GDAL 配置选项，格式为 KEY=VALUE，可重复使用设置多个配置项 #}
{# @param filename file_path optional 当指定文件名时作为快捷方式，等同于 gdal info <文件名> 或 gdal pipeline <文件名> #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_main

gdal{% if command %} {{ command }}{% endif %}{% if config %} --config {{ config | quote }}{% endif %}{% if filename %} {{ filename | safe_path | quote }}{% endif %}

REM Done
