{# @id gdal_vector_grid #}
{# @name 矢量散点网格化 #}
{# @description 将矢量点数据通过多种插值方法生成规则栅格网格 #}
{# @keyword grid #}
{# @keyword interpolation #}
{# @keyword invdist #}
{# @keyword linear #}
{# @keyword nearest #}
{# @keyword 插值 #}
{# @keyword 散点 #}
{# @keyword 栅格化 #}
{# @keyword GDAL #}
{# @concept "gdal vector grid 将矢量点数据按指定插值算法（反距离加权、线性、三角网等）填入规则网格的每个像元，输出栅格结果" #}
{# @concept "搜索半径参数（rx/ry）定义了插值时每个像元中心点的邻域范围，用于确定参与计算的数据点" #}
{# @note 插值算法中 invdist（反距离加权）和 linear（线性三角网）是应用最广泛的两种方法 #}
{# @note 可通过设置 GDAL_NUM_THREADS=ALL_CPUS 开启多线程并行处理提升大数据量时的速度 #}
{# @seealso gdal_rasterize #}
{# @seealso gdal_grid_create #}
{# @common_error "No valid points found" — 输入矢量数据中无可用于插值的有效数值点，或指定的Z值字段不存在或为空 #}
{# @common_error "Could not fetch layer" — 输入文件路径错误或文件格式不支持，常见于指定了不存在的图层 #}
{# @param input file_path required 输入矢量数据源路径（包含散点数据的文件） #}
{# @param algorithm enum required 插值算法方法 options=invdist,invdistnn,average,nearest,linear,minimum,maximum,range,average-distance,average-distance-points,count #}
{# @param output file_path optional 输出栅格文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,GPKG,HFA,KEA,netCDF #}
{# @param res float optional 输出栅格像元分辨率（以地图单位计） #}
{# @param columns string optional 用于插值的数值型字段名称（Z值字段） #}
{# @param nodata float optional 无效值标记 #}
{# @param rx float optional 搜索椭圆X方向半径 #}
{# @param ry float optional 搜索椭圆Y方向半径 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_grid

gdal vector grid {{ algorithm | quote }}{% if of %} -f {{ of | quote }}{% endif %}{% if output %} -outfile {{ output | safe_path | quote }}{% endif %}{% if res %} -res {{ res | quote }}{% endif %}{% if columns %} -zfield {{ columns | quote }}{% endif %}{% if nodata %} -nodata {{ nodata | quote }}{% endif %}{% if rx %} -rx {{ rx | quote }}{% endif %}{% if ry %} -ry {{ ry | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
