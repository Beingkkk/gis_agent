{# @id gdal_vector_sql #}
{# @name 矢量数据SQL查询 #}
{# @description 使用SQL语句对矢量数据集进行查询、筛选和修改操作 #}
{# @keyword sql #}
{# @keyword 矢量查询 #}
{# @keyword ogr #}
{# @keyword sqlite #}
{# @keyword gdal #}
{# @keyword 属性查询 #}
{# @keyword 更新删除 #}
{# @concept "gdal vector sql 是 GDAL 3.x 提供的矢量数据SQL查询工具，支持执行SELECT查询以及UPDATE、DELETE等修改操作" #}
{# @concept "从 GDAL 3.12 起，结合 --update 选项可对输入数据集进行原地修改，无需指定输出路径" #}
{# @note SQL语句可从文件读取（使用 @filename 语法），且可以重复指定 --sql 执行多条语句 #}
{# @note 常用SQL方言包括 OGRSQL（默认）和 SQLITE（支持更丰富的函数和JOIN操作） #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在、文件格式不支持或文件已损坏 #}
{# @common_error "SQL syntax error" — SQL语句语法错误，或所选方言不支持该语法结构 #}
{# @param input file_path required 输入矢量数据集路径（支持多个文件） #}
{# @param output file_path optional 输出矢量数据集路径（省略则支持原地修改） #}
{# @param of format optional 输出格式名称 options=GDALG,GPKG,GeoJSON,ESRI Shapefile,SQLite,GML #}
{# @param dialect string optional SQL方言（如 OGRSQL、SQLITE） #}
{# @param output_layer string optional 输出图层名称 #}
{# @param sql string required 要执行的SQL语句或SQL文件路径（@filename） #}
{# @param update boolean optional 以更新模式打开现有数据集，允许执行UPDATE/DELETE等修改语句 #}
{# @param overwrite boolean optional 覆盖已存在的输出数据集 #}
{# @param overwrite_layer boolean optional 覆盖已存在的输出图层 #}
{# @param append boolean optional 追加到现有图层（与--upsert互斥） #}
{# @param upsert boolean optional Upsert方式写入要素（隐含append，与--append互斥） #}
{# @param skip_errors boolean optional 写入时跳过错误继续执行 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_sql

gdal vector sql {{ input | safe_path | quote }}{% if output %} {{ output | safe_path | quote }}{% endif %}{% if of %} --of {{ of | quote }}{% endif %}{% if dialect %} --dialect {{ dialect | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if sql %} --sql {{ sql | quote }}{% endif %}{% if update %} --update{% endif %}{% if overwrite %} --overwrite{% endif %}{% if overwrite_layer %} --overwrite-layer{% endif %}{% if append %} --append{% endif %}{% if upsert %} --upsert{% endif %}{% if skip_errors %} --skip-errors{% endif %}

REM Done
