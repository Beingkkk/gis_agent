{# @id gdal_driver_pdf_list_layers #}
{# @name PDF图层列表查询 #}
{# @description 列出PDF文件中的所有图层信息，支持JSON或文本格式输出 #}
{# @keyword pdf #}
{# @keyword 图层 #}
{# @keyword layers #}
{# @keyword gdal #}
{# @keyword 矢量 #}
{# @keyword 查询 #}
{# @keyword list #}
{# @concept "该工具用于读取PDF文件中的图层信息，返回的图层数据可能包含矢量要素或栅格图像" #}
{# @concept "JSON格式返回图层数组，text格式返回逐行列表" #}
{# @note 该命令是gdal driver pdf的子命令，用于PDF文件的元数据查询而非数据转换 #}
{# @note 默认输出为JSON格式，便于程序化处理 #}
{# @seealso gdal_driver_pdf_info #}
{# @common_error "Unable to open dataset" — 指定的PDF文件不存在或无法被GDAL识别 #}
{# @common_error "Not a valid PDF" — 输入文件不是有效的PDF格式，或该PDF不含可识别的图层 #}
{# @param input file_path required 输入的PDF文件路径（可以是包含栅格或矢量数据的PDF数据集） #}
{# @param output_format enum optional 输出格式类型 default=json options=json,text #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_driver_pdf_list_layers

gdal driver pdf list-layers{% if output_format %} -f {{ output_format | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
