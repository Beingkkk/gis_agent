{# @id gdal_dataset_rename #}
{# @name 数据集文件重命名 #}
{# @description 使用 gdal dataset rename 命令重命名 GDAL 数据集文件 #}
{# @keyword rename #}
{# @keyword 重命名 #}
{# @keyword dataset #}
{# @keyword 数据集 #}
{# @keyword gdal #}
{# @keyword 文件 #}
{# @concept "gdal dataset rename 是 GDAL 工具集中的一个实用程序，用于重命名一个或多个数据集文件" #}
{# @concept "该工具仅改变文件名，不会修改文件内容或元数据" #}
{# @note 重命名操作仅改变文件名，不进行格式转换或数据处理 #}
{# @note 使用 --overwrite 需谨慎，因为它会覆盖已存在的目标文件 #}
{# @common_error "Unable to open source dataset" — 源文件不存在或无法访问，请检查文件路径是否正确 #}
{# @common_error "Destination file already exists" — 目标文件名已存在，且未使用 --overwrite 参数 #}
{# @param source file_path required 源数据集名称（要重命名的文件路径） #}
{# @param destination file_path required 目标数据集名称（重命名后的文件路径） #}
{# @param format format optional 数据集格式（通过格式短名称指定，如 GTiff、HFA 等） options=GTiff,HFA,ENVI,ERS,GPKG,JPEG,PNG #}
{# @param overwrite boolean optional 是否允许覆盖已存在的目标文件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_dataset_rename

gdal dataset rename{% if format %} -f {{ format | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %} {{ source | safe_path | quote }} {{ destination | safe_path | quote }}

REM Done
