{# @id gdal_vector_simplify_coverage #}
{# @name 矢量覆盖范围简化 #}
{# @description 简化多边形数据集的边界，同时保持共享边界无缝隙或重叠 #}
{# @keyword simplify #}
{# @keyword boundary #}
{# @keyword polygon #}
{# @keyword coverage #}
{# @keyword shared edge #}
{# @keyword no gaps #}
{# @keyword no overlaps #}
{# @keyword visvalingam #}
{# @keyword 简化 #}
{# @keyword 边界保持 #}
{# @concept "gdal vector simplify-coverage 使用 Visvalingam-Whyatt 算法简化多边形边界的共享边，简化过程中不会产生缝隙或重叠" #}
{# @concept "此命令需要将整个数据集加载到内存中处理，因此可能不适合超大规模数据集" #}
{# @note 该工具仅简化共享边界，已有的缝隙或重叠不会被修复；如无需保持共享边界，可使用 gdal vector simplify 流式处理大数据 #}
{# @note 需要 GDAL 编译时链接 GEOS 3.12 及以上版本才能使用此功能 #}
{# @common_error "Too much memory requested" — 数据集过大，无法一次性加载到内存中，考虑使用 gdal vector simplify 替代 #}
{# @common_error "GEOS support not available" — 当前 GDAL 未编译 GEOS 支持，或 GEOS 版本低于 3.12 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param tolerance float required 简化的距离容差值（单位与数据集坐标单位相同） #}
{# @param input_layer string optional 输入图层名称 #}
{# @param of format optional 输出格式名称 options=GPKG,GeoJSON,ESRI Shapefile,GML #}
{# @param co text optional 数据集创建选项 (KEY=VALUE) #}
{# @param lco text optional 图层创建选项 (KEY=VALUE) #}
{# @param preserve_boundary boolean optional 保留外边界不被简化 #}
{# @param overwrite boolean optional 允许覆盖现有输出数据集 #}
{# @param update boolean optional 以更新模式打开现有数据集 #}
{# @param append boolean optional 允许追加到现有图层 #}
{# @param upsert boolean optional Upsert 要素（隐含追加模式） #}
{# @param output_layer string optional 输出图层名称 #}
{# @param skip_errors boolean optional 写入要素时跳过错误 #}
{# @param active_layer string optional 设置活跃图层名称（默认处理所有图层） #}
{# @param if format optional 输入格式名称 options=GPKG,GeoJSON,ESRI Shapefile,GML,KML #}
{# @param oo text optional 输入打开选项 (KEY=VALUE) #}
{# @param output_oo text optional 输出打开选项 (KEY=VALUE) #}
{# @param overwrite_layer boolean optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_simplify_coverage

gdal vector simplify-coverage{% if input_layer %} -l {{ input_layer | quote }}{% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if lco %} --lco {{ lco | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if overwrite_layer %} --overwrite-layer{% endif %}{% if append %} --append{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if skip_errors %} --skip-errors{% endif %}{% if active_layer %} --active-layer {{ active_layer | quote }}{% endif %}{% if preserve_boundary %} --preserve-boundary{% endif %}{% if if %} --if {{ if | quote }}{% endif %}{% if oo %} --oo {{ oo | quote }}{% endif %}{% if output_oo %} --output-oo {{ output_oo | quote }}{% endif %}{% if upsert %} --upsert{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }} {{ tolerance }}

REM Done
