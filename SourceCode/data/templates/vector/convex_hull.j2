{# @id convex_hull #}
{# @name 计算凸包 #}
{# @description 计算矢量数据集几何对象的最小凸多边形（凸包） #}
{# @keyword convex hull #}
{# @keyword convex #}
{# @keyword hull #}
{# @keyword polygon #}
{# @keyword envelope #}
{# @keyword geometry #}
{# @keyword vector analysis #}
{# @keyword 凸包 #}
{# @keyword 凸多边形 #}
{# @keyword shp #}
{# @keyword geojson #}
{# @concept "凸包（Convex Hull）是包含输入几何所有点的最小凸多边形，是计算几何中的基础操作" #}
{# @concept "凸包结果通常为 Polygon，但对于退化输入（如单点或共线点）可能返回 LineString 或 Point" #}
{# @note --append 和 --upsert 是互斥选项，不能同时使用 #}
{# @note 此命令也可作为 gdal vector pipeline 的一个处理步骤使用 #}
{# @seealso ogr2ogr_convert #}
{# @seealso buffer_zone #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或格式不支持，请检查路径和输入格式 #}
{# @common_error "Layer not found" — 指定的输入图层名称不存在，可用 --input-layer 指定正确的图层名 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param input_layer string optional 输入图层名称 #}
{# @param output_format format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML,PostgreSQL #}
{# @param output_layer string optional 输出图层名称 #}
{# @param creation_option string optional 数据集创建选项（KEY=VALUE 格式） #}
{# @param layer_creation_option string optional 图层创建选项（KEY=VALUE 格式） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param update boolean optional 以更新模式打开已存在的数据集 #}
{# @param append boolean optional 允许追加到现有图层 #}
{# @param upsert boolean optional Upsert 模式追加特征（隐含 --append） #}
{# @param active_layer string optional 设置活动图层名称 #}
{# @param active_geometry string optional 限定处理的几何字段名称 #}
{# @param skip_errors boolean optional 写入特征时跳过错误 #}
{# @param quiet boolean optional 静默模式（不显示进度条和警告信息） #}

@echo off
REM Generated by GIS Agent
REM Template: convex_hull

gdal vector convex-hull{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if output_format %} --output-format {{ output_format | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %}{% if layer_creation_option %} --layer-creation-option {{ layer_creation_option | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if append %} --append{% endif %}{% if upsert %} --upsert{% endif %}{% if active_layer %} --active-layer {{ active_layer | quote }}{% endif %}{% if active_geometry %} --active-geometry {{ active_geometry | quote }}{% endif %}{% if skip_errors %} --skip-errors{% endif %}{% if quiet %} -q{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
