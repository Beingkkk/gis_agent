{# @id gdal_cog_validate #}
{# @name COG验证工具 #}
{# @description 验证GeoTIFF文件是否符合Cloud Optimized GeoTIFF云优化标准并生成验证报告 #}
{# @keyword cog #}
{# @keyword cloud optimized #}
{# @keyword geotiff #}
{# @keyword validate #}
{# @keyword tiff #}
{# @keyword 验证 #}
{# @keyword 云优化 #}
{# @concept "Cloud Optimized GeoTIFF (COG) 是一种特殊格式的GeoTIFF文件，经过优化以支持HTTP Range请求，可直接从云存储中高效读取，而无需下载整个文件" #}
{# @concept "COG验证会检查文件的内部结构是否符合云优化规范，包括TIFF标签布局、块组织方式等" #}
{# @note 此工具依赖 GDAL Python 绑定，必须在安装有Python GDAL的环境中运行 #}
{# @common_error "GDAL Python bindings are not available" — 未正确安装GDAL Python绑定，需要通过pip install GDAL或系统包管理器安装对应版本的python-gdal #}
{# @common_error "File is not a TIFF file" — 输入文件不是有效的TIFF格式，可能文件损坏或扩展名错误 #}
{# @param input file_path required 要验证的COG数据集文件路径 #}
{# @param full_check enum optional 是否执行完整检查（auto自动检测，yes强制完整检查，no仅快速检查） default=auto options=auto,yes,no #}
{# @param quiet boolean optional 静默模式，不显示进度条或警告信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_cog_validate

gdal driver cog validate{% if full_check %} --full-check {{ full_check }}{% endif %}{% if quiet %} -q{% endif %} {{ input | safe_path | quote }}

REM Done
