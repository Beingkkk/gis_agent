{# @id gdal_raster_tri #}
{# @name 地形粗糙度指数计算 #}
{# @description 从DEM数据计算地形粗糙度指数(TRI)栅格，衡量像元与其周围邻域的高程差异 #}
{# @keyword DEM #}
{# @keyword TRI #}
{# @keyword 地形粗糙度 #}
{# @keyword 坡度 #}
{# @keyword 山体阴影 #}
{# @keyword 高程 #}
{# @keyword Riley #}
{# @keyword Wilson #}
{# @keyword dem #}
{# @concept "TRI（Terrain Ruggedness Index）地形粗糙度指数通过计算中心像元与周围3x3邻域8个像元的高程差绝对值之和来量化地形的起伏程度" #}
{# @concept "Riley算法默认将邻域值插值处理后计算，Wilson算法采用不同的加权方式" #}
{# @note 输入应为单波段高程数据（如DEM），输出为单波段浮点型栅格，值越大表示地形越破碎 #}
{# @note 若3x3窗口内存在NoData值，输出像元也会被设为NoData（默认值为-9999） #}
{# @seealso gdal_raster_aspect #}
{# @seealso gdal_raster_slope #}
{# @seealso gdal_raster_hillshade #}
{# @common_error "Unable to open" — 输入文件路径不存在或文件损坏，需确认DEM文件完整性 #}
{# @common_error "Too many bands" — 输入数据波段数不足指定值，需使用有效波段数量 #}
{# @param input file_path required 输入高程栅格数据（DEM）路径 #}
{# @param output file_path required 输出TRI栅格结果路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,COG,GPKG,HFA,ENVI #}
{# @param band integer optional 输入波段序号（从1开始，默认为1） default=1 #}
{# @param algorithm enum optional TRI计算算法 default=Riley options=Riley,Wilson #}
{# @param no_edges boolean optional 边缘不插值，直接设为Nodata #}
{# @param overwrite boolean optional 覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有文件 #}
{# @param co text optional 输出格式的创建选项（如COMPRESS=LZW） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_tri

gdal raster tri{% if of %} -f {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if algorithm %} --algorithm {{ algorithm | quote }}{% endif %}{% if no_edges %} --no-edges{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if co %} --co {{ co | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
