{# @id gdal_vector_make_valid #}
{# @name 修复无效几何 #}
{# @description 使用 GDAL 修复矢量数据中的无效几何要素，确保符合 Simple Features 规范 #}
{# @keyword geometry #}
{# @keyword valid #}
{# @keyword makevalid #}
{# @keyword repair #}
{# @keyword 修复 #}
{# @keyword 几何 #}
{# @keyword validity #}
{# @keyword GEOS #}
{# @keyword shp #}
{# @keyword gpkg #}
{# @concept "MakeValid 操作通过 OGRGeometry::MakeValid() 方法修复自相交、碎片化、重叠等无效几何问题" #}
{# @concept "默认使用 linework 算法将重叠多边形拆分为独立要素，structure 算法则保持拓扑关系" #}
{# @note 此命令需要 GDAL 编译时链接 GEOS 库方可使用 #}
{# @note 处理完成后会自动移除低维度几何组件（如孤立点），除非指定 --keep-lower-dim #}
{# @seealso ogr2ogr_convert #}
{# @common_error "GEOS support not enabled" — 当前 GDAL 版本未编译 GEOS 支持，无法使用 make-valid 功能，需重新编译 GDAL #}
{# @common_error "Skipped N features due to errors" — 部分要素因几何修复失败被跳过，可尝试添加 --skip-errors 参数忽略错误继续处理 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param format format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,GML,KML,SQLite #}
{# @param co string optional 数据集创建选项（KEY=VALUE 格式） #}
{# @param lco string optional 图层创建选项（KEY=VALUE 格式） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param update boolean optional 以更新模式打开已存在的数据集 #}
{# @param overwrite_layer boolean optional 允许覆盖已存在的输出图层 #}
{# @param append boolean optional 允许追加到已存在的图层 #}
{# @param output_layer string optional 输出图层名称 #}
{# @param skip_errors boolean optional 写入要素时跳过错误继续处理 #}
{# @param active_layer string optional 指定要处理的活动图层名称（不指定则处理所有图层） #}
{# @param active_geometry string optional 限制处理的几何字段名称（不指定则处理所有几何字段） #}
{# @param method enum optional 修复算法 default=linework options=linework,structure #}
{# @param keep_lower_dim boolean optional 保留 MakeValid() 处理后较低维度的几何组件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_make_valid

gdal vector make-valid{% if format %} -f {{ format | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if lco %} --lco {{ lco | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if overwrite_layer %} --overwrite-layer{% endif %}{% if append %} --append{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if skip_errors %} --skip-errors{% endif %}{% if active_layer %} --active-layer {{ active_layer | quote }}{% endif %}{% if active_geometry %} --active-geometry {{ active_geometry | quote }}{% endif %}{% if method %} --method {{ method | quote }}{% endif %}{% if keep_lower_dim %} --keep-lower-dim{% endif %}{{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
