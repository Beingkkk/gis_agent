{# @id gdal_raster_colormap #}
{# @name 栅格单波段色彩映射 #}
{# @description 使用颜色表将单波段栅格数据（如DEM）转换为RGB或RGBA彩色图像 #}
{# @keyword dem #}
{# @keyword hypsometric #}
{# @keyword color table #}
{# @keyword rgb #}
{# @keyword palette #}
{# @keyword color-map #}
{# @keyword 栅格着色 #}
{# @keyword 高程图 #}
{# @concept "颜色表（Color Map）是GDAL栅格数据中附带的调色板信息，每个像素值对应一个RGB颜色，常用于分类栅格或索引色图像的着色" #}
{# @concept "插值模式（interpolate）会在颜色表值之间进行平滑过渡，适合连续值数据如DEM；精确模式（exact）仅使用颜色表中定义的颜色" #}
{# @note 如果栅格本身自带颜色表（Color Table），可无需指定外部颜色表文件 #}
{# @note 对于DEM生成分层设色图（晕染地形图），常用插值模式和预设的颜色渐变文件 #}
{# @seealso gdal_raster_info #}
{# @seealso gdal_raster_reproject #}
{# @common_error "Input band does not have a color table" — 输入栅格波段没有关联的颜色表，且未提供外部颜色表文件 #}
{# @common_error "Color map file could not be opened" — 指定的颜色表文件路径不存在或格式错误 #}
{# @param input file_path required 输入栅格文件路径（通常为单波段数据，如DEM） #}
{# @param output file_path required 输出RGB/RGBA彩色栅格文件路径 #}
{# @param format format optional 输出格式名称 options=GTiff,PNG,JPEG,GIF,HFA,ENVI,MEM,KEA #}
{# @param band integer optional 输入栅格波段编号（从1开始） default=1 #}
{# @param color_map file_path optional 外部颜色表文件路径（文本格式，每行定义一个颜色映射） #}
{# @param color_selection enum optional 颜色计算方式 default=interpolate options=interpolate,exact,nearest #}
{# @param add_alpha boolean optional 添加透明通道（Alpha）到输出图像 #}
{# @param creation_options text optional 创建选项，以 KEY=VALUE 形式指定，可重复使用 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有输出文件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_colormap

gdal raster color-map{% if format %} -f {{ format | quote }}{% endif %}{% if creation_options %} --co {{ creation_options | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if band %} -b {{ band }}{% endif %}{% if color_map %} --color-map {{ color_map | quote }}{% endif %}{% if add_alpha %} --add-alpha{% endif %}{% if color_selection %} --color-selection {{ color_selection }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
