{# @id gdal_raster_clip #}
{# @name 栅格数据裁剪 #}
{# @description 使用 gdal raster clip 工具基于边界框或参考数据集裁剪栅格数据 #}
{# @keyword tiff #}
{# @keyword geotiff #}
{# @keyword clip #}
{# @keyword 裁剪 #}
{# @keyword bbox #}
{# @keyword 边界框 #}
{# @keyword 裁切 #}
{# @keyword 范围裁剪 #}
{# @concept "gdal raster clip 是 GDAL 的栅格裁剪工具，支持基于边界框坐标（WGS84 或其他坐标系统）或参考数据集进行裁剪" #}
{# @concept "裁剪时保持原始分辨率，像素位置取整不进行重采样，输出与输入使用相同的空间参考系统" #}
{# @note 必须指定 --bbox 或 --like 其中一个参数才能执行裁剪操作 #}
{# @note 边界框格式为 xmin,ymin,xmax,ymax，支持 EPSG 代码或 WKT 字符串指定坐标系统 #}
{# @seealso gdal_raster_warp #}
{# @common_error "Either --bbox or --like must be specified" — 未指定裁剪范围，必须使用 --bbox 定义边界框或使用 --like 指定参考数据集 #}
{# @common_error "Output file already exists" — 输出文件已存在，需要使用 --overwrite 覆盖或 --append 追加模式 #}
{# @param input file_path required 输入栅格数据文件路径 #}
{# @param output file_path required 输出栅格数据文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,COG,JPEG,PNG,HFA,ENVI,GPKG #}
{# @param bbox string optional 裁剪边界框，格式为 xmin,ymin,xmax,ymod #}
{# @param bbox_crs crs optional 边界框的坐标系统定义（如 EPSG:4326） #}
{# @param geometry text optional 裁剪几何要素（WKT 或 GeoJSON 格式） #}
{# @param geometry_crs crs optional 几何要素的坐标系统定义 #}
{# @param like file_path optional 参考数据集路径，用于定义裁剪范围模板 #}
{# @param only_bbox boolean optional 对于 geometry 和 like，仅使用其边界框进行裁剪 #}
{# @param add_alpha boolean optional 当源栅格无透明通道时，为目标添加透明通道 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有输出文件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_clip

gdal raster clip {% if input %} -i {{ input | safe_path | quote }}{% endif %}{% if output %} -o {{ output | safe_path | quote }}{% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if bbox %} --bbox {{ bbox | quote }}{% endif %}{% if bbox_crs %} --bbox-crs {{ bbox_crs | quote }}{% endif %}{% if geometry %} --geometry {{ geometry | quote }}{% endif %}{% if geometry_crs %} --geometry-crs {{ geometry_crs | quote }}{% endif %}{% if like %} --like {{ like | safe_path | quote }}{% endif %}{% if only_bbox %} --only-bbox{% endif %}{% if add_alpha %} --add-alpha{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}

REM Done
