{# @id gdal_raster_unscale #}
{# @name 栅格值反缩放 #}
{# @description 将栅格数据中的缩放值根据缩放系数和偏移量转换为原始未缩放值 #}
{# @keyword scale #}
{# @keyword offset #}
{# @keyword 栅格缩放 #}
{# @keyword 值转换 #}
{# @keyword 反缩放 #}
{# @keyword gdal raster #}
{# @concept "栅格缩放常用于存储整数类型数据以节省空间，通过 scale（缩放系数）和 offset（偏移量）将浮点值映射到整数范围，反缩放即还原这一过程" #}
{# @concept "反缩放公式：unscaled_value = scaled_value * scale + offset，当缺少元数据时像素值保持不变" #}
{# @note 该工具是 gdal raster scale 的逆操作，可将存储的缩放整数值恢复为实际物理量 #}
{# @note 默认输出数据类型根据输入类型自动确定（如 Byte/UInt16/Int16 等默认转为 Float32） #}
{# @seealso gdal_raster_scale #}
{# @common_error "Input band has no scale/offset metadata" — 输入栅格不包含缩放/偏移元数据，相应波段的值将保持不变，无法完成有效的反缩放转换 #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或文件格式不兼容，应检查文件完整性 #}
{# @param input file_path required 输入栅格文件路径或数据源（包含缩放值的栅格） #}
{# @param output file_path required 输出栅格文件路径或数据源（反缩放后的结果） #}
{# @param of format optional 输出格式名称 options=GTiff,NetCDF,GRIB,ENVI,HFA #}
{# @param ot enum optional 输出数据类型 options=UInt8,Int8,UInt16,Int16,UInt32,Int32,Int64,UInt64,Float16,Float32,Float64,CInt16,CInt32,CFloat32,CFloat64 #}
{# @param co text optional 创建选项（KEY=VALUE格式，可重复） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有输出文件（与overwrite互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_unscale

gdal raster unscale{% if of %} --format {{ of | quote }}{% endif %}{% if ot %} --datatype {{ ot | quote }}{% endif %}{% if co %} --creation-option {{ co | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
