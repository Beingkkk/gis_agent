{# @id vector_explode_collections #}
{# @name 分解几何集合 #}
{# @description 将矢量数据中的几何集合（如MULTIPOINT）拆分为独立要素 #}
{# @keyword explode #}
{# @keyword collection #}
{# @keyword multipoint #}
{# @keyword geometry #}
{# @keyword 几何集合 #}
{# @keyword 分解 #}
{# @keyword 拆散 #}
{# @concept "几何集合分解：将MULTIPOINT拆分为多个独立POINT要素，如MULTIPOINT(1 2, 3 4)会生成POINT(1 2)和POINT(3 4)两个要素" #}
{# @concept "分解不会递归进行，嵌套的几何集合如GEOMETRYCOLLECTION(GEOMETRYCOLLECTION(...))只分解一层" #}
{# @note 常用于将多部件几何转换为单部件几何，便于后续的空间分析操作 #}
{# @note 支持作为gdal vector pipeline的中间步骤使用 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或文件格式不被支持 #}
{# @common_error "Failed to change geometry type" — 输出格式不支持目标几何类型，或几何类型转换失败 #}
{# @param input file_path required 输入矢量文件路径或数据源 #}
{# @param output file_path required 输出矢量文件路径或数据源 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML #}
{# @param layer string optional 输入图层名称 #}
{# @param creation_option string optional 数据集创建选项（KEY=VALUE格式） #}
{# @param layer_creation_option string optional 图层创建选项（KEY=VALUE格式） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 允许追加到已存在的图层 #}
{# @param skip_on_type_mismatch boolean optional 当几何类型转换失败时跳过该要素 #}
{# @param output_layer string optional 输出图层名称 #}

@echo off
REM Generated by GIS Agent
REM Template: vector_explode_collections

gdal vector explode-collections{% if input %} {{ input | safe_path | quote }}{% endif %}{% if output %} {{ output | safe_path | quote }}{% endif %}{% if of %} --format {{ of | quote }}{% endif %}{% if layer %} --layer {{ layer | quote }}{% endif %}{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %}{% if layer_creation_option %} --layer-creation-option {{ layer_creation_option | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if skip_on_type_mismatch %} --skip-on-type-mismatch{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}

REM Done
