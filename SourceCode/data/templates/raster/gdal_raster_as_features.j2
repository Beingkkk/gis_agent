{# @id gdal_raster_as_features #}
{# @name 栅格转矢量点面 #}
{# @description 将栅格像素转换为矢量要素，与polygonize不同，不会合并相邻同值像素 #}
{# @concept "gdal raster as-features 将栅格每个像素转换为独立的矢量要素，相邻同值像素不会合并，适合需要保留像素独立性的分析场景" #}
{# @concept "与 gdal raster polygonize 的主要区别在于：polygonize 会将相邻同值像素合并为一个大面，而 as-features 保持每个像素独立" #}
{# @note geometry_type 默认为 none，表示只输出属性表而不创建几何形状，适合作为查找表使用 #}
{# @note 使用 --include-xy 或 --include-row-col 可以方便地在属性表中记录像素的空间位置信息 #}
{# @common_error "Unable to open datasource" — 输入栅格文件路径不存在或文件格式不受支持 #}
{# @common_error "Band 1 out of range" — 指定的波段编号超出栅格实际波段数量，应使用1到实际波段数的索引 #}
{# @param input file_path required 输入栅格数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param of string optional 输出矢量格式名称（如 GeoJSON、ESRI Shapefile、GML 等） #}
{# @param band integer optional 输入波段编号（从1开始的索引），支持多次指定多波段 #}
{# @param geometry_type string optional 输出几何类型：none（无几何，仅属性）、point（点要素）或polygon（面要素） default=none #}
{# @param skip_nodata boolean optional 跳过NoData值像素，不创建对应矢量要素 #}
{# @param include_xy boolean optional 在输出要素中添加像素中心点的X和Y坐标字段 #}
{# @param include_row_col boolean optional 在输出要素中添加像素的行号和列号字段 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 追加到现有图层而不是创建新图层 #}
{# @param co string optional (auto-completed) #}
{# @param lco string optional (auto-completed) #}
{# @param output_layer string optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_as_features

gdal raster as-features{% if of %} -f {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if geometry_type %} --geometry-type {{ geometry_type | quote }}{% endif %}{% if skip_nodata %} --skip-nodata{% endif %}{% if include_xy %} --include-xy{% endif %}{% if include_row_col %} --include-row-col{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if lco %} --lco {{ lco | quote }}{% endif %} {{ output | safe_path | quote }} {{ input | safe_path | quote }}

REM Done
