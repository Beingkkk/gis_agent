{# @id gdal_vector_dissolve #}
{# @name 矢量几何融合 #}
{# @description 使用 GDAL 对矢量要素几何执行并集运算，合并重叠多边形并消除重复顶点 #}
{# @keyword dissolve #}
{# @keyword union #}
{# @keyword 合并 #}
{# @keyword 融合 #}
{# @keyword 多边形 #}
{# @keyword shp #}
{# @keyword geojson #}
{# @concept "矢量融合通过执行几何并集运算来合并要素：消除重复顶点、在线要素相交处添加节点、合并重叠多边形为单一要素" #}
{# @concept "对于线要素，融合后还会执行线段合并操作，在恰好构成两个线段端点的节点处连接线段" #}
{# @note 仅融合单个要素的几何，若需将多个要素合并为一个，需先用 gdal vector combine 工具 #}
{# @note 融合可作为 gdal vector pipeline 流程中的一个处理步骤 #}
{# @seealso gdal_vector_combine #}
{# @seealso gdal_vector_pipeline #}
{# @common_error "Unsupported operation on this layer" — 指定的图层类型不支持融合操作（如点要素图层通常无法进行融合） #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在、格式不支持或缺少相应驱动 #}
{# @param input file_path required 输入矢量数据源路径 #}
{# @param output file_path required 输出矢量数据目标文件路径 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML #}
{# @param layer string optional 输入图层名称 #}
{# @param output_layer string optional 输出图层新名称 #}
{# @param overwrite boolean optional 是否覆盖已存在的输出数据集 #}
{# @param update boolean optional 以更新模式打开已存在的数据源 #}
{# @param append boolean optional 追加到现有图层（与 upsert 互斥） #}
{# @param active_layer string optional 设置活跃图层名称 #}
{# @param active_geometry string optional 限制处理的几何字段名称 #}
{# @param co text optional 数据集创建选项（KEY=VALUE 格式，多个选项可重复使用） #}
{# @param lco text optional 图层创建选项（KEY=VALUE 格式） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_dissolve

gdal vector dissolve{% if of %} -f {{ of | quote }}{% endif %}{% if layer %} -l {{ layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if append %} --append{% endif %}{% if active_layer %} --active-layer {{ active_layer | quote }}{% endif %}{% if active_geometry %} --active-geometry {{ active_geometry | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if lco %} --lco {{ lco | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
