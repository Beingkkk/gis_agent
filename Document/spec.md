# GIS Agent 产品需求规格说明书

| 项目 | 内容 |
|------|------|
| 项目代号 | `gis-agent` |
| 版本 | v1.5.0 |
| 状态 | 设计基线 |
| 目标读者 | 系统设计师、后端开发工程师 |

---

## 目录

1. [项目概述](#1-项目概述)
2. [用户与场景](#2-用户与场景)
3. [功能需求（MoSCoW）](#3-功能需求moscow)
   - 3.1 [Must — 必须实现](#31-must--必须实现)
   - 3.2 [Should — 应该实现](#32-should--应该实现)
   - 3.3 [Could — 可以扩展](#33-could--可以扩展)
   - 3.4 [Won't — 暂不实现](#34-wont--暂不实现)
4. [技术约束与选型](#4-技术约束与选型)
5. [工作空间与记忆机制](#5-工作空间与记忆机制)
   - 5.1 [工作空间（Workspace）](#51-工作空间workspace)
   - 5.2 [会话内记忆（短期）](#52-会话内记忆短期)
   - 5.3 [任务入口手册 Agents.md（长期）](#53-任务入口手册-agentsmd长期)
6. [关键设计原则](#6-关键设计原则)
7. [验收标准](#7-验收标准)
8. [后续设计方向](#8-后续设计方向)

---

## 1. 项目概述

### 1.1 目标

为内部 GIS 团队提供一个**数据处理助手**，支持命令行和浏览器两种交互方式，具备以下核心能力：

| 能力 | 说明 |
|------|------|
| 文档问答 | 基于本地 GDAL 帮助文档，解答工具使用问题 |
| 任务脚本化 | 接受自然语言数据处理需求，生成可审查的批处理脚本 |
| 安全执行 | 用户确认后在指定工作空间内执行脚本 |

### 1.2 定位

- **维护模式**：单人维护
- **使用范围**：内部工具，不对外发布
- **交互方式**：命令行（CLI）和浏览器界面（Browser UI）并行支持，用户按需选择
- **设计哲学**：简单可靠、零外部服务依赖

---

## 2. 用户与场景

### 2.1 目标用户

团队内的 **GIS 分析师** 与 **数据工程师**，具备基础空间数据概念知识。

### 2.2 典型使用场景

| 场景编号 | 用户输入示例 |
|----------|-------------|
| S1 | "把这三个省的 shp 合并成一个 GeoJSON，坐标系转 4326" |
| S2 | "用这个边界 shp 裁剪那批 tif" |
| S3 | "ogr2ogr 里怎么忽略无效字段？" |

---

## 3. 功能需求（MoSCoW）

### 3.1 Must — 必须实现

以下功能构成系统的最小可用产品（MVP），必须在首期交付。

| ID | 功能模块 | 需求描述 | 关键设计点 |
|:---:|---------|---------|-----------|
| **F1** | **文档问答** | 基于 J2 模板元数据（`@concept`、`@note`、`@common_error` 等）和 LLM 参数知识回答用户问题。基础概念由 LLM 直接回答；用法指导基于模板元数据；错误诊断结合模板 `@common_error` 与 LLM 推理。 | 模板注释格式扩展；Q&A 分层策略；模板匹配逻辑 |
| **F2** | **意图→模板映射** | 将用户自然语言分类到预定义的工作流模板（如 `shp2geojson`、`clip_raster`）。 | 模板注册表；意图分类 Prompt 设计；兜底策略 |
| **F3** | **参数抽取与追问** | 从输入中提取文件路径、坐标参考、选项等参数；缺失时主动向用户追问。 | 参数 Schema 定义；必填/可选校验规则；追问话术模板 |
| **F4** | **脚本生成** | 根据模板 + 参数，使用 Jinja2 渲染生成可执行的批处理脚本（`.bat`/`.sh`）。 | 模板语法规范；跨平台路径处理；参数转义安全 |
| **F5** | **用户确认与执行** | 向用户完整展示脚本内容，要求明确确认后执行。CLI 模式使用 Y/N 文本确认；UI 模式下提供独立的脚本执行视图，用户调整参数后点击刷新即可预览生成的命令，确认后执行。 | 确认交互流程；命令预览刷新机制；执行超时控制；进程隔离 |
| **F6** | **安全与隔离** | 默认操作目录为工作空间，支持外部数据路径；禁止静默覆盖（输出文件默认加时间戳）；支持 `--dry-run` 空跑模式。 | 路径规范化；文件名时间戳规则；空跑模式实现 |
| **F7** | **工作空间管理** | 支持启动时指定工作空间（`--workspace`）作为默认 cwd 和路径解析基准；数据路径不限定在该目录内。 | CLI 参数解析；工作空间路径规范化；目录存在性校验 |
| **F8** | **会话内记忆** | 单次对话中保留上下文（多轮追问和补充），避免信息丢失。 | 消息列表结构；Token 预算管理；上下文截断策略 |

### 3.2 Should — 应该实现

以下功能在 MVP 完成后优先实现，提升用户体验。

| ID | 功能模块 | 需求描述 | 依赖 |
|:---:|---------|---------|------|
| **F9** | **多步任务** | 支持简单的链式操作，如"裁剪 → 重投影 → 转格式"。 | F2、F4 |
| **F10** | **错误诊断** | 执行失败时，捕获 GDAL 错误信息并给出修复建议。CLI 模式下直接展示诊断文本；UI 模式下支持"一键诊断"：自动收集模板元数据、执行命令、参数值、stdout/stderr 等上下文，发送到 LLM 进行深度诊断。 | F5、F1 |
| **F14** | **命令预览** | UI 模式下，用户调整参数后点击"预览命令"按钮，实时查看根据当前参数生成的 GDAL 命令。参数未填完时允许预览但禁用执行。 | F3、F4 |
| **F15** | **一键诊断** | UI 模式下，脚本执行失败时，用户点击"一键诊断"按钮，系统自动收集模板信息、参数值、执行命令、stdout/stderr、返回码等上下文，发送到 GIS 问答区域进行 LLM 诊断；诊断前自动清空问答历史，确保诊断上下文独立。 | F1、F5、F10 |

### 3.3 Could — 可以扩展

以下功能作为未来可选增强项，当前阶段仅预留接口。

| ID | 功能模块 | 需求描述 | 备注 |
|:---:|---------|---------|------|
| **F12** | **处理历史** | 记录每次执行的脚本和日志到工作空间内，便于回溯。 | 需设计日志目录结构与轮转策略 |
| **F13** | **可视化预览** | 生成前后数据的基本统计或快速地图截图（需额外轻量工具）。 | 强依赖外部可视化库，暂不纳入核心 |

### 3.4 Won't — 暂不实现

以下功能明确排除在当前版本范围外：

- 复杂空间分析（网络分析、空间统计）
- 多用户权限管理

> **注**：v1.1.0 中曾将"原生桌面 GUI 应用（Electron）"列入 Won't，现已实现并作为 UI 模式的唯一入口，该约束已解除。

---

## 4. 技术约束与选型

### 4.1 依赖栈

| 层级 | 技术项 | 版本/说明 |
|------|--------|----------|
| 运行环境 | Python | 3.10+ |
| GDAL 环境 | Conda + conda-forge | `conda install -c conda-forge gdal` |
| GDAL CLI 工具 | `ogr2ogr`、`gdalwarp`、`gdal_translate` 等 | 随 Conda 包提供 |
| LLM API | Claude 兼容 API | 支持切换本地模型 |
| 模板引擎 | **Jinja2** | 脚本渲染与知识元数据载体 |
| 核心依赖包 | `anthropic`、`jinja2` | 仅两项生产第三方库（P5 约束） |
| 浏览器 UI 后端 | `fastapi`、`uvicorn[standard]`、`websockets` | 仅用于 UI 模式，放入 `dev` 依赖组，CLI 模式不加载 |

### 4.2 LLM 使用边界

**原则**：代码决定场景，LLM 只负责回答，不猜意图。每个场景有独立的系统 Prompt，LLM 无需在单个 Prompt 中承担多个角色。

| 场景 | 代码判断依据 | LLM 职责 | 使用的 Prompt |
|------|------------|---------|--------------|
| 意图识别 | 用户在 DiscoveryTab 输入 | 从候选模板中选择最匹配项 | `build_intent_prompt()` |
| 模板知识问答 | `locked_template is not None` | 基于模板上下文回答用法问题 | `build_template_qa_prompt()` |
| GIS 专家问答 | `locked_template is None` | 用参数知识回答通用 GIS 问题 | `build_gis_expert_prompt()` |
| 参数抽取 | 进入 PARAM_COLLECT 状态 | 从输入中提取参数值 | `build_param_prompt()` |
| 错误诊断 | 进入 ERROR_RECOVERY 状态 | 分析错误根因并给出修复建议 | `build_diagnosis_prompt()` |

**禁止**:
- 直接生成 GDAL 命令字符串（P1）
- 直接执行文件系统操作
- 脱离模板上下文自由编造命令用法

### 4.3 架构约束

- **会话状态**：单次对话内保留上下文（内存列表），退出即丢弃
- **持久化策略**：初期不持久化会话历史
- **依赖最小化**：新增依赖仅限 Jinja2，要求轻量、成熟、无复杂配置

#### 4.3.1 前后端通信协议选择

前端与 API 层通信分两种协议，必须按场景严格区分（constitution.md CODE-5）：

| 场景 | 协议 | 理由 |
|------|------|------|
| 状态流转、数据查询、模板管理 | **HTTP REST** | 请求-响应，即时返回完整结果 |
| **LLM 流式输出**（Q&A 对话） | **WebSocket** | 逐块推送，避免 HTTP 超时截断 |
| **实时日志推送**（脚本/Pipeline 执行） | **WebSocket** | subprocess stdout/stderr 逐行实时推送 |
| 模板生成/校验/保存 | HTTP REST | 同步计算，无需流式 |

**约束**：禁止前端用同步 HTTP 调用替代 WebSocket 处理流式/实时场景。

### 4.4 模板知识源可靠性说明

本节说明系统如何通过 J2 模板元数据确保文档问答（F1）的回答基于**结构化、可执行、人工验证**的知识，而非自由检索或 LLM 幻觉。

#### 4.4.1 知识双层结构

| 层级 | 来源 | 用途 | 准确性保障 |
|------|------|------|-----------|
| **基础概念** | LLM 参数知识 | 回答"TIF 是什么""EPSG 是什么"等概念性问题 | LLM 训练数据中的通用知识 |
| **用法指导** | J2 模板元数据（`@concept`、`@note`、`@param`） | 回答"怎么把 SHP 转成 GeoJSON""参数怎么填" | 人工编码的可执行模板，与系统行动能力同源 |
| **错误诊断** | `@common_error` + LLM 推理 | 分析执行失败原因并给出修复建议 | 模板中记录的典型错误模式 |

#### 4.4.2 模板作为知识卡片的机制

每个 `.j2` 模板同时承载"可执行脚本"和"结构化知识"双重角色：

```
模板文件: vector/shp2geojson.j2
    │
    ├──→ 执行角色: 渲染为 ogr2ogr 命令脚本
    │
    └──→ 知识角色:
            ├── @description —— 功能描述（一句话）
            ├── @concept "GeoJSON" —— 格式概念解释
            ├── @param input file_path —— 参数定义与约束
            ├── @note "输出路径自动加时间戳" —— 使用提示
            └── @common_error "Unable to open datasource" —— 常见错误
```

#### 4.4.3 约束保证

| 约束层级 | 机制 | 效果 |
|---------|------|------|
| **数据源约束** | 模板元数据与可执行脚本同源，人工编写并纳入 Git 版本控制 | 知识质量可控，可追溯 |
| **场景隔离** | 代码根据 `locked_template` 是否存在选择专用 Prompt，LLM 不判断场景 | 消除 LLM 场景误判（如问"参数怎么填"时无模板上下文导致错误搜索模板） |
| **Prompt 约束** | 模板知识问答 Prompt 明确 instruct："所有回答必须基于提供的模板上下文，禁止编造模板中未定义的参数" | 禁止 LLM 凭记忆编造命令用法 |
| **行动一致性** | Q&A 回答的知识与后续脚本生成使用同一套模板 | 消除"说得对但做不了"的脱节 |

#### 4.4.4 潜在偏差与缓解

| 风险 | 原因 | 缓解措施 |
|------|------|---------|
| 模板覆盖不全 | 新增 GDAL 功能暂无对应模板 | 开发新模板时以 GDAL HTML 文档为参考，将关键知识编码进元数据 |
| LLM 概念幻觉 | 基础概念回答依赖 LLM 参数知识，有训练截止日期 | GDAL 核心概念变化极慢；对版本特定细节引导用户到模板用法 |
| 模板元数据缺失 | 老模板未补充 `@concept`/`@common_error` | 渐进式补充，作为模板维护的一部分 |

---

## 5. 工作空间与记忆机制

### 5.1 工作空间（Workspace）

#### 定义

一个文件系统目录，作为默认操作上下文（cwd）和输出文件的基准目录。切换工作空间不影响当前会话状态。

#### 设置方式

| 方式 | 优先级 | 示例 |
|------|--------|------|
| 启动参数 `--workspace` | 高 | `python main.py --workspace /path/to/project` |
| 默认回退 | 低 | 当前工作目录（`os.getcwd()`） |

#### 约束规则

| 规则编号 | 规则描述 | 校验时机 |
|:--------:|---------|---------|
| W1 | 所有生成脚本和输出文件默认放置在工作空间下 | 脚本生成时 |
| W2 | 脚本执行时，将工作空间作为进程的当前目录（`cwd`） | 脚本执行时 |
| W3 | 文件路径规范化：所有路径经 `resolve()` 规范化后使用 | 参数校验时 |

### 5.2 会话内记忆（短期）

| 属性 | 说明 |
|------|------|
| **存储介质** | Python 内存中的 `messages` 列表（`List[Dict]`） |
| **内容** | 保留用户和助手的所有对话轮次 |
| **用途** | 支持多轮追问和补充信息 |
| **生命周期** | Agent 进程启动时创建，退出时销毁，不写入磁盘 |

**多轮对话示例：**

```
User:  "把 roads.shp 转成 GeoJSON"
Agent: "已识别任务：shp2geojson。输出文件名是什么？"
User:  "输出叫 roads_out.json，放当前目录"
Agent: [生成脚本并请求确认]
```

## 6. 关键设计原则

以下原则为系统设计的最高约束，所有模块实现必须遵守。

| 编号 | 原则名称 | 原则内容 | 违规后果 |
|:----:|---------|---------|---------|
| P1 | **模板化命令，杜绝幻觉** | LLM **绝不直接生成 GDAL 命令**。仅负责选择模板 ID 并填充参数，实际命令由 Jinja2 模板渲染生成。 | 命令错误、数据损坏 |
| P2 | **先展后行** | 任何脚本必须向用户完整展示内容，获得明确确认后才执行。 | 误操作、不可逆数据丢失 |
| P3 | **最小权限** | 默认在工作空间内操作，输出文件默认加时间戳防覆盖；支持外部数据路径。 | 文件被覆盖 |
| P4 | **模板知识唯一** | 用法指导类知识仅来源于 J2 模板元数据，不混合外部网络内容。基础概念类回答由 LLM 参数知识提供。 | 回答不可靠、与系统行动能力脱节 |
| P5 | **依赖极简** | 生产运行时依赖锁定为 `anthropic` 和 `jinja2`。浏览器 UI 所需的 `fastapi` 等库作为可选 `dev` 依赖，CLI 模式不加载。不引入未经批准的第三方库。 | 部署困难、维护成本上升 |

---

## 7. 验收标准

### 7.1 功能验收清单

| 编号 | 验收项 | 测试输入 | 期望输出/行为 |
|:----:|--------|---------|-------------|
| AC-1 | 文档问答 | "ogr2ogr 能输出哪些格式？" | 能从本地文档中检索并回答几种常见驱动名称 |
| AC-2 | 脚本生成与确认 | "将 `data/roads.shp` 转为 GeoJSON" | 展示如下脚本并等待用户确认：<br>`ogr2ogr -f "GeoJSON" data/roads.json data/roads.shp` |
| AC-3 | 错误处理 | 用不存在的输入文件执行 | 能捕获 GDAL 错误并向用户提示原因 |
| AC-4 | 工作空间隔离 | 指定 `--workspace /tmp/test` | 所有生成文件和执行命令均在该目录下，不影响其他位置 |

### 7.2 非功能验收标准

| 属性 | 标准 |
|------|------|
| 启动时间 | CLI 模式 < 2 秒；UI 模式（后端）< 3 秒 |
| 响应延迟 | 简单意图识别 < 2 秒；文档问答 < 5 秒 |
| 生产依赖数量 | ≤ 2 个（`anthropic`、`jinja2`）；UI 可选依赖不纳入生产计数 |

---

## 8. 后续设计方向

本规格作为后续系统设计的唯一依据。下一阶段的详细设计可围绕以下模块展开：

1. **命令模板结构** — 模板注册表 Schema、参数定义规范、Jinja2 模板语法约定、知识元数据标签规范
2. **模板知识问答策略** — 基础概念/用法指导/错误诊断三层路由、模板匹配逻辑、Prompt 设计
3. **交互状态机** — 用户对话状态流转（空闲 → 意图确认 → 参数收集 → 脚本展示 → 执行）
4. **安全校验层** — 路径规范化实现、命令注入防护、脚本执行机制
5. **浏览器界面** — React + FastAPI 双入口架构、WebSocket 流式推送、Pipeline 可视化编辑、J2 模板生成器 UX

---

## 附录 A：术语表

| 术语 | 定义 |
|------|------|
| **MoSCoW** | 需求优先级分类法：Must（必须）、Should（应该）、Could（可以）、Won't（暂不） |
| **RAG** | Retrieval-Augmented Generation，检索增强生成 |
| **工作空间** | 用户指定的文件系统根目录，所有操作的边界 |
| **模板** | 预定义的 Jinja2 脚本模板，对应一类标准 GIS 操作 |

---

## 附录 B：修订记录

| 版本 | 日期 | 修订内容 |
|------|------|---------|
| v1.0.0 | 2026-05-26 | 初版发布，定义 CLI 模式 MVP 需求与五大设计原则 |
| v1.1.0 | 2026-05-29 | 扩展为双入口架构：§1.1 目标改为"支持命令行和浏览器两种交互方式"；§1.2 新增"交互方式"项；§3.4 Won't 中"GUI 界面"改为"原生桌面 GUI 应用"；§4.1 加入浏览器 UI 后端依赖；§6 P5 明确区分生产依赖与 UI 可选依赖；§7.2 非功能验收更新；§8 加入浏览器界面设计方向 |
| v1.4.0 | 2026-05-31 | **移除 Agents.md + 工作空间行为精简**：删除 §5.3 Agents.md 章节、F11 需求项、AC-5 验收标准；§5.1 更新工作空间定义为"默认操作目录"；切换工作空间不再清空会话状态，仅变更默认 cwd 和输出基准目录 |
| v1.3.0 | 2026-05-31 | **LLM 场景隔离 + API 分离**：§4.2 更新 LLM 使用边界为场景表格（5 个专用 Prompt）；§4.4.3 新增"场景隔离"约束；附录新增 v1.3.0 修订。代码层面根据 UI Tab 和 `locked_template` 状态选择 Prompt，LLM 不再通过关键词猜测用户意图；新增 `POST /session/{id}/chat` API 作为 Q&A 专用入口 |
| v1.2.0 | 2026-05-31 | **UI 模式架构升级**：§3.1 F5 扩展"命令预览"机制；§3.2 F10 扩展"一键诊断"；新增 F14（命令预览）、F15（一键诊断）；§3.4 Won't 移除已实现的"原生桌面 GUI"限制；确立 Electron UI 三 TAB 架构（模板识别 / GIS 问答 / 脚本执行）为 UI 模式唯一入口 |
| v1.5.0 | 2026-06-01 | **新增前后端通信协议约束**：§4.3.1 新增协议选择原则表，明确 HTTP REST 与 WebSocket 的场景边界；constitution.md CODE-5 同步约束流式交互必须走 WebSocket，防止重复 Q&A HTTP 超时问题 |
