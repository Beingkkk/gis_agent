{# @id gdal_vsi #}
{# @name GDAL虚拟文件系统管理 #}
{# @description 通过GDAL虚拟文件系统(VSI)对云存储等远程文件进行复制、删除、列表、搬移、同步等管理操作 #}
{# @keyword vsi #}
{# @keyword 虚拟文件系统 #}
{# @keyword cloud #}
{# @keyword s3 #}
{# @keyword oss #}
{# @keyword copy #}
{# @keyword delete #}
{# @keyword list #}
{# @keyword move #}
{# @keyword sync #}
{# @keyword remote #}
{# @keyword 远程文件 #}
{# @concept "GDAL虚拟文件系统(VSI)是一种统一访问机制，允许通过标准文件路径语法访问本地文件、内存、HTTP/HTTPS、云存储(S3/OSS/GCS/Azure)等不同后端" #}
{# @concept "VSI路径通常以 /vsi/ 前缀开头，如 /vsis3/、/vsicurl/、/vsimem/、/vsizip/ 等" #}
{# @note 所有子命令都支持 --json-usage 选项以 JSON 格式输出用法说明 #}
{# @note copy 和 sync 命令可用于在本地与云存储之间传输数据 #}
{# @seealso gdal_vsi_copy #}
{# @seealso gdal_vsi_sync #}
{# @common_error "Unable to open" — VSI路径格式错误或指定的后端存储服务未正确配置（如云存储凭据缺失） #}
{# @common_error "No such file or directory" — 指定的源路径不存在，或目标路径的父目录缺失 #}
{# @param subcommand enum required VSI子命令操作类型 options=copy,delete,list,move,sozip,sync #}
{# @param source file_path optional 源文件或目录路径(VSI路径，支持 /vsi/ 前缀格式) #}
{# @param target file_path optional 目标文件或目录路径(VSI路径) #}
{# @param recursive boolean optional 递归处理子目录（用于 delete、list 等操作） #}
{# @param config string optional GDAL配置选项，格式为 KEY=VALUE（如 VSI_CACHE=TRUE） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vsi

gdal vsi {{ subcommand }}{% if source %} {{ source | quote }}{% endif %}{% if target %} {{ target | quote }}{% endif %}{% if recursive %} -r{% endif %}{% if config %} --config {{ config | quote }}{% endif %}

REM Done
