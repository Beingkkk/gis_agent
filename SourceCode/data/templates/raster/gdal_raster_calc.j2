{# @id gdal_raster_calc #}
{# @name 栅格代数计算 #}
{# @description 对栅格数据集进行像素级代数运算，支持多波段处理和复杂数学表达式 #}
{# @keyword raster #}
{# @keyword calc #}
{# @keyword algebra #}
{# @keyword pixel #}
{# @keyword band #}
{# @keyword 运算 #}
{# @keyword 栅格代数 #}
{# @keyword gdal_calc #}
{# @concept "gdal raster calc 是 GDAL 3.12+ 的栅格代数计算工具，支持基于 muparser 的数学表达式（如 A+B、A*2、sin(A)）和 builtin 内置聚合函数（如 sum、mean、min、max）" #}
{# @concept "多波段数据中每个波段可用不同变量名（A、B、C...）引用，单波段文件默认用变量 X 引用" #}
{# @note 表达式中的变量名需与输入文件前缀对应，如 A=input1.tif B=input2.tif，表达式写 A+B #}
{# @note 输出格式默认从扩展名推断，--overwrite 和 --append 互斥，不可同时使用 #}
{# @common_error "Error: could not parse expression" — 表达式语法错误，请检查括号匹配、变量名拼写及支持的运算符 #}
{# @common_error "Dimensions of input files do not match" — 输入栅格尺寸不一致，请添加 --no-check-extent 参数（要求所有输入维度完全相同） #}
{# @param input file_path required 输入栅格文件路径，用于计算的主数据集（多文件可用表达式中的变量名如 A=file1.tif B=file2.tif 引用） #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param calc string required 代数计算表达式（如 A*2+B-1 或 sin(X)*cos(X)），支持 muparser（默认）或 builtin 方言 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,HFA,ENVI,EHdr,GPKG,VRT,COG,MBTiles #}
{# @param ot enum optional 输出数据类型 options=UInt8,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,Float32,Float64 #}
{# @param nodata string optional 指定输出 NoData 值（可设 'nan'、'inf'、'-inf' 或具体数值） #}
{# @param dialect enum optional 表达式解析器方言，builtin 支持内置聚合函数（如 sum、mean） default=muparser options=muparser,builtin #}
{# @param flatten boolean optional 生成单波段输出，每个表达式输出一个波段 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param propagate_nodata boolean optional 当任意输入像素为 NoData 时，将对应输出像素设为 NoData 值 #}
{# @param no_check_crs boolean optional 不检查输入坐标系的一致性 #}
{# @param no_check_extent boolean optional 不检查输入空间范围的一致性（此时要求输入维度相同） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_calc

gdal raster calc{% if of %} --format {{ of | quote }}{% endif %}{% if ot %} --output-data-type {{ ot | quote }}{% endif %}{% if nodata %} --nodata {{ nodata | quote }}{% endif %}{% if dialect %} --dialect {{ dialect | quote }}{% endif %}{% if flatten %} --flatten{% endif %}{% if no_check_crs %} --no-check-crs{% endif %}{% if no_check_extent %} --no-check-extent{% endif %}{% if propagate_nodata %} --propagate-nodata{% endif %}{% if overwrite %} --overwrite{% endif %} --calc {{ calc | quote }} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
