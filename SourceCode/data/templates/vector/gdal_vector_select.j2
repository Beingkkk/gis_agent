{# @id gdal_vector_select #}
{# @name 矢量字段选择 #}
{# @description 从矢量数据集中选择指定字段子集，生成新的数据集 #}
{# @keyword shp #}
{# @keyword geojson #}
{# @keyword gpkg #}
{# @keyword 字段选择 #}
{# @keyword 属性筛选 #}
{# @keyword gdal vector #}
{# @concept "gdal vector select 是 GDAL 矢量数据处理工具，用于从矢量数据中提取特定字段生成新数据集" #}
{# @concept "该工具既可作为独立命令使用，也可嵌入 gdal vector pipeline 工作流中作为中间步骤" #}
{# @note 字段参数支持同时指定多个字段名称，逗号分隔 #}
{# @note 输出格式默认根据文件扩展名自动判断 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "UnauthorizedAccess" — 输出文件路径所在目录无写入权限，或者文件已存在且被其他程序占用 #}
{# @common_error "Field xxx not found" — 指定的字段名称在数据源中不存在，可使用 --ignore-missing-fields 忽略此类错误 #}
{# @param input file_path required 输入矢量数据源路径 #}
{# @param output file_path required 输出矢量数据源路径 #}
{# @param fields string required 要选择的字段名称（多个字段用逗号分隔） #}
{# @param of format optional 输出格式名称 default=GeoJSON options=GeoJSON,ESRI Shapefile,GPKG,GML,KML,MapInfo File #}
{# @param layer string optional 输入图层名称（不指定则处理所有图层） #}
{# @param output_layer string optional 输出图层名称 #}
{# @param exclude boolean optional 排除指定字段而非选择（与ignore-missing-fields互斥） #}
{# @param ignore_missing_fields boolean optional 忽略不存在的字段而非报错 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 追加到现有图层（与upsert互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_select

gdal vector select{% if of %} -f {{ of | quote }}{% endif %}{% if layer %} -l {{ layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if exclude %} --exclude{% endif %}{% if ignore_missing_fields %} --ignore-missing-fields{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }} {{ fields | quote }}

REM Done
