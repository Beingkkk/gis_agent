{# @id gdal_vector_index #}
{# @name 矢量数据索引创建 #}
{# @description 使用 gdal vector index 为多个矢量数据集创建矢量索引（tileindex），用于 MapServer 等地图服务 #}
{# @keyword vector index #}
{# @keyword tileindex #}
{# @keyword vector tile #}
{# @keyword index creation #}
{# @keyword spatial index #}
{# @keyword mapserver #}
{# @keyword 矢量索引 #}
{# @concept "gdal vector index 用于创建矢量 tileindex（瓦片索引），将多个矢量文件的路径和范围多边形聚合为一个矢量数据集，便于 MapServer 等地图服务进行批量矢量数据管理" #}
{# @concept "tileindex 输出数据集会包含一个存储文件路径的位置字段，以及一个表示每个矢量文件覆盖范围的矩形几何图形" #}
{# @note 位置字段默认存储相对路径，可使用 --absolute-path 改为绝对路径 #}
{# @note 在管道模式下无法使用 --quiet 静默模式 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在，或指定的文件不是有效的矢量数据格式 #}
{# @common_error "Dataset not acknowledged as a vector tileindex" — 更新模式下指定的输出数据集不是有效的 tileindex 格式 #}
{# @param input file_path required 输入矢量文件路径（支持多个输入） #}
{# @param output file_path required 输出矢量索引文件路径 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML,SQLite #}
{# @param overwrite boolean optional 覆盖已存在的输出文件（与 update 互斥） #}
{# @param update boolean optional 以更新模式打开已存在的数据源（与 overwrite 互斥） #}
{# @param append boolean optional 追加到已存在的图层 #}
{# @param nln string optional 输出图层名称 #}
{# @param recursive boolean optional 递归处理输入目录中的所有矢量文件 #}
{# @param filename_filter string optional 文件名过滤模式（支持 * 和 ? 通配符） #}
{# @param location_name string optional 存储路径的字段名称（默认：location） #}
{# @param absolute_path boolean optional 存储绝对路径而非相对路径 #}
{# @param output_crs crs optional 输出坐标参考系统定义（如 EPSG:4326） #}
{# @param dataset_name_only boolean optional 只写入数据集名称，不附加图层索引后缀 #}
{# @param accept_different_crs boolean optional 接受不同坐标系统的图层 #}
{# @param accept_different_schemas boolean optional 接受不同属性结构的图层 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_index

gdal vector index{% if of %} --format {{ of | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if append %} --append{% endif %}{% if nln %} --output-layer {{ nln | quote }}{% endif %}{% if recursive %} --recursive{% endif %}{% if filename_filter %} --filename-filter {{ filename_filter | quote }}{% endif %}{% if location_name %} --location-name {{ location_name | quote }}{% endif %}{% if absolute_path %} --absolute-path{% endif %}{% if output_crs %} --output-crs {{ output_crs | quote }}{% endif %}{% if dataset_name_only %} --dataset-name-only{% endif %}{% if accept_different_crs %} --accept-different-crs{% endif %}{% if accept_different_schemas %} --accept-different-schemas{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
