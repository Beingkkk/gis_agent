{# @id gdal_vector_segmentize #}
{# @name 矢量线段加密 #}
{# @description 通过插入中间点使线段或多边形边界不超过指定最大长度，适用于平滑曲线和加密高精度边界 #}
{# @keyword segmentize #}
{# @keyword 加密 #}
{# @keyword 线段 #}
{# @keyword 多边形 #}
{# @keyword vertices #}
{# @keyword 插点 #}
{# @keyword 平滑曲线 #}
{# @concept "segmentize 操作通过 OGRGeometry::segmentize() 方法实现，在曲线的长线段上等间距插入中间点" #}
{# @concept "该工具仅处理 LineString 和 Polygon 类型的几何要素，Point 等其他类型会被忽略" #}
{# @note max_length 必须为正数，值越小加密点越多，输出文件越大 #}
{# @note 与 gdal vector pipeline 配合使用时可实现自动化批量处理 #}
{# @common_error "Failure during geometry segmentize" — 可能是 max_length 参数值无效（为负数或零），或输入几何对象类型不支持segmentize操作 #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或格式不支持，请检查文件是否损坏或格式是否正确 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param max_length float required 线段最大长度，超过此长度的线段将被加密添加中间点 #}
{# @param of format optional 输出格式名称 default=GeoJSON options=GeoJSON,ESRI Shapefile,GPKG,KML,MapInfo File #}
{# @param layer string optional 指定输入图层名称，默认为所有图层 #}
{# @param overwrite boolean optional 是否覆盖已存在的输出文件 #}
{# @param update boolean optional 是否以更新模式打开已存在的输出文件 #}
{# @param append boolean optional 是否追加到现有图层而非创建新图层 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_segmentize

gdal vector segmentize{% if of %} -f {{ of | quote }}{% endif %}{% if layer %} -l {{ layer | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if append %} --append{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }} {{ max_length | quote }}

REM Done
