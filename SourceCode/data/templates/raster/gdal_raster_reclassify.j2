{# @id gdal_raster_reclassify #}
{# @name 栅格重分类 #}
{# @description 使用映射规则将栅格像素值重新分类，支持区间范围和条件映射 #}
{# @keyword tiff #}
{# @keyword gtiff #}
{# @keyword geotiff #}
{# @keyword raster #}
{# @keyword 重分类 #}
{# @keyword 像素值 #}
{# @keyword 值映射 #}
{# @keyword 区间 #}
{# @keyword reclassify #}
{# @concept "栅格重分类是一种常用的栅格分析操作，通过定义输入值到输出值的映射关系，将原始像素值转换为新的分类值" #}
{# @concept "映射规则支持多种区间表示法：[a,b] 表示闭区间（包含 a 和 b），[a,b) 表示左闭右开区间，(a,b] 表示左开右闭区间" #}
{# @note 映射文件中可使用 DEFAULT 关键字指定未匹配像素的处理方式，如 DEFAULT=NO_DATA、DEFAULT=PASS_THROUGH 或 DEFAULT=50 #}
{# @note 支持写入 VRT 格式，但生成的 VRT 文件需要 GDAL 3.11 以上版本才能打开 #}
{# @seealso gdal_raster_info #}
{# @seealso gdal_raster_calc #}
{# @common_error "Unmapped value found at line" — 输入栅格中存在映射规则未覆盖的像素值，可使用 DEFAULT 关键字处理 #}
{# @common_error "Invalid mapping format" — 映射字符串格式错误，应使用正确的区间表示法（如 [a,b]=value）或单值映射（value=value） #}
{# @param input file_path required 输入栅格文件路径 #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param mapping string required 重分类映射规则，格式为 "[a,b]=value; [c,d)=value; default=value"，或使用 @filename 指定映射文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,COG,NetCDF,GPKG,VRT #}
{# @param datatype enum optional 输出数据类型 options=UInt8,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,Float16,Float32,Float64 #}
{# @param creation_option text optional 输出格式的创建选项（KEY=VALUE 形式） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 追加为已有数据集的子数据集（与 --overwrite 互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_reclassify

gdal raster reclassify{% if of %} -f {{ of | quote }}{% endif %}{% if mapping %} -m {{ mapping | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if datatype %} --ot {{ datatype }}{% endif %}{% if creation_option %} --co {{ creation_option | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
