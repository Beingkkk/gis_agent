{# @id gdal_dataset_identify #}
{# @name 识别栅格数据集驱动 #}
{# @description 识别栅格数据文件所对应的 GDAL 驱动格式 #}
{# @concept "gdal dataset identify 是 gdal dataset 子命令之一，用于检测数据文件使用的 GDAL 驱动类型" #}
{# @concept "该命令支持多种栅格格式的自动识别，包括 GeoTIFF、HDF、NetCDF、JPEG 等常见格式" #}
{# @note 返回结果包含驱动名称、长名称、可能的文件扩展名等元信息 #}
{# @note 可配合 --config 排除不需要检测的驱动以提高识别准确度 #}
{# @seealso gdal_dataset_copy #}
{# @seealso gdal_dataset_delete #}
{# @common_error "Unable to identify dataset" — 指定的文件无法被 GDAL 任何驱动识别，可能是损坏的文件或不支持的格式 #}
{# @common_error "Driver name not found" — 系统中没有安装对应格式的 GDAL 驱动 #}
{# @param input file_path required 要识别的栅格数据集文件路径 #}
{# @param config string optional 配置选项，格式为 KEY=VALUE（如 GDAL_SKIP 可指定忽略的驱动） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_dataset_identify

gdal dataset identify {{ input | safe_path | quote }}{% if config %} --config {{ config | quote }}{% endif %}

REM Done
