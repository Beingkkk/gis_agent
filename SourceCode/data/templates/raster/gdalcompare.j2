{# @id gdalcompare #}
{# @name 栅格数据比较 #}
{# @description 比较两个GDAL支持的栅格数据集，报告像素和元数据的差异 #}
{# @keyword tiff #}
{# @keyword raster #}
{# @keyword compare #}
{# @keyword diff #}
{# @keyword pixel #}
{# @keyword metadata #}
{# @keyword 比较 #}
{# @keyword 差异 #}
{# @concept "gdalcompare 是 GDAL 的栅格数据比较工具，逐像素比较两个栅格文件，并检查元数据、坐标系、仿射变换等信息的差异" #}
{# @concept "工具会返回差异计数作为退出值。如果只关心GDAL可见数据是否相同，差异计数为1（二进制差异）通常可接受" #}
{# @note gdalcompare 是 Python 脚本工具，需要 GDAL Python 绑定支持 #}
{# @note 默认进行字节级二进制比较，如需只比较GDAL可读数据，可用 -skip_binary 跳过 #}
{# @seealso gdal_translate #}
{# @seealso gdalinfo #}
{# @common_error "Unable to open" — 指定的栅格文件路径不存在或文件格式GDAL不支持 #}
{# @common_error "Different dimensions" — 两个栅格文件的尺寸（行、列、波段数）不一致，无法进行逐像素比较 #}
{# @param golden_file file_path required 作为基准的正确栅格文件路径（golden file） #}
{# @param new_file file_path required 与基准文件进行比较的新栅格文件路径 #}
{# @param skip_binary boolean optional 跳过逐字节的二进制内容比较 #}
{# @param skip_overviews boolean optional 跳过金字塔（概览）层级的比较 #}
{# @param skip_geolocation boolean optional 跳过GEOLOCATION元数据域的比较 #}
{# @param skip_geotransform boolean optional 跳过仿射变换矩阵的比较 #}
{# @param skip_metadata boolean optional 跳过元数据的比较 #}
{# @param skip_rpc boolean optional 跳过有理多项式系数（RPC）元数据域的比较 #}
{# @param skip_srs boolean optional 跳过空间参考系统的比较 #}
{# @param sds boolean optional 比较所有子数据集，否则忽略子数据集 #}
{# @param dumpdiffs boolean optional 输出像素差异内容到当前目录的TIFF文件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdalcompare

gdalcompare{% if skip_binary %} -skip_binary{% endif %}{% if skip_overviews %} -skip_overviews{% endif %}{% if skip_geolocation %} -skip_geolocation{% endif %}{% if skip_geotransform %} -skip_geotransform{% endif %}{% if skip_metadata %} -skip_metadata{% endif %}{% if skip_rpc %} -skip_rpc{% endif %}{% if skip_srs %} -skip_srs{% endif %}{% if sds %} -sds{% endif %}{% if dumpdiffs %} -dumpdiffs{% endif %} {{ golden_file | safe_path | quote }} {{ new_file | safe_path | quote }}

REM Done
