{# @id gdal_read #}
{# @name 栅格数据读取 #}
{# @description 在 GDAL 管道中读取单个栅格数据集，支持多种格式和打开选项 #}
{# @keyword gdal #}
{# @keyword raster #}
{# @keyword read #}
{# @keyword pipeline #}
{# @keyword 输入 #}
{# @keyword 读取 #}
{# @keyword tif #}
{# @keyword geotiff #}
{# @concept "gdal read 是 GDAL 管道处理的第一步操作，用于读取栅格数据供后续处理使用" #}
{# @concept "支持多种栅格格式的读取，包括 GeoTIFF、HFA、JPEG2000 等常见格式" #}
{# @note read 操作必须在管道中作为第一个步骤使用，用于加载数据源 #}
{# @note 可通过 open-option 指定格式特定的读取参数 #}
{# @common_error "Unable to open" — 输入文件不存在、路径错误或 GDAL 不支持该格式 #}
{# @common_error "Open options are not supported" — 指定的打开选项不被当前栅格格式支持 #}
{# @param input file_path required 输入栅格文件路径 #}
{# @param if format optional 输入栅格格式名称（如 GTiff、JP2OpenJPEG、HFA 等） options=GTiff,HFA,JPEG2000,ENVI,EHdr,GPKG,COG #}
{# @param oo text optional GDAL 打开选项，格式为 KEY=VALUE（如 COMPRESSION=LZW） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_read

read{% if if %} --input-format {{ if | quote }}{% endif %}{% if oo %} --open-option {{ oo | quote }}{% endif %} --input {{ input | safe_path | quote }}

REM Done
