{# @id gdal_raster_create #}
{# @name 创建栅格数据集 #}
{# @description 使用 gdal raster create 命令创建新的空栅格文件，支持设置尺寸、波段数、数据类型、坐标系统和元数据 #}
{# @keyword raster #}
{# @keyword geotiff #}
{# @keyword tif #}
{# @keyword gdal #}
{# @keyword 创建 #}
{# @keyword 新建 #}
{# @keyword 空栅格 #}
{# @keyword grid #}
{# @concept "gdal raster create 用于初始化新的栅格文件，可以通过 --like 参数基于模板栅格复制属性（尺寸、波段、投影等），也可以直接使用 --size 等参数指定" #}
{# @concept "创建时可通过 --crs、--bbox、--nodata 等参数设置空间参考和像素填充方式，支持数十种栅格格式输出" #}
{# @note 必须通过 --like 或 --size 指定输出尺寸，否则无法创建文件 #}
{# @note 对于 GeoTIFF 输出，设置 SPARSE_OK=YES 创建选项可以创建最小大小的稀疏文件，节省磁盘空间 #}
{# @common_error "Size and template not specified" — 既未指定 --size 也未指定 --like 参数，导致无法确定输出尺寸 #}
{# @common_error "Cannot open datasource" — 输出路径无效、磁盘空间不足或指定的格式不受支持 #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param of format optional 输出栅格格式名称 default=GTiff options=GTiff,GPKG,HFA,ENVI,netCDF,PNG,JPEG #}
{# @param like file_path optional 模板栅格数据集路径，复制其尺寸、波段、投影等属性 #}
{# @param size string optional 输出尺寸，格式为 width,height（如 512,512） #}
{# @param band_count integer optional 输出波段数量 default=1 #}
{# @param ot enum optional 输出数据类型 default=Byte options=Byte,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,CInt16,CInt32,Float16,Float32,Float64,CFloat32,CFloat64 #}
{# @param nodata string optional nodata 值（支持 nan、inf、-inf 或具体数值） #}
{# @param crs crs optional 坐标参考系统定义（如 EPSG:4326） #}
{# @param bbox string optional 空间边界范围，格式为 xmin,ymin,xmax,ymax #}
{# @param overwrite boolean optional 覆盖已存在的输出文件（与 --append 互斥） #}
{# @param append boolean optional 作为子数据集追加到现有输出（与 --overwrite 互斥） #}
{# @param co text optional GDAL 创建选项，格式为 KEY=VALUE（如 COMPRESS=LZW） #}
{# @param metadata string optional 元数据项，格式为 KEY=VALUE #}
{# @param burn string optional 初始化波段的值（可重复指定多个波段） #}
{# @param copy_metadata boolean optional 从模板数据集复制元数据（需配合 --like 使用） #}
{# @param copy_overviews boolean optional 从模板数据集复制概览结构（需配合 --like 使用） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_create

gdal raster create{% if of %} -f {{ of | quote }}{% endif %}{% if like %} --like {{ like | safe_path | quote }}{% endif %}{% if size %} --size {{ size | quote }}{% endif %}{% if band_count %} --band-count {{ band_count | quote }}{% endif %}{% if ot %} -ot {{ ot | quote }}{% endif %}{% if nodata %} --nodata {{ nodata | quote }}{% endif %}{% if crs %} --crs {{ crs | quote }}{% endif %}{% if bbox %} --bbox {{ bbox | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if metadata %} --metadata {{ metadata | quote }}{% endif %}{% if burn %} --burn {{ burn | quote }}{% endif %}{% if copy_metadata %} --copy-metadata{% endif %}{% if copy_overviews %} --copy-overviews{% endif %} {{ output | safe_path | quote }}

REM Done
