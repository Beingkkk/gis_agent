{# @id gdalattachpct #}
{# @name 附加颜色表到栅格 #}
{# @description 将调色板文件或颜色表附加到栅格数据生成RGB输出文件 #}
{# @keyword palette #}
{# @keyword color table #}
{# @keyword rgb #}
{# @keyword 调色板 #}
{# @keyword 颜色表 #}
{# @keyword 栅格 #}
{# @keyword cat #}
{# @keyword gdal_translate #}
{# @concept "gdalattachpct 用于将调色板/颜色表应用到栅格数据，将带颜色表的单波段数据转换为RGB多波段输出" #}
{# @concept "颜色表文件可以是已包含调色板的栅格文件，或txt/qml/qlr格式的专用颜色配置文件" #}
{# @note 该工具为Python脚本，需要GDAL Python绑定支持才能运行 #}
{# @note 输出格式默认根据目标文件扩展名自动判断 #}
{# @seealso gdal_translate #}
{# @seealso rgb2pct #}
{# @common_error "Unable to open" — 指定的颜色表文件或源文件路径不存在，或格式不受GDAL支持 #}
{# @common_error "not recognized as a supported file format" — 颜色表文件格式不正确，应为栅格调色板或txt/qml/qlr颜色配置文件 #}
{# @param palette_file file_path required 包含颜色表的文件路径，支持带调色板的栅格或颜色配置文件（txt/qml/qlr） #}
{# @param source_file file_path required 输入栅格文件路径（将应用颜色表） #}
{# @param dest_file file_path required 输出RGB文件路径 #}
{# @param of format optional 输出格式名称 options=GTiff,PNG,JPEG,GIF,HFA,ENVI #}

@echo off
REM Generated by GIS Agent
REM Template: gdalattachpct

gdalattachpct{% if of %} -of {{ of | quote }}{% endif %} {{ palette_file | safe_path | quote }} {{ source_file | safe_path | quote }} {{ dest_file | safe_path | quote }}

REM Done
