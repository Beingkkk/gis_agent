{# @id gdal_raster_overview #}
{# @name 栅格金字塔管理 #}
{# @description 管理栅格数据中的概视图层，支持添加、删除和刷新操作 #}
{# @concept "概视图（Overviews）也称金字塔，是栅格数据的多分辨率缩略图，用于加速大图像的显示和浏览" #}
{# @concept "gdal raster overview 子命令分别用于创建概视图层、删除已有概视图、以及强制重新计算过时的概视图" #}
{# @note 添加概视图时默认会使用合适的重采样算法，可通过配置文件调整行为 #}
{# @note 并非所有栅格格式都支持概视图操作，需注意输出格式的兼容性 #}
{# @seealso gdalinfo_dataset_info #}
{# @common_error "not recognized as a raster dataset" — 指定的文件不是有效的栅格数据格式，或文件路径不存在 #}
{# @common_error "Overview does not exist" — 尝试删除或刷新的概视图编号不存在 #}
{# @param input file_path required 输入栅格文件路径或数据源 #}
{# @param subcommand string required 概视图子命令：add（添加）、delete（删除）、refresh（刷新）概视图 #}
{# @param config string optional 配置选项，格式为 KEY=VALUE（如 USE_RDD=YES） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_overview

gdal raster overview {{ subcommand }}{% if config %} --config {{ config | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
