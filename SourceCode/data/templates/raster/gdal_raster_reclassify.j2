{# @id gdal_raster_reclassify #}
{# @name 栅格重分类 #}
{# @description 根据指定的映射规则对栅格数据中的像素值进行重新分类，支持区间范围和单值映射 #}
{# @keyword reclassify #}
{# @keyword 重分类 #}
{# @keyword remap #}
{# @keyword raster #}
{# @keyword 栅格 #}
{# @keyword 像素值 #}
{# @keyword 分类 #}
{# @keyword 值域映射 #}
{# @concept "gdal raster reclassify 支持四种区间表示法：[a,b] 闭区间、[a,b) 左闭右开、(a,b] 左开右闭、(a,b) 开区间，可精确控制值域边界" #}
{# @concept "使用 DEFAULT 关键字可以处理未被其他规则覆盖的像素值，如 DEFAULT=PASS_THROUGH 表示保持原值、DEFAULT=NO_DATA 表示转为无数据值" #}
{# @note 映射文件中 # 开头的行为注释，不会被解析，可用于标注规则用途 #}
{# @note 输入栅格中若有未被映射规则覆盖的像素值会导致报错，除非配置了 DEFAULT 处理方式 #}
{# @seealso gdal_raster_calc #}
{# @common_error "Error: values not covered by any mapping rule" — 输入栅格中存在未在映射规则中定义的像素值，可通过添加 DEFAULT 规则解决 #}
{# @common_error "Invalid mapping syntax" — 映射规则语法错误，如区间符号不匹配、缺少等号等，应检查 mapping 参数格式 #}
{# @param input file_path required 输入栅格文件路径 #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param mapping text required 重分类映射规则，支持单值映射（如 0=10）和区间映射（如 [2,4]=20），也可用 @filename 指定外部映射文件 #}
{# @param of string optional 输出栅格格式（如 GTiff） #}
{# @param ot enum optional 输出数据类型 options=UInt8,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,Float16,Float32,Float64 #}
{# @param co string optional 创建选项（KEY=VALUE），可重复使用以设置多个选项 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出文件 #}
{# @param append boolean optional 追加为现有数据集的子数据集（与 overwrite 互斥） #}
{# @param input_format string optional 输入栅格格式 #}
{# @param open_option string optional 打开选项（KEY=VALUE） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_reclassify

gdal raster reclassify{% if of %} --format {{ of | quote }}{% endif %}{% if ot %} --ot {{ ot | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if input_format %} --input-format {{ input_format | quote }}{% endif %}{% if open_option %} --open-option {{ open_option | quote }}{% endif %} --mapping {{ mapping | quote }} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
