{# @id gdal_zarr_add_georef_convention #}
{# @name ZARR地理参考约定添加 #}
{# @description 为现有的ZARR多维栅格数据集添加地理参考约定，支持GDAL和spatial_proj两种约定 #}
{# @keyword zarr #}
{# @keyword georeference #}
{# @keyword convention #}
{# @keyword multidimensional #}
{# @keyword raster #}
{# @keyword spatial_proj #}
{# @keyword 元数据 #}
{# @concept "ZARR是一种用于存储科学数据的开放规范，支持存储大型多维栅格数据" #}
{# @concept "地理参考约定用于在ZARR数据集中记录空间坐标系统、投影信息等地理元数据" #}
{# @note 此命令是添加而非替换约定，不会覆盖已存在的地理参考信息 #}
{# @note spatial_proj约定支持更丰富的空间投影元数据，推荐用于需要精确空间坐标的应用 #}
{# @common_error "Unable to open dataset" — 输入的ZARR数据集路径不存在或文件格式损坏 #}
{# @common_error "Invalid convention name" — 指定的convention参数值不在支持列表中，应为GDAL或spatial_proj #}
{# @param input file_path required 输入的多维栅格ZARR数据集路径 #}
{# @param convention enum required 地理参考约定类型 options=GDAL,spatial_proj #}
{# @param json_usage boolean optional 以JSON格式输出命令使用说明 #}
{# @param config text optional GDAL配置选项，格式为 KEY=VALUE，可重复使用 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_zarr_add_georef_convention

gdal driver zarr add-georeferencing-convention{% if json_usage %} --json-usage{% endif %}{% if config %} --config {{ config | quote }}{% endif %} {{ input | safe_path | quote }} {{ convention | quote }}

REM Done
