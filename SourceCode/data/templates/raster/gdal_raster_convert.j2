{# @id gdal_raster_convert #}
{# @name 栅格格式转换 #}
{# @description 使用 gdal raster convert 将栅格数据在不同格式之间转换，支持创建选项和覆盖模式 #}
{# @keyword tiff #}
{# @keyword geotiff #}
{# @keyword png #}
{# @keyword jpeg #}
{# @keyword bil #}
{# @keyword asc #}
{# @keyword hdf #}
{# @keyword netcdf #}
{# @keyword 格式转换 #}
{# @keyword 栅格转换 #}
{# @concept "gdal raster convert 是 GDAL 新一代 Python 命令，用于栅格格式转换，支持几十种栅格格式如 GeoTIFF、JPEG、PNG、HDF、NetCDF 等" #}
{# @concept "可通过 --creation-option 指定压缩算法、块大小等高级创建参数，控制输出文件的质量和大小" #}
{# @note 输出格式通常从文件扩展名自动判断，也可显式用 --format 指定 #}
{# @note --overwrite 和 --append 是互斥的，不能同时使用 #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在、文件损坏或指定的格式不受支持 #}
{# @common_error "File exists" — 输出文件已存在且未指定 --overwrite 或 --append 模式 #}
{# @param input file_path required 输入栅格数据集路径 #}
{# @param output file_path required 输出栅格数据集路径 #}
{# @param of format optional 输出格式名称 options=GTiff,JPEG,PNG,HFA,ENVI,EHdr,GPKG,MBTiles,COG #}
{# @param co text optional 创建选项，格式为 KEY=VALUE（如 COMPRESS=LZW, TILED=YES） #}
{# @param input_format format optional 输入格式名称（通常自动识别） options=GTiff,JPEG,PNG,HFA,ENVI,EHdr,GPKG,HDF5,NetCDF #}
{# @param open_option text optional 打开选项，格式为 KEY=VALUE #}
{# @param overwrite boolean optional 是否覆盖已存在的输出文件（与 --append 互斥） #}
{# @param append boolean optional 作为子数据集追加到现有输出文件（与 --overwrite 互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_convert

gdal raster convert{% if of %} --format {{ of | quote }}{% endif %}{% if co %} --creation-option {{ co | quote }}{% endif %}{% if input_format %} --input-format {{ input_format | quote }}{% endif %}{% if open_option %} --open-option {{ open_option | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
