{# @id gdal_raster_colormap #}
{# @name 栅格色彩映射 #}
{# @description 使用色彩映射表将单波段栅格数据转换为 RGB 或 RGBA 多波段输出，常用于从 DEM 生成分层设色地图 #}
{# @keyword colormap #}
{# @keyword color-map #}
{# @keyword dem #}
{# @keyword 分层设色 #}
{# @keyword rgb #}
{# @keyword rgba #}
{# @keyword 色彩表 #}
{# @keyword 高程渲染 #}
{# @concept "色彩映射（Color Map）是将栅格像元值映射为颜色的查找表，可以来源于栅格内置色彩表或外部文件" #}
{# @concept "该工具常用于将 DEM 等单波段高程数据转换为分级设色地图（晕渲图），便于可视化展示" #}
{# @note 输出格式默认根据文件扩展名自动判断，也可通过 -f 显式指定 #}
{# @note 如果输入栅格已包含内置色彩表，则无需额外指定 --color-map 参数 #}
{# @common_error "Input band index out of range" — 指定的波段编号超过了输入栅格的实际波段数量 #}
{# @common_error "Cannot open color map file" — 外部色彩映射表文件路径不存在或格式错误 #}
{# @param input file_path required 输入栅格文件路径（单波段数据，如 DEM） #}
{# @param output file_path required 输出 RGB/RGBA 栅格文件路径 #}
{# @param of format optional 输出格式名称 options=GTiff,PNG,JPEG,MEM,COG #}
{# @param co text optional 输出格式的创建选项（KEY=VALUE 格式，多个选项可重复使用） #}
{# @param overwrite boolean optional 是否允许覆盖已存在的输出文件 #}
{# @param append boolean optional 是否追加为现有数据集的子数据集 #}
{# @param band integer optional 输入波段编号（从1开始，默认为1） #}
{# @param color_map file_path optional 外部色彩映射表文件路径（文本格式） #}
{# @param add_alpha boolean optional 是否添加透明通道（Alpha 掩码） #}
{# @param color_selection enum optional 颜色插值方式 options=interpolate,exact,nearest #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_colormap

gdal raster color-map{% if of %} -f {{ of | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if band %} -b {{ band }}{% endif %}{% if color_map %} --color-map {{ color_map | safe_path | quote }}{% endif %}{% if add_alpha %} --add-alpha{% endif %}{% if color_selection %} --color-selection {{ color_selection | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
