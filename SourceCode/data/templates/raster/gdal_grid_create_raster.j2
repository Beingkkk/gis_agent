{# @id gdal_grid_create_raster #}
{# @name 散点栅格插值生成 #}
{# @description 从散点矢量数据使用各种插值算法生成规则栅格网格 #}
{# @keyword gdal_grid #}
{# @keyword interpolation #}
{# @keyword raster #}
{# @keyword grid #}
{# @keyword DEM #}
{# @keyword 点数据 #}
{# @keyword 散点 #}
{# @keyword 插值 #}
{# @keyword voronoi #}
{# @keyword invdist #}
{# @concept "gdal_grid 将散点矢量数据插值生成规则栅格网格，支持多种插值算法：距离反比加权法(invdist)、线性插值(linear)、最近邻(nearest)、样条(spline)等" #}
{# @concept "常用算法格式如 invdist:power=2:smoothing=1 指定距离指数和光滑参数；linear 算法无需额外参数" #}
{# @note 输出范围可通过 -txe/-tye 或 -spat 指定，分辨率可通过 -tr 或 -outsize 指定，两者不能同时使用 #}
{# @note zfield 参数用于指定包含高程值的属性字段，否则使用几何体中的 Z 坐标 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入数据源路径不存在或格式不受支持 #}
{# @common_error "-outsize cannot be used with -tr" — -outsize 和 -tr 不能同时使用，请只选择其中一种方式定义输出分辨率 #}
{# @param input file_path required 输入矢量数据源，包含散点数据（支持 Shapefile、GeoJSON 等 OGR 格式） #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param algorithm string optional 插值算法名称及参数，格式为 算法:参数1=值1:参数2=值2，如 invdist:power=2:smoothing=1 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,PNG,JPEG,HFA,AAIGrid,NetCDF #}
{# @param ot enum optional 输出波段数据类型 options=Byte,Int16,UInt16,Int32,UInt32,Float32,Float64 #}
{# @param zfield string optional 用于提取 Z 值的属性字段名称（覆盖几何体中的 Z 值） #}
{# @param z_increase float optional Z 值的加法增量，用于单位转换 #}
{# @param z_multiply float optional Z 值的乘法系数，如从英尺转换为米 #}
{# @param a_srs crs optional 覆盖输出文件的投影定义（WKT/PROJ.4/EPSG:n） #}
{# @param txe string optional 输出文件地理参考 X 范围，格式为 "xmin xmax" #}
{# @param tye string optional 输出文件地理参考 Y 范围，格式为 "ymin ymax" #}
{# @param tr string optional 输出文件分辨率（目标地理参考单位），格式为 "xres yres" #}
{# @param outsize string optional 输出文件像素大小，格式为 "xsize ysize" #}
{# @param spat string optional 空间过滤边界框，格式为 "xmin ymin xmax ymax" #}
{# @param layer string optional 输入数据源中要使用的图层名称 #}
{# @param where string optional 属性过滤表达式 #}
{# @param sql string optional SQL 查询语句从输入数据源选择要素 #}
{# @param lco string optional 输出格式的创建选项（如压缩方式） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_grid_create_raster

gdal_grid{% if of %} -of {{ of | quote }}{% endif %}{% if ot %} -ot {{ ot | quote }}{% endif %}{% if zfield %} -zfield {{ zfield | quote }}{% endif %}{% if z_increase %} -z_increase {{ z_increase | quote }}{% endif %}{% if z_multiply %} -z_multiply {{ z_multiply | quote }}{% endif %}{% if a_srs %} -a_srs {{ a_srs | quote }}{% endif %}{% if algorithm %} -a {{ algorithm | quote }}{% endif %}{% if txe %} -txe {{ txe | quote }}{% endif %}{% if tye %} -tye {{ tye | quote }}{% endif %}{% if tr %} -tr {{ tr | quote }}{% endif %}{% if outsize %} -outsize {{ outsize | quote }}{% endif %}{% if spat %} -spat {{ spat | quote }}{% endif %}{% if layer %} -l {{ layer | quote }}{% endif %}{% if where %} -where {{ where | quote }}{% endif %}{% if sql %} -sql {{ sql | quote }}{% endif %}{% if lco %} -co {{ lco | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
