{# @id gdalmdiminfo #}
{# @name 多维栅格信息查询 #}
{# @description 以 JSON 格式输出 GDAL 多维栅格数据集的数组结构、属性信息和统计摘要 #}
{# @concept "gdalmdiminfo 用于探查多维栅格数据（如 Climate and Forecast (CF) NetCDF、GRIB、ZARR、GeoTIFF 等）的层级结构和变量数组信息" #}
{# @concept "多维数据模型允许存储超出传统 2D 影像的更高维度数据（如时间序列、温度场深度等多维数组）" #}
{# @note 输出格式遵循 JSON Schema，可直接被其他程序解析处理 #}
{# @note 该工具相当于新命令行接口中的 gdal mdim info 命令 #}
{# @seealso gdalinfo_raster_info #}
{# @common_error "It is not a multidimensional GDAL dataset" — 指定的文件不是 GDAL 支持的多维栅格格式，或文件损坏无法识别 #}
{# @common_error "I/O error: No such file or directory" — 输入的数据集文件路径不存在或无法访问 #}
{# @param dataset_name file_path required 要查询的多维栅格数据集文件路径（如 NetCDF、ZARR、GRIB 等格式） #}
{# @param detailed boolean optional 输出详细信息，包括属性数据类型和数组值（与 -summary 互斥） #}
{# @param summary boolean optional 输出摘要信息（默认模式，与 -detailed 互斥） #}
{# @param nopretty boolean optional 单行输出，不添加缩进格式化 #}
{# @param array string optional 限制输出仅显示指定数组名称的信息 #}
{# @param limit integer optional 限制每个维度显示的值数量（仅与 -detailed 配合使用） #}
{# @param stats boolean optional 读取并显示数组统计信息（均值、最大值、最小值等） #}
{# @param if_format string optional 指定尝试打开数据集的驱动格式名称 #}
{# @param arrayoption string optional (auto-completed) #}
{# @param oo string optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdalmdiminfo

gdalmdiminfo{% if detailed %} -detailed{% endif %}{% if summary %} -summary{% endif %}{% if nopretty %} -nopretty{% endif %}{% if array %} -array {{ array | quote }}{% endif %}{% if limit is not none %} -limit {{ limit }}{% endif %}{% if stats %} -stats{% endif %}{% if if_format %} -if {{ if_format | quote }}{% endif %}{% if oo %} -oo {{ oo | quote }}{% endif %}{% if arrayoption %} -arrayoption {{ arrayoption | quote }}{% endif %} {{ dataset_name | safe_path | quote }}

REM Done
