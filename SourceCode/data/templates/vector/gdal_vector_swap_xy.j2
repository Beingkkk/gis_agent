{# @id gdal_vector_swap_xy #}
{# @name 交换坐标轴 #}
{# @description 交换矢量数据几何图形的X和Y坐标值，用于处理坐标系轴序问题 #}
{# @keyword swap #}
{# @keyword xy #}
{# @keyword 坐标轴 #}
{# @keyword 经纬度 #}
{# @keyword axis order #}
{# @keyword lon #}
{# @keyword lat #}
{# @keyword 互换 #}
{# @concept "gdal vector swap-xy 用于处理坐标系轴序问题，某些坐标系（如 EPSG:4326 的 WKT 表示）可能以 (lat, lon) 顺序存储坐标，而非标准的 (lon, lat) 顺序" #}
{# @concept "该工具仅交换坐标数值，不改变坐标系定义本身" #}
{# @note 该工具是 gdal vector pipeline 的一部分，可嵌入多步骤处理流程中 #}
{# @note 交换坐标后，数据的空间参考系统标识不变，但几何坐标值已被调换 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在，或指定的格式不受支持 #}
{# @common_error "Layer not found" — 指定的输入图层名称不存在或拼写错误 #}
{# @param input file_path required 输入矢量文件路径或数据源 #}
{# @param output file_path required 输出矢量文件路径或数据源 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GeoPackage #}
{# @param layer string optional 输入图层名称 #}
{# @param append boolean optional 追加到现有图层 #}
{# @param overwrite boolean optional 覆盖已存在的输出数据集 #}
{# @param upsert boolean optional 插入或更新特征（隐含追加模式），与 --append 互斥 #}
{# @param output_layer string optional 输出图层名称 #}
{# @param active_layer string optional 设置活动图层（未指定则处理所有图层） #}
{# @param active_geometry string optional 限制处理的几何字段名称（未指定则处理所有几何字段） #}
{# @param quiet boolean optional 静默模式，不显示进度条和警告信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_swap_xy

gdal vector swap-xy{% if layer %} -l {{ layer | quote }}{% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if upsert %} --upsert{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if active_layer %} --active-layer {{ active_layer | quote }}{% endif %}{% if active_geometry %} --active-geometry {{ active_geometry | quote }}{% endif %}{% if quiet %} -q{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
