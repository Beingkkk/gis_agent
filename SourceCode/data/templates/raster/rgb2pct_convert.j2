{# @id rgb2pct_convert #}
{# @name RGB转调色板图像 #}
{# @description 使用中位切割算法将RGB图像转换为伪彩色图像，支持调色板提取和Floyd-Steinberg抖动 #}
{# @keyword rgb #}
{# @keyword 调色板 #}
{# @keyword pct #}
{# @keyword 伪彩色 #}
{# @keyword tiff #}
{# @keyword 颜色转换 #}
{# @keyword median cut #}
{# @keyword dithering #}
{# @keyword 量化 #}
{# @concept "rgb2pct 将RGB多通道图像转换为单通道调色板图像，通过中位切割算法计算最优调色板，再使用Floyd-Steinberg抖动技术最大化输出图像视觉质量" #}
{# @concept "转换后图像从多通道RGB变为带调色板的单通道图像，大幅减少文件体积" #}
{# @note rgb2pct 是Python工具，需要GDAL Python绑定支持 #}
{# @note 输出格式必须支持调色板颜色表，GTiff是最常用的选择 #}
{# @common_error "Unable to convert source image to palette" — 输出格式不支持调色板颜色表，请使用GTiff、PNG等支持的格式 #}
{# @common_error "Source image has no RGB data" — 源图像不是RGB格式，或已经是调色板图像 #}
{# @param source_file file_path required 输入RGB图像文件路径 #}
{# @param dest_file file_path required 输出伪彩色图像文件路径 #}
{# @param of format optional 输出格式名称 default=GTiff options=GTiff,PNG,JPEG #}
{# @param n integer optional 生成调色板的颜色数量，默认为256，必须在2到256之间 default=256 #}
{# @param pct file_path optional 从现有调色板文件提取颜色表，用于保持多文件颜色一致性 #}
{# @param creation_option string optional GeoTIFF驱动选项，如压缩方式 COMPRESS=LZW #}

@echo off
REM Generated by GIS Agent
REM Template: rgb2pct_convert

rgb2pct{% if of %} -of {{ of | quote }}{% endif %}{% if n %} -n {{ n | quote }}{% endif %}{% if pct %} -pct {{ pct | quote }}{% endif %}{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %} {{ source_file | safe_path | quote }} {{ dest_file | safe_path | quote }}

REM Done
