{# @id gdal_vector_export_schema #}
{# @name 导出矢量数据模式 #}
{# @description 导出矢量数据集的 OGR_SCHEMA 结构描述信息（JSON 格式） #}
{# @keyword schema #}
{# @keyword ogr #}
{# @keyword json #}
{# @keyword 导出 #}
{# @keyword 结构描述 #}
{# @keyword gdal vector #}
{# @concept "OGR_SCHEMA 是 JSON 格式的结构描述文件，记录矢量数据集的字段类型、几何类型、空间参考等元信息" #}
{# @concept "gdal vector export-schema 用于提取和导出矢量数据的数据结构，可作为数据迁移或模式验证的前置步骤" #}
{# @note 当不指定输出文件时，模式信息直接输出到标准输出，可重定向到文件或管道处理 #}
{# @note 输入格式通常自动识别，也可通过 --input-format 显式指定 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或无法读取，检查文件权限 #}
{# @common_error "no layer specified" — 未找到指定名称的图层，确认图层名称与数据源中实际名称一致 #}
{# @param input file_path required 输入矢量数据集路径（可指定多个） #}
{# @param output file_path optional 输出文件路径，若不指定则输出到标准输出 #}
{# @param input_layer string optional 指定输入图层名称（可重复以指定多个图层） #}
{# @param input_format format optional 输入数据格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML,PostgreSQL #}
{# @param overwrite boolean optional 是否允许覆盖已存在的输出文件 #}
{# @param layer string optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_export_schema

gdal vector export-schema{% if input_layer %}{% for layer in input_layer %} --input-layer {{ layer | quote }}{% endfor %}{% endif %}{% if input_format %} --input-format {{ input_format | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if output %} --output {{ output | safe_path | quote }}{% endif %}{{ input | safe_path | quote }}

REM Done
