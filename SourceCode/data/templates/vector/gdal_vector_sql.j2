{# @id gdal_vector_sql #}
{# @name 矢量数据SQL查询 #}
{# @description 使用SQL语句对矢量数据集进行查询、筛选和修改操作 #}
{# @keyword sql #}
{# @keyword query #}
{# @keyword 矢量查询 #}
{# @keyword 属性筛选 #}
{# @keyword geojson #}
{# @keyword gpkg #}
{# @keyword shp #}
{# @keyword ogrosql #}
{# @keyword sqlite #}
{# @concept "gdal vector sql 是 GDAL 3.12+ 引入的新命令，通过 SQL 语句对矢量数据进行查询、转换和修改操作" #}
{# @concept "支持 OGRSQL 和 SQLITE 等多种 SQL 方言，可以直接执行 SELECT、UPDATE、DELETE 等语句" #}
{# @note 配合 --update 参数且不指定输出时，可直接修改输入数据源（如执行 UPDATE、DELETE 操作） #}
{# @note --sql 参数可以重复使用以执行多条 SQL 语句 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unrecognized token" — SQL语句语法错误，可能是使用了不支持的SQL语法或方言选择错误 #}
{# @common_error "no such column" — SQL中引用的字段名称不存在，检查字段名拼写或确认是否需要使用引号 #}
{# @param input file_path required 输入矢量数据集路径（支持文件或数据源） #}
{# @param output file_path optional 输出矢量数据集路径 #}
{# @param sql string required SQL语句或SQL文件路径（以@开头） #}
{# @param dialect string optional SQL方言（如 OGRSQL、SQLITE） #}
{# @param format format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML,PostgreSQL #}
{# @param output_layer string optional 输出图层名称 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param update boolean optional 以更新模式打开现有数据集（配合--sql可执行UPDATE、DELETE等修改操作） #}
{# @param append boolean optional 追加到现有图层 #}
{# @param overwrite_layer boolean optional 允许覆盖现有输出图层 #}
{# @param creation_option string optional (auto-completed) #}
{# @param layer_creation_option string optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_sql

gdal vector sql{% if input %} {{ input | safe_path | quote }}{% endif %}{% if output %} {{ output | safe_path | quote }}{% endif %}{% if sql %} --sql {{ sql | quote }}{% endif %}{% if dialect %} --dialect {{ dialect | quote }}{% endif %}{% if format %} --format {{ format | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if update %} --update{% endif %}{% if append %} --append{% endif %}{% if overwrite_layer %} --overwrite-layer{% endif %}{% if creation_option %} --co {{ creation_option | quote }}{% endif %}{% if layer_creation_option %} --lco {{ layer_creation_option | quote }}{% endif %}

REM Done
