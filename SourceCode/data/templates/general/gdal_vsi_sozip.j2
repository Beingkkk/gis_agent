{# @id gdal_vsi_sozip #}
{# @name SOZip文件处理 #}
{# @description 创建、列表查看、优化和验证SOZip（Seek-Optimized ZIP）压缩文件 #}
{# @keyword sozip #}
{# @keyword zip #}
{# @keyword 压缩 #}
{# @keyword 优化 #}
{# @keyword seek #}
{# @keyword gdal vsi #}
{# @keyword 文件处理 #}
{# @concept "SOZip（Seek-Optimized ZIP）是一种专为地理空间数据设计的ZIP格式优化方案，支持高效的随机访问读取操作" #}
{# @concept "该工具支持四种操作：创建新的SOZip文件、查看ZIP内容列表、将普通ZIP转换为SOZip、以及验证SOZip文件的完整性" #}
{# @note create命令需要指定输入文件和输出SOZip文件路径 #}
{# @note optimize命令可自动将普通ZIP文件转换为支持快速查找的SOZip格式 #}
{# @common_error "Not a valid ZIP file" — 指定的输入文件不是有效的ZIP格式或文件已损坏 #}
{# @common_error "SOZip validation failed" — SOZip文件结构不符合Seek-Optimized规范，可能需要重新创建或优化 #}
{# @param subcommand enum required 子命令类型 options=create,list,optimize,validate #}
{# @param input file_path optional 输入ZIP/SOZip文件路径（用于list/optimize/validate命令） #}
{# @param output file_path optional 输出SOZip文件路径（用于create/optimize命令） #}
{# @param config string optional GDAL配置选项（KEY=VALUE格式） #}
{# @param help boolean optional 显示帮助信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vsi_sozip

gdal vsi sozip {{ subcommand | quote }}{% if input %} {{ input | quote }}{% endif %}{% if output %} {{ output | quote }}{% endif %}{% if config %} --config {{ config | quote }}{% endif %}{% if help %} -h{% endif %}

REM Done
