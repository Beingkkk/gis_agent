{# @id gdal_raster_pixel_info #}
{# @name 栅格像元信息查询 #}
{# @description 查询栅格数据中指定位置（像元坐标或地理坐标）的像素值和相关属性信息 #}
{# @keyword raster #}
{# @keyword pixel #}
{# @keyword band #}
{# @keyword value #}
{# @keyword info #}
{# @keyword 像元 #}
{# @keyword 像素值 #}
{# @keyword 查询 #}
{# @keyword GDAL #}
{# @concept "gdal raster pixel-info 可以查询栅格中任意位置的像素值，支持像素坐标、地理坐标、矢量数据集三种指定位置的方式" #}
{# @concept "查询结果包括：输入坐标、转换为像元行列号的坐标、各波段的像素值（含未缩放值），对于VRT文件还会显示贡献源文件" #}
{# @note 未指定位置时，可从标准输入流逐行读取 'X Y' 坐标对 #}
{# @note 输出默认输出到标准输出，可用 --output 参数写入文件，当使用 --position-dataset 时默认输出格式为 GeoJSON #}
{# @seealso gdal_raster_info #}
{# @seealso gdal_raster_translate #}
{# @common_error "Unable to open datasource" — 输入的栅格文件不存在、格式不支持或文件损坏 #}
{# @common_error "band has not been selected" — 指定的波段编号超出范围，应使用 1 到实际波段数之间的整数 #}
{# @param input file_path required 输入栅格数据集路径 #}
{# @param pos string optional 像素位置坐标，格式为 'column,line'（像素坐标）或 'X,Y'（地理坐标） #}
{# @param of format optional 输出格式，默认输出到标准输出时为 GeoJSON options=GeoJSON,CSV,GML #}
{# @param b string optional 要查询的波段编号（从1开始的索引），可重复指定多个波段 #}
{# @param position_dataset file_path optional 包含坐标的矢量数据集，用于批量查询多个位置 #}
{# @param position_crs crs optional 位置坐标的CRS定义，默认为像素坐标 #}
{# @param output file_path optional 输出矢量数据集路径（由算法创建） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_pixel_info

gdal raster pixel-info{% if pos %} -p {{ pos | quote }}{% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if b %} -b {{ b | quote }}{% endif %}{% if position_dataset %} --position-dataset {{ position_dataset | safe_path | quote }}{% endif %}{% if position_crs %} --position-crs {{ position_crs | quote }}{% endif %}{% if output %} -o {{ output | safe_path | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
