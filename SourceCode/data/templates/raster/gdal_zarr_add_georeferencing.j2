{# @id gdal_zarr_add_georeferencing #}
{# @name ZARR数据集添加地理参考约定 #}
{# @description 为ZARR多维栅格数据集添加地理参考约定（GDAL或spatial_proj） #}
{# @keyword zarr #}
{# @keyword 多维栅格 #}
{# @keyword georeferencing #}
{# @keyword GDAL约定 #}
{# @keyword spatial_proj #}
{# @concept "ZARR是一种用于存储多维数组数据的云原生格式，支持高效的分块存储和访问" #}
{# @concept "地理参考约定定义了ZARR数组中的空间坐标信息，使数据具备地理空间意义" #}
{# @note 该命令是追加而非替换操作，不会覆盖已有的地理参考信息 #}
{# @note spatial_proj约定提供更标准的空间坐标系统描述方式 #}
{# @common_error " unable to open dataset" — 输入路径无效或指定的ZARR数据集不存在 #}
{# @common_error " unrecognized convention" — 指定的约定类型不受支持，请使用 GDAL 或 spatial_proj #}
{# @param input file_path required 输入的多维栅格ZARR数据集路径 #}
{# @param convention enum required 地理参考约定类型 options=GDAL,spatial_proj #}
{# @param config text optional GDAL配置选项，格式为 KEY=VALUE，可重复使用 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_zarr_add_georeferencing

gdal driver zarr add-georeferencing-convention{% if config %} --config {{ config | quote }}{% endif %} {{ input | safe_path | quote }} {{ convention | quote }}

REM Done
