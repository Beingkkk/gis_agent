{# @id gdal_raster_aspect #}
{# @name 坡向分析 #}
{# @description 从DEM生成坡向图，计算每个像元坡面的朝向方位角（0-360°） #}
{# @keyword dem #}
{# @keyword dem处理 #}
{# @keyword 坡向 #}
{# @keyword aspect #}
{# @keyword terrain #}
{# @keyword 地形分析 #}
{# @keyword azimuth #}
{# @keyword 坡度 #}
{# @concept "坡向（Aspect）表示坡面朝向，用0-360度表示：0°北、90°东、180°南、270°西" #}
{# @concept "输出为32位浮点栅格，无效值（平地或靠近nodata）为-9999" #}
{# @note 默认使用Horn算法（3×3窗口）计算梯度，结果假设栅格北朝上 #}
{# @note 使用--no-edges可在图像边缘1像素区域输出nodata避免插值 #}
{# @seealso gdal_raster_slope #}
{# @seealso gdal_raster_hillshade #}
{# @common_error "Unable to open datasource" — 输入栅格路径不存在或格式不支持，无法读取 #}
{# @common_error "Band index out of range" — 指定的波段序号超出输入栅格的实际波段数 #}
{# @param input file_path required 输入的高程栅格数据文件路径（DEM） #}
{# @param output file_path required 输出坡向栅格文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,HFA,COG,GPKG #}
{# @param co text optional 输出格式的创建选项（KEY=VALUE格式，可重复） #}
{# @param band integer optional 输入栅格的波段序号（从1开始） default=1 #}
{# @param convention enum optional 输出方位角的定义方式 default=azimuth options=azimuth,trigonometric-angle #}
{# @param gradient_alg enum optional 计算坡度的算法 default=Horn options=Horn,ZevenbergenThorne #}
{# @param zero_for_flat boolean optional 平地输出零值而非nodata #}
{# @param no_edges boolean optional 边缘像元不使用插值，直接输出nodata #}
{# @param overwrite boolean optional 覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有文件 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_aspect

gdal raster aspect{% if of %} -f {{ of | quote }}{% endif %}{% if co %} --co {{ co | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if band %} -b {{ band }}{% endif %}{% if convention %} --convention {{ convention | quote }}{% endif %}{% if gradient_alg %} --gradient-alg {{ gradient_alg | quote }}{% endif %}{% if zero_for_flat %} --zero-for-flat{% endif %}{% if no_edges %} --no-edges{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
