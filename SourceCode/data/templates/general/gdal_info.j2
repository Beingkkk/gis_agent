{# @id gdal_info #}
{# @name 数据集信息查询 #}
{# @description 查询栅格或矢量数据集的元数据、坐标系、边界框等信息 #}
{# @keyword info #}
{# @keyword metadata #}
{# @keyword json #}
{# @keyword 属性 #}
{# @keyword 元数据 #}
{# @keyword info #}
{# @keyword 查询 #}
{# @keyword 查看 #}
{# @concept "gdal info 是 GDAL 的元数据查询工具，可快速获取栅格（波段数、尺寸、投影等）和矢量（图层数、要素数、字段信息等）的详细信息" #}
{# @concept "使用 --format json 可输出结构化 JSON 格式，便于程序化处理" #}
{# @note 该命令是 gdal raster info 和 gdal vector info 的快捷方式，实际执行哪个取决于输入数据类型 #}
{# @note 输出的 JSON 格式包含坐标系、仿射变换参数、元数据等核心信息 #}
{# @common_error "ERROR 1: No such file or directory" — 指定的输入文件路径不存在或无法访问 #}
{# @common_error "Unable to open datasource" — 输入路径存在但不是有效的 GDAL 支持的数据集格式 #}
{# @param input file_path required 输入栅格、矢量或多维栅格数据集路径 #}
{# @param of enum optional 输出格式 default=json options=json,text #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_info

gdal info{% if of %} --format {{ of }}{% endif %} {{ input | safe_path | quote }}

REM Done
