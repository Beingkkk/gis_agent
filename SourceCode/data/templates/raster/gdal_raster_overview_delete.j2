{# @id gdal_raster_overview_delete #}
{# @name 删除栅格影像金字塔 #}
{# @description 删除栅格数据集的所有概览（pyramids/pyramids），用于清理或重新生成影像金字塔 #}
{# @keyword raster #}
{# @keyword overview #}
{# @keyword pyramid #}
{# @keyword pyramids #}
{# @keyword 删除 #}
{# @keyword 清理金字塔 #}
{# @keyword gdalraster #}
{# @concept "概览（Overview）是栅格影像的多分辨率金字塔，用于加速大范围影像的显示和浏览" #}
{# @concept "gdal raster overview delete 会直接修改原始数据（in-place），删除所有已存在的概览层" #}
{# @note 删除概览后原文件空间通常不会自动释放，可能需要使用 gdal raster convert 重新压缩才能回收空间 #}
{# @note 外部概览文件（如 GeoTIFF 的 .ovr 文件）需要加 --external 参数才会被删除 #}
{# @seealso gdal_raster_overview_build #}
{# @common_error "Unable to open datasource" — 指定的栅格文件路径不存在、文件损坏或格式不被 GDAL 支持 #}
{# @common_error "Permission denied" — 文件为只读状态或当前用户没有写入权限 #}
{# @param input file_path required 要删除概览的栅格数据集路径，支持 GeoTIFF、GPKG 等格式 #}
{# @param external boolean optional 删除外部概览文件（如 .ovr 文件） #}
{# @param open_options text optional 打开数据源时的额外选项，格式为 KEY=VALUE，可重复使用 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_overview_delete

gdal raster overview delete{% if input %} {{ input | safe_path | quote }}{% endif %}{% if external %} --external{% endif %}{% if open_options %} --oo {{ open_options | quote }}{% endif %}

REM Done
