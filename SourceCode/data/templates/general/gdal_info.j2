{# @id gdal_info #}
{# @name 数据集信息查看 #}
{# @description 使用 gdal info 查看栅格、矢量或多维数据集的元数据信息 #}
{# @keyword info #}
{# @keyword metadata #}
{# @keyword json #}
{# @keyword 栅格信息 #}
{# @keyword 矢量信息 #}
{# @keyword 元数据 #}
{# @keyword 查看 #}
{# @concept "gdal info 是 GDAL 的通用数据集查询工具，可自动识别栅格、矢量或高维数据格式并输出相应的元数据信息" #}
{# @concept "输出格式支持 JSON（结构化，便于程序解析）和 TEXT（人类可读）两种模式" #}
{# @note gdal info 会自动检测输入数据的类型并调用相应的子命令（raster info 或 vector info） #}
{# @note JSON 格式输出包含坐标系统、像素大小、覆盖范围、属性字段等详细信息 #}
{# @seealso ogr2ogr_convert #}
{# @seealso gdal_translate #}
{# @common_error "Unable to open" — 指定的输入文件路径不存在或文件损坏，GDAL 无法识别该数据格式 #}
{# @common_error "Permission denied" — 对输入文件没有读取权限，可能需要检查文件或目录权限设置 #}
{# @param input file_path required 输入的栅格、矢量或 multidimensional raster 数据集路径 #}
{# @param of format optional 输出格式 default=json options=json,text #}
{# @param config text optional 配置选项 KEY=VALUE 形式，可重复使用 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_info

gdal info{% if of %} -of {{ of | quote }}{% endif %}{% if config %} --config {{ config | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
