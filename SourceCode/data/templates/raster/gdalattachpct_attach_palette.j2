{# @id gdalattachpct_attach_palette #}
{# @name 栅格调色板附加 #}
{# @description 将调色板文件中的颜色表（调色板）附加到栅格数据，生成RGB彩色输出 #}
{# @keyword palette #}
{# @keyword color table #}
{# @keyword 调色板 #}
{# @keyword 颜色表 #}
{# @keyword rgb #}
{# @keyword 栅格 #}
{# @keyword 影像 #}
{# @concept "gdalattachpct 是一个 Python 工具，用于将调色板（颜色表）从一个文件复制到另一个栅格数据，生成真彩色RGB输出" #}
{# @concept "调色板文件可以是带颜色表的栅格（如GIF、单波段GeoTIFF）或颜色配置文件（txt/qml/qlr）" #}
{# @note 源栅格文件通常是单波段灰度影像，附加调色板后转换为RGB彩色影像 #}
{# @note 工具需要 GDAL Python 绑定支持，非 Python 版本无法使用 #}
{# @seealso gdalinfo_raster_info #}
{# @seealso rgb2pct_convert_to_palette #}
{# @common_error "Unable to open" — 调色板文件或源文件路径不存在，或文件格式 GDAL 不支持 #}
{# @common_error "Error: palette_file has no color table" — 指定的调色板栅格文件没有关联的颜色表 #}
{# @param palette_file file_path required 调色板文件路径（包含颜色表的栅格文件或颜色配置文件如txt/qml/qlr） #}
{# @param source_file file_path required 输入栅格文件路径 #}
{# @param dest_file file_path required 输出RGB彩色文件路径 #}
{# @param of format optional 输出格式名称 default=GTiff options=GTiff,JPEG,PNG,HFA,ECW,MBTiled #}

@echo off
REM Generated by GIS Agent
REM Template: gdalattachpct_attach_palette

gdalattachpct{% if of %} -of {{ of | quote }}{% endif %} {{ palette_file | safe_path | quote }} {{ source_file | safe_path | quote }} {{ dest_file | safe_path | quote }}

REM Done
