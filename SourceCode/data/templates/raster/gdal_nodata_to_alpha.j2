{# @id gdal_nodata_to_alpha #}
{# @name 栅格nodata转Alpha通道 #}
{# @description 将栅格数据中的nodata值转换为透明Alpha通道 #}
{# @keyword tiff #}
{# @keyword geotiff #}
{# @keyword alpha #}
{# @keyword nodata #}
{# @keyword 透明 #}
{# @keyword 栅格处理 #}
{# @concept "Alpha通道是栅格数据的第四个波段，用于控制像素透明度。nodata值转为Alpha后，可实现真正的透明背景显示" #}
{# @note 如果输入数据没有设置nodata值，该工具不会执行任何操作（no-op） #}
{# @note --nodata选项可以覆盖输入波段上的nodata元数据 #}
{# @param input file_path required 输入栅格数据集路径 #}
{# @param output file_path required 输出栅格数据集路径 #}
{# @param of format optional 输出格式名称 default=GTiff options=GTiff,COG,GPKG,ENVI,HFA #}
{# @param nodata string optional 显式指定nodata值（数值、nan、inf、-inf） #}
{# @param overwrite boolean optional 覆盖已存在的输出文件 #}
{# @param append boolean optional 作为子数据集追加到现有文件 #}
{# @param input_format format optional 输入数据格式 options=GTiff,HFA,ENVI,GPKG,HDF5 #}
{# @param creation_option string optional 创建选项（KEY=VALUE格式） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_nodata_to_alpha

gdal raster nodata-to-alpha{% if of %} -f {{ of | quote }}{% endif %}{% if nodata %} --nodata {{ nodata | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if input_format %} --if {{ input_format | quote }}{% endif %}{% if creation_option %} --co {{ creation_option | quote }}{% endif %}{{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
