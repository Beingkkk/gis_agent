{# @id gdalmdimtranslate #}
{# @name 多维栅格格式转换 #}
{# @description 使用 gdalmdimtranslate 在不同多维栅格格式之间转换，支持数组选择和维度子集操作 #}
{# @concept "gdalmdimtranslate 是 GDAL 的多维栅格转换工具，支持 NetCDF、Zarr、GRIB、HDF5、JSON 等多维数据格式互转" #}
{# @concept "可通过 -array 和 -subset 参数对特定数组执行选择、重采样、转置和维度切片等操作" #}
{# @note 输出格式默认从文件扩展名自动判断，也可用 -of 显式指定 #}
{# @note 当只转换一个二维数组时，也可输出为经典 2D 栅格格式 #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在，或指定的格式驱动无法识别该文件 #}
{# @common_error "Array does not exist" — 指定的数组名称不存在，或维度子集语法错误 #}
{# @param input file_path required 输入多维栅格数据集路径（源数据集） #}
{# @param output file_path required 输出多维栅格数据集路径（目标数据集） #}
{# @param if string optional 输入格式驱动名称，指定后跳过自动检测 #}
{# @param of string optional 输出格式名称，如 NetCDF、Zarr、多维 VRT 等 #}
{# @param co string optional 创建选项，格式为 NAME=VALUE，可指定 ARRAY: 前缀设置数组级选项 #}
{# @param arrays string optional 选择要转换的数组，可指定重命名、重采样、转置或视图表达式 #}
{# @param subsets string optional 维度子集规格，如 [dim,start,end] 或 [dim,value] 形式 #}
{# @param quiet boolean optional 静默模式，抑制进度消息和非错误输出 #}
{# @param array string optional (auto-completed) #}
{# @param subset string optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdalmdimtranslate

gdalmdimtranslate{% if quiet %} -q{% endif %}{% if if %} -if {{ if | quote }}{% endif %}{% if of %} -of {{ of | quote }}{% endif %}{% if co %} -co {{ co | quote }}{% endif %}{% for array in arrays %} -array {{ array | quote }}{% endfor %}{% for subset in subsets %} -subset {{ subset | quote }}{% endfor %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
