{# @id gdal_create #}
{# @name 创建栅格数据集 #}
{# @description 使用 gdal_create 创建空白栅格文件，设置尺寸、波段数、数据类型、坐标系统和元数据 #}
{# @keyword tiff #}
{# @keyword geotiff #}
{# @keyword 创建 #}
{# @keyword 栅格 #}
{# @keyword 空白 #}
{# @keyword 波段 #}
{# @keyword 尺寸 #}
{# @keyword nodata #}
{# @concept "gdal_create 用于创建空白栅格文件，可设置图像尺寸、波段数、数据类型、空间参考和地理范围等基础参数" #}
{# @concept "可通过 -burn 参数初始化所有像素为指定值，或使用 -a_nodata 设置背景无数据标记" #}
{# @note 默认输出格式根据文件扩展名自动判断，TIFF 文件使用 GTiff 驱动 #}
{# @note 如果不指定 -outsize，默认创建 0x0 的空文件 #}
{# @common_error "Failed to create output file" — 输出路径无效或不可写，或者指定的格式驱动不可用 #}
{# @common_error "Invalid band count" — 波段数量必须为正整数，超出驱动支持范围 #}
{# @param output file_path required 输出栅格文件路径 #}
{# @param of format optional 输出格式 options=GTiff,COG,GPKG,HFA,JP2OpenJPEG,ENVI,PCRaster #}
{# @param outsize_x integer optional 输出图像宽度（像素） #}
{# @param outsize_y integer optional 输出图像高度（像素） #}
{# @param bands integer optional 波段数量，默认值为1 default=1 #}
{# @param ot enum optional 输出数据类型 options=Byte,Int8,UInt16,Int16,UInt32,Int32,UInt64,Int64,Float32,Float64,CInt16,CInt32,CFloat32,CFloat64 #}
{# @param burn string optional 写入所有波段的固定值（像素初始值） #}
{# @param a_srs crs optional 覆盖输出文件的坐标系统定义（不进行投影转换） #}
{# @param ulx float optional 左上角X坐标（与 uly、lrx、lry 配合使用设置地理范围） #}
{# @param uly float optional 左上角Y坐标 #}
{# @param lrx float optional 右下角X坐标 #}
{# @param lry float optional 右下角Y坐标 #}
{# @param a_nodata string optional 为输出波段设置的无效值标记 #}
{# @param mo string optional 元数据键值对，格式为 KEY=VALUE #}
{# @param co string optional 格式驱动创建选项，如压缩方式、分块策略等 #}
{# @param if file_path optional 模板输入数据集，用于继承其尺寸、波段、数据类型等参数（仅 3.3+ 版本） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_create

gdal_create{% if of %} -of {{ of | quote }}{% endif %}{% if outsize_x and outsize_y %} -outsize {{ outsize_x | quote }} {{ outsize_y | quote }}{% endif %}{% if bands %} -bands {{ bands | quote }}{% endif %}{% if ot %} -ot {{ ot | quote }}{% endif %}{% if burn %} -burn {{ burn | quote }}{% endif %}{% if a_srs %} -a_srs {{ a_srs | quote }}{% endif %}{% if ulx and uly and lrx and lry %} -a_ullr {{ ulx | quote }} {{ uly | quote }} {{ lrx | quote }} {{ lry | quote }}{% endif %}{% if a_nodata %} -a_nodata {{ a_nodata | quote }}{% endif %}{% if mo %} -mo {{ mo | quote }}{% endif %}{% if co %} -co {{ co | quote }}{% endif %}{% if if %} -if {{ if | quote }}{% endif %} {{ output | safe_path | quote }}

REM Done
