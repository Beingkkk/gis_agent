{# @id gdal_raster_aspect #}
{# @name 坡向计算 #}
{# @description 从DEM高程数据生成坡向图，计算地表坡面朝向角度 #}
{# @keyword dem #}
{# @keyword elevation #}
{# @keyword aspect #}
{# @keyword terrain #}
{# @keyword azimuth #}
{# @keyword 坡向 #}
{# @keyword 地形分析 #}
{# @keyword 高程 #}
{# @concept "坡向（Aspect）表示地表坡面的朝向，以北向为0度，顺时针方向递增：东90°、南180°、西270°" #}
{# @concept "输出为32位浮点栅格，值为0-360度，无效区域使用-9999作为nodata标识" #}
{# @note 默认情况下边缘像素和nodata区域会通过插值计算，如需避免插值请使用--no-edges参数 #}
{# @note 输入DEM应保证高程值正确，无效像素应用nodata标识以获得准确的坡向结果 #}
{# @seealso gdal_raster_slope #}
{# @common_error "nodata value in target" — 输入DEM中存在nodata值，导致输出坡向图出现大片无效区域，建议预先处理DEM数据 #}
{# @common_error "Unable to open dataset" — 指定的输入DEM文件不存在或格式不被GDAL支持 #}
{# @param input file_path required 输入高程/DEM栅格文件路径 #}
{# @param output file_path required 输出坡向栅格文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,HFA,ENVI,COG #}
{# @param co text optional GDAL创建选项（KEY=VALUE格式） #}
{# @param band integer optional 输入波段序号（1-based, default: 1） default=1 #}
{# @param convention enum optional 角度输出约定 default=azimuth options=azimuth,trigonometric-angle #}
{# @param gradient_alg enum optional 梯度计算算法 default=Horn options=Horn,ZevenbergenThorne #}
{# @param zero_for_flat boolean optional 平坦区域输出零值而非nodata #}
{# @param no_edges boolean optional 边缘像素不插值，直接设为nodata #}
{# @param overwrite boolean optional 覆盖已存在的输出文件 #}
{# @param append boolean optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_aspect

gdal raster aspect{% if of %} --format {{ of | quote }}{% endif %}{% if co %} --creation-option {{ co | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if band %} --band {{ band | quote }}{% endif %}{% if convention %} --convention {{ convention | quote }}{% endif %}{% if gradient_alg %} --gradient-alg {{ gradient_alg | quote }}{% endif %}{% if zero_for_flat %} --zero-for-flat{% endif %}{% if no_edges %} --no-edges{% endif %} --input {{ input | safe_path | quote }} --output {{ output | safe_path | quote }}

REM Done
