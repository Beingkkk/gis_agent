{# @id rgb2pct #}
{# @name RGB转伪彩色调色板 #}
{# @description 将RGB图像转换为使用最优调色板的伪彩色图像，支持调色板文件导入和自定义颜色数 #}
{# @keyword rgb #}
{# @keyword pct #}
{# @keyword pseudo-color #}
{# @keyword palette #}
{# @keyword 调色板 #}
{# @keyword 伪彩色 #}
{# @keyword 栅格转换 #}
{# @keyword 颜色表 #}
{# @concept "rgb2pct使用中位切割算法计算最优调色板，然后通过Floyd-Steinberg抖动（误差扩散）技术将RGB图像转换为伪彩色图像，最大化输出视觉质量" #}
{# @concept "该工具需要GDAL Python绑定支持，输出格式需支持调色板（如GeoTIFF、JPEG等）" #}
{# @note 当同时指定-n和-pct时，-pct优先级更高，会使用指定的调色板文件而非计算 #}
{# @note 输出格式默认从文件扩展名推断，建议使用支持调色板的格式如GTiff #}
{# @seealso gdal_translate_palette #}
{# @common_error "Image has fewer than 2 unique values" — 输入图像通道数不足或颜色种类太少，无法生成有效的调色板 #}
{# @common_error "Not enough colors, 256 distinct values after rounding" — 指定的-n值小于图像中实际存在的不同颜色数量 #}
{# @param input file_path required 输入RGB图像文件路径 #}
{# @param output file_path required 输出伪彩色图像文件路径 #}
{# @param n integer optional 生成调色板的颜色数量，默认256，范围2-256 #}
{# @param pct file_path optional 从指定的调色板文件导入颜色表（可以是GDAL支持的栅格文件或颜色配置文件txt/qml/qlr） #}
{# @param of format optional 输出格式名称，使用短格式名 options=GTiff,JPEG,PNG,HFA,EHdr #}
{# @param creation_option string optional GDAL驱动创建选项，可多次指定（如COMPRESS=LZW） #}

@echo off
REM Generated by GIS Agent
REM Template: rgb2pct

rgb2pct{% if creation_option %} --creation-option {{ creation_option | quote }}{% endif %}{% if n %} -n {{ n }}{% endif %}{% if pct %} -pct {{ pct | safe_path | quote }}{% endif %}{% if of %} -of {{ of | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
