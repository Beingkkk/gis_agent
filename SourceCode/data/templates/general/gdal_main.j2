{# @id gdal_main #}
{# @name GDAL主入口 #}
{# @description GDAL的统一入口工具，通过子命令访问各类栅格和矢量数据处理功能 #}
{# @concept "gdal 是 GDAL 工具集的统一入口点，通过子命令分发到具体功能模块，如 raster、vector、dataset 等" #}
{# @concept "常见使用方式包括 gdal info 查看元数据、gdal convert 格式转换、gdal pipeline 流程处理等" #}
{# @note 不带参数运行 gdal 会显示可用子命令列表 #}
{# @note gdal <FILENAME> 是 gdal info <FILENAME> 的快捷方式 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unrecognized command" — 指定的子命令不存在，请检查是否拼写正确或 GDAL 版本是否支持该命令 #}
{# @common_error "Permission denied" — 指定的文件路径没有读取或写入权限 #}
{# @param command string optional GDAL子命令，包括 convert、dataset、driver、info、mdim、pipeline、raster、vector、vsi 等 #}
{# @param filename file_path optional 数据文件路径，gdal <FILENAME> 会自动执行 info 命令 #}
{# @param config string optional GDAL配置选项，格式为 KEY=VALUE，可多次使用 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_main

gdal{% if command %} {{ command }}{% endif %}{% if config %} --config {{ config | quote }}{% endif %}{% if filename %} {{ filename | safe_path | quote }}{% endif %}

REM Done
