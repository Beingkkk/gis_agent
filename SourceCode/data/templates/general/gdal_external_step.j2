{# @id gdal_external_step #}
{# @name 外部程序执行 #}
{# @description 在 GDAL 管道中执行任意外部程序或命令，支持输入输出文件自动替换 #}
{# @keyword pipeline #}
{# @keyword external #}
{# @keyword shell #}
{# @keyword command #}
{# @keyword execute #}
{# @keyword 管道 #}
{# @keyword 外部命令 #}
{# @concept "gdal external 是 GDAL 管道系统的外部程序执行步骤，支持在管道中插入任意外部工具或脚本进行处理" #}
{# @concept "占位符 <INPUT> 会被替换为前一步输出的临时文件路径，<OUTPUT> 生成下一步的临时文件，<INPUT-OUTPUT> 用于原地更新文件" #}
{# @note 默认临时文件存放在 CPL_TMPDIR 指定的目录，栅格默认格式为 GeoTIFF，矢量默认格式为 GeoPackage #}
{# @note 执行外部程序需要将 GDAL_ENABLE_EXTERNAL 配置选项设置为 YES #}
{# @common_error "External command execution is not allowed" — GDAL_ENABLE_EXTERNAL 配置选项未设置为 YES，外部命令执行被禁用 #}
{# @common_error "File not found" — 命令中引用的文件路径不存在或不可访问 #}
{# @param command string required 要执行的外部命令，可包含占位符：<INPUT>（前一步输入）、<OUTPUT>（输出文件）、<INPUT-OUTPUT>（原地更新） #}
{# @param input_format format optional 指定输入文件的格式（默认：栅格为 GeoTIFF，矢量为 GeoPackage） options=GTiff,GPKG,GeoJSON,GML,HDF5,NetCDF #}
{# @param output_format format optional 指定输出文件的格式（默认：栅格为 GeoTIFF，矢量为 GeoPackage） options=GTiff,GPKG,GeoJSON,GML,HDF5,NetCDF #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_external_step

gdal external {{ command | quote }}{% if input_format %} --input-format {{ input_format | quote }}{% endif %}{% if output_format %} --output-format {{ output_format | quote }}{% endif %}

REM Done
