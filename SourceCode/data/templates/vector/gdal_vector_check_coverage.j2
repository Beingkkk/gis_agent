{# @id gdal_vector_check_coverage #}
{# @name 多边形覆盖检查 #}
{# @description 检查矢量多边形数据集是否形成有效的空间覆盖，输出无效边缘的线性要素 #}
{# @concept "有效多边形覆盖需满足三个条件：相邻多边形之间无重叠、公共边界在相同位置有节点、缝隙宽度不超过指定阈值" #}
{# @concept "该工具通过检测无效边缘并将其输出为线性要素来报告覆盖问题，适合用于数据质量检查和修复" #}
{# @note 需要 GDAL 链接 GEOS 库 3.12 或更高版本才能使用此工具 #}
{# @note 输出结果为线性要素层，每个要素代表一条被检测为无效的覆盖边界 #}
{# @seealso gdal_vector_check_geometry #}
{# @seealso gdal_vector_check_topology #}
{# @common_error "Unable to open datasource" — 输入文件路径不存在或指定的格式不受支持，请检查文件是否可读 #}
{# @common_error "This functionality requires a GDAL build with GEOS >= 3.12" — 当前 GDAL 编译版本未链接所需版本的 GEOS 库，请升级 GDAL 或使用支持 GEOS 3.12+ 的版本 #}
{# @param input file_path required 输入的矢量多边形数据集路径，用于检查覆盖有效性 #}
{# @param output file_path required 输出矢量数据集，存储无效覆盖边缘的线性要素 #}
{# @param geometry_field string optional 待检查的几何字段名称，默认为图层的主几何字段 #}
{# @param maximum_gap_width float optional 标记为无效的最大缝隙宽度，缝隙超过此值的相邻多边形将被输出其边界线 #}
{# @param include_valid boolean optional 是否在输出中包含有效多边形（空几何形状） #}
{# @param input_layer string optional 输入图层名称，指定处理特定图层而非所有图层 #}
{# @param output_format string optional 输出格式名称，如 GeoJSON、ESRI Shapefile 等 #}
{# @param creation_option string optional 数据集创建选项（KEY=VALUE格式） #}
{# @param layer_creation_option string optional 图层创建选项（KEY=VALUE格式） #}
{# @param overwrite boolean optional 是否允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 是否允许追加到现有图层 #}
{# @param output_layer string optional 输出图层的新名称 #}
{# @param skip_errors boolean optional 写入要素时是否跳过错误继续处理 #}
{# @param no_create_empty_layers boolean optional 是否避免创建没有要素的图层 #}
{# @param quiet boolean optional 静默模式，不显示进度条或警告信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_check_coverage

gdal vector check-coverage{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if geometry_field %} --geometry-field {{ geometry_field | quote }}{% endif %}{% if maximum_gap_width %} --maximum-gap-width {{ maximum_gap_width }}{% endif %}{% if include_valid %} --include-valid{% endif %}{% if quiet %} -q{% endif %}{% if output_format %} --output-format {{ output_format | quote }}{% endif %}{% if creation_option %} --co {{ creation_option | quote }}{% endif %}{% if layer_creation_option %} --lco {{ layer_creation_option | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if skip_errors %} --skip-errors{% endif %}{% if no_create_empty_layers %} --no-create-empty-layers{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
