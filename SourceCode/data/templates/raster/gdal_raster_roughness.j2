{# @id gdal_raster_roughness #}
{# @name 栅格粗糙度计算 #}
{# @description 基于DEM数据计算表面粗糙度，生成反映地形起伏程度的单波段栅格 #}
{# @keyword roughness #}
{# @keyword dem #}
{# @keyword elevation #}
{# @keyword terrain #}
{# @keyword 起伏 #}
{# @keyword 粗糙度 #}
{# @keyword 地形分析 #}
{# @keyword dem #}
{# @concept "粗糙度是中心像元与其周围8个像元（3x3窗口）之间的最大高程差值，反映地表起伏剧烈程度" #}
{# @concept "该算法参考 Wilson et al. (2007, Marine Geodesy 30:3-35) 的定义，常用于地形分析和地表特征提取" #}
{# @note 输出默认使用 -9999 作为 nodata 值 #}
{# @note 当3x3窗口内存在 nodata 值时，默认会进行插值计算；使用 --no-edges 可避免插值，直接输出 nodata #}
{# @seealso gdal_raster_slope #}
{# @seealso gdal_raster_aspect #}
{# @seealso gdal_raster_hillshade #}
{# @common_error "Too few parameters" — 未提供必需的输入和输出文件路径 #}
{# @common_error "Selected band is out-of-range" — 指定的波段编号超过输入栅格的实际波段数 #}
{# @param input file_path required 输入高程栅格文件路径（支持DEM等高程数据） #}
{# @param output file_path required 输出粗糙度栅格文件路径 #}
{# @param of format optional 输出栅格格式 default=GTiff options=GTiff,GPKG,HFA,JPEG,PNG #}
{# @param band integer optional 输入栅格的波段编号（从1开始，默认为1） default=1 #}
{# @param no_edges boolean optional 不尝试插值边缘或靠近nodata值的位置，直接将1像素边框设置为nodata #}
{# @param overwrite boolean optional 覆盖已存在的输出文件（与--append互斥） #}
{# @param append boolean optional 作为子数据集追加到现有输出文件（与--overwrite互斥） #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_roughness

gdal raster roughness {% if input %} {{ input | safe_path | quote }} {% endif %}{% if output %} {{ output | safe_path | quote }} {% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if no_edges %} --no-edges{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}

REM Done
