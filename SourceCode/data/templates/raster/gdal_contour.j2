{# @id gdal_contour #}
{# @name 等高线提取 #}
{# @description 从栅格DEM数据提取矢量等高线或等高面 #}
{# @keyword dem #}
{# @keyword 等高线 #}
{# @keyword contour #}
{# @keyword 高程 #}
{# @keyword raster #}
{# @keyword 矢量转换 #}
{# @keyword elevation #}
{# @concept "gdal_contour 是 GDAL 的等高线提取工具，将栅格数字高程模型(DEM)转换为矢量等高线要素，等高线高侧始终在右侧（沿等高线顺时针环绕高地）" #}
{# @concept "支持三种等高线生成模式：等间隔模式(-i)、固定高程模式(-fl)、指数增长模式(-e)" #}
{# @note 必须指定等高线间隔参数：interval、fixed_levels 或 exp_base 三者选其一 #}
{# @note 多边形模式(-p)需要至少两个固定等级才能生成有效多边形，可使用 MIN/MAX 关键字包含栅格数据的实际最小/最大值 #}
{# @common_error "Failed to create feature" — 输出格式不支持指定的字段名或属性类型，或写入路径无写入权限 #}
{# @common_error "band out of range" — 指定的波段号超出栅格文件实际波段数量（从1开始计数） #}
{# @param input file_path required 输入栅格DEM文件路径 #}
{# @param output file_path required 输出矢量等高线文件路径 #}
{# @param interval float optional 等高线高程间隔（如10表示每10米一条等高线），与 fixed_levels 或 exp_base 互斥 #}
{# @param fixed_levels string optional 指定固定等高线高程值，用空格分隔（如 '100 200 300'），与 interval 或 exp_base 互斥 #}
{# @param exp_base float optional 指数等高线底数，生成 base^k 的等高线序列，与 interval 或 fixed_levels 互斥 #}
{# @param offset float optional 等高线基准偏移量，用于调整等高线起始位置 #}
{# @param aname string optional 输出图层中存储高程值的属性字段名 #}
{# @param of format optional 输出矢量格式 options=ESRI Shapefile,GeoJSON,GPKG,KML,GML,MapInfo File #}
{# @param band integer optional 从输入栅格中读取高程数据的波段号 default=1 #}
{# @param three_d boolean optional 生成3D矢量线（包含每个顶点的高程值），默认输出2D矢量 #}
{# @param polygon boolean optional 生成等高面多边形而非等高线，需配合 interval、fixed_levels 或 exp_base 使用 #}
{# @param nln string optional 输出图层的名称 default=contour #}
{# @param snodata float optional 指定栅格中代表无数据的像素值 #}
{# @param inodata boolean optional 忽略数据集中的nodata值，将所有像素视为有效数据 #}
{# @param amin string optional 等高面多边形的最小高程属性名（仅在多边形模式下有效） #}
{# @param amax string optional 等高面多边形的最大高程属性名（仅在多边形模式下有效） #}
{# @param quiet boolean optional 静默模式，不输出进度信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_contour

gdal_contour{% if of %} -f {{ of | quote }}{% endif %}{% if band %} -b {{ band }}{% endif %}{% if aname %} -a {{ aname | quote }}{% endif %}{% if amin %} -amin {{ amin | quote }}{% endif %}{% if amax %} -amax {{ amax | quote }}{% endif %}{% if snodata %} -snodata {{ snodata }}{% endif %}{% if inodata %} -inodata{% endif %}{% if interval %} -i {{ interval }}{% endif %}{% if exp_base %} -e {{ exp_base }}{% endif %}{% if offset %} -off {{ offset }}{% endif %}{% if polygon %} -p{% endif %}{% if three_d %} -3d{% endif %}{% if nln %} -nln {{ nln | quote }}{% endif %}{% if quiet %} -q{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
