{# @id gdal_sieve #}
{# @name 栅格区域过滤 #}
{# @description 移除栅格数据中面积小于阈值的孤立小区域像元 #}
{# @keyword sieve #}
{# @keyword raster #}
{# @keyword filter #}
{# @keyword threshold #}
{# @keyword 小区域 #}
{# @keyword 孤立像元 #}
{# @keyword 清理 #}
{# @concept "gdal_sieve 通过连通区域分析移除栅格中过小的斑块，被移除的区域会被替换为相邻最大邻域的像元值" #}
{# @concept "该工具将数据读取为整数类型，因此浮点数据会先被取整处理" #}
{# @note 对于浮点数据（如0-1范围的32位浮点），可能需要先进行重缩放以获得有效的整数值 #}
{# @note 默认使用8连通邻域分析，-4参数可切换为4连通分析 #}
{# @seealso gdal_polygonize #}
{# @common_error "Unable to open datasource" — 输入栅格文件路径不存在或格式无法识别 #}
{# @common_error "Round down to integer for input" — 浮点数据被取整可能导致处理结果不准确，建议先对数据进行预处理或重缩放 #}
{# @param input file_path required 输入栅格文件路径 #}
{# @param output file_path optional 输出栅格文件路径 #}
{# @param st integer optional 面积阈值（像素数量），小于此阈值的区域将被替换 #}
{# @param four boolean optional 使用4连通邻域而非默认的8连通 #}
{# @param eight boolean optional 使用8连通邻域（默认） #}
{# @param o string optional 输出选项，格式为 name=value #}
{# @param mask_file file_path optional 指定掩膜栅格文件路径 #}
{# @param nomask boolean optional 不使用默认掩膜 #}
{# @param of format optional 输出格式 default=GTiff options=GTiff,JPEG,PNG,ENVI,HFA #}
{# @param quiet boolean optional 静默模式，不显示进度信息 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_sieve

gdal_sieve{% if quiet %} -q{% endif %}{% if st %} -st {{ st }}{% endif %}{% if four %} -4{% endif %}{% if eight %} -8{% endif %}{% if o %} -o {{ o | quote }}{% endif %}{% if mask_file %} -mask {{ mask_file | quote }}{% endif %}{% if nomask %} -nomask{% endif %}{% if of %} -of {{ of | quote }}{% endif %} {{ input | safe_path | quote }}{% if output %} {{ output | safe_path | quote }}{% endif %}

REM Done
