{# @id gdal_raster_index #}
{# @name 栅格瓦片索引创建 #}
{# @description 为多个栅格数据集创建矢量索引，生成包含每个栅格范围边界多边形的矢量文件 #}
{# @concept "栅格索引（Tile Index）是一种记录多个栅格文件位置和边界的矢量数据集，每个记录对应一个栅格文件，包含文件路径和覆盖范围的多边形几何" #}
{# @concept "生成的矢量索引可用于 MapServer、GDAL GTI 驱动等工具高效管理大量栅格瓦片，尤其适合卫星影像、分幅 DEM 等大规模栅格数据集" #}
{# @note 输出矢量图层会自动包含 location 字段存储栅格路径，以及表示栅格覆盖范围的多边形几何 #}
{# @note 使用 --recursive 可自动扫描目录及其子目录下的所有栅格文件，适合处理大量分幅数据 #}
{# @seealso gdal_raster_tileindex #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Unable to open datasource" — 输入的栅格文件不存在、文件损坏或格式不支持 #}
{# @common_error "Permission denied" — 输出路径无写入权限，或目标文件被其他程序占用 #}
{# @common_error "Output driver not recognised" — 指定的输出格式不被支持，需使用 gdalinfo --formats 查看可用格式 #}
{# @param input file_path required 输入栅格文件路径，支持多个文件或目录（可重复使用多次） #}
{# @param output file_path required 输出矢量索引文件路径（GeoPackage、 Shapefile 等格式） #}
{# @param of string optional 输出矢量格式，如 GPKG、ESRI Shapefile、GeoJSON 等 #}
{# @param lco string optional 图层创建选项（如 Shapefile 的 ENCODING 参数） #}
{# @param nln string optional 输出图层名称 #}
{# @param output_crs crs optional 输出矢量图层的坐标系统（如 EPSG:4326） #}
{# @param location_name string optional 存储栅格路径的字段名称 default=location #}
{# @param recursive boolean optional 递归扫描输入目录中的所有栅格文件 #}
{# @param absolute_path boolean optional 将栅格路径存储为绝对路径（默认相对路径） #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 追加到现有图层 #}
{# @param filename_filter string optional 文件名过滤模式（支持 * 和 ? 通配符） #}
{# @param min_pixel_size float optional 栅格最小像素分辨率阈值（地理范围/像素） #}
{# @param max_pixel_size float optional 栅格最大像素分辨率阈值（地理范围/像素） #}
{# @param metadata string optional 添加数据集元数据项（格式：KEY=VALUE） #}
{# @param source_crs_format string optional 存储源栅格 CRS 的格式（auto/WKT/EPSG/PROJ） default=auto #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_index

gdal raster index{% if of %} -of {{ of | quote }}{% endif %}{% if lco %} -lco {{ lco | quote }}{% endif %}{% if nln %} -nln {{ nln | quote }}{% endif %}{% if output_crs %} -output-crs {{ output_crs | quote }}{% endif %}{% if location_name %} -location-name {{ location_name | quote }}{% endif %}{% if filename_filter %} -filename-filter {{ filename_filter | quote }}{% endif %}{% if recursive %} --recursive{% endif %}{% if absolute_path %} --absolute-path{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if min_pixel_size %} --min-pixel-size {{ min_pixel_size }}{% endif %}{% if max_pixel_size %} --max-pixel-size {{ max_pixel_size }}{% endif %}{% if metadata %} --metadata {{ metadata | quote }}{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
