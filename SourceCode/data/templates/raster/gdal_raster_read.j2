{# @id gdal_raster_read #}
{# @name 栅格数据读取 #}
{# @description 在 GDAL 管线中读取单个栅格数据集，作为管线处理的第一步 #}
{# @keyword gdal #}
{# @keyword raster #}
{# @keyword read #}
{# @keyword 读取 #}
{# @keyword tif #}
{# @keyword gtiff #}
{# @keyword jp2 #}
{# @keyword 管线 #}
{# @keyword pipeline #}
{# @concept "gdal raster read 是 GDAL 管线工具的一部分，用于在管线中读取栅格数据，作为后续处理步骤的输入" #}
{# @concept "管线是一种将多个 GDAL 操作链接在一起的处理方式，数据从前一步流向下一步" #}
{# @note read 操作必须是管线的第一步，用于读取输入数据 #}
{# @note input_format 和 open_option 参数在需要显式指定格式或特殊打开选项时使用 #}
{# @common_error "Unable to open" — 指定的栅格文件路径不存在或文件格式不受支持 #}
{# @common_error "Unsupported Open Option" — 指定的 open_option 格式错误，应为 KEY=VALUE 格式 #}
{# @param input file_path required 输入栅格数据集路径（如 GeoTIFF、JP2、IMG 等格式） #}
{# @param input_format format optional 输入数据格式 options=GTiff,JPEG2000,HFA,ENVI,GRIB,netCDF #}
{# @param open_option text optional 打开选项 (KEY=VALUE 格式)，可重复使用 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_raster_read

read{% if input_format %} --if {{ input_format | quote }}{% endif %}{% if open_option %} --oo {{ open_option | quote }}{% endif %} {{ input | safe_path | quote }}

REM Done
