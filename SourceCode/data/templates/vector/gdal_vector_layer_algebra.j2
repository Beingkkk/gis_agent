{# @id gdal_vector_layer_algebra #}
{# @name 矢量图层代数运算 #}
{# @description 对两个矢量图层执行集合代数运算，包括并集、交集、对称差集、裁剪、擦除等操作 #}
{# @concept "矢量图层代数运算通过对两个图层执行空间集合操作，生成新的几何要素，如 clip（裁剪）保留被方法图层覆盖的区域，erase（擦除）则移除与方法图层重叠的部分" #}
{# @concept "union 将两个图层的所有要素合并，intersection 仅保留重叠区域，sym-difference 保留非重叠部分" #}
{# @note clip 和 erase 操作基于方法图层的几何形状对输入图层进行空间过滤，是常用的空间裁切工具 #}
{# @note 运算结果会保留两个输入图层的属性字段，可通过字段选择参数控制输出字段 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Geometry type mismatch" — 输入图层和方法图层的几何类型不兼容，或指定的几何类型参数不正确 #}
{# @common_error "Invalid operation" — 指定的运算类型不在支持列表中，请检查 operation 参数是否正确 #}
{# @param operation string required 执行的空间运算类型：union（并集）、intersection（交集）、sym-difference（对称差集）、identity（身份）、update（更新）、clip（裁剪）、erase（擦除） #}
{# @param input file_path required 输入矢量数据集路径（源图层） #}
{# @param method file_path required 方法矢量数据集路径（用于定义运算区域） #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param of string optional 输出格式名称，如 GPKG、ESRI Shapefile、GeoJSON 等 #}
{# @param overwrite boolean optional 允许覆盖已存在的输出数据集 #}
{# @param append boolean optional 追加到现有图层而不是创建新图层 #}
{# @param input_layer string optional 指定输入数据集中的图层名称 #}
{# @param method_layer string optional 指定方法数据集中的图层名称 #}
{# @param output_layer string optional 指定输出图层名称 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_layer_algebra

gdal vector layer-algebra{% if of %} --output-format {{ of | quote }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if method_layer %} --method-layer {{ method_layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %} {{ operation | quote }} {{ input | safe_path | quote }} {{ method | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
