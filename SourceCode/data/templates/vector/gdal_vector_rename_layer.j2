{# @id gdal_vector_rename_layer #}
{# @name 矢量图层重命名 #}
{# @description 使用 gdal vector rename-layer 重命名矢量数据中的图层名称，支持批量重命名和命名规范转换 #}
{# @keyword layer #}
{# @keyword rename #}
{# @keyword 重命名 #}
{# @keyword ascii #}
{# @keyword 小写 #}
{# @keyword 文件名兼容 #}
{# @keyword 批量 #}
{# @keyword 矢量 #}
{# @concept "gdal vector rename-layer 可用于批量修改矢量数据中的图层名称，支持多种命名规范转换" #}
{# @concept "可通过 --ascii、--lower-case、--filename-compatible 等选项自动规范化图层名称，处理文件名不兼容问题" #}
{# @note 使用 --output-layer 指定单个图层的新名称，配合 --input-layer 处理多图层数据集 #}
{# @note 批量重命名时，不兼容字符会被移除，可通过 --replacement-character 自定义替换规则 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Layer not found" — 指定的输入图层不存在，请检查图层名称是否正确 #}
{# @common_error "Layer already exists" — 目标图层名已存在，可使用 --overwrite-layer 选项覆盖 #}
{# @param input file_path required 输入矢量数据集路径 #}
{# @param output file_path required 输出矢量数据集路径 #}
{# @param input_layer string optional 输入图层名称 #}
{# @param output_layer string optional 输出图层新名称 #}
{# @param of format optional 输出格式名称 options=GPKG,GeoJSON,ESRI Shapefile,GML,KML #}
{# @param ascii boolean optional 强制将名称转换为 ASCII 字符 #}
{# @param lower_case boolean optional 强制将名称转为小写 #}
{# @param filename_compatible boolean optional 强制名称符合文件命名规范 #}
{# @param reserved_characters string optional 要移除的保留字符列表 #}
{# @param replacement_character string optional ASCII 转换失败时的替换字符 #}
{# @param max_length integer optional 图层名称最大长度限制 #}
{# @param overwrite boolean optional 允许覆盖现有输出数据集 #}
{# @param append boolean optional 追加到现有图层 #}
{# @param overwrite_layer boolean optional 允许覆盖现有输出图层 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_rename_layer

gdal vector rename-layer{% if input_layer %} -l {{ input_layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if of %} -f {{ of | quote }}{% endif %}{% if ascii %} --ascii{% endif %}{% if lower_case %} --lower-case{% endif %}{% if filename_compatible %} --filename-compatible{% endif %}{% if reserved_characters %} --reserved-characters {{ reserved_characters | quote }}{% endif %}{% if replacement_character %} --replacement-character {{ replacement_character | quote }}{% endif %}{% if max_length %} --max-length {{ max_length }}{% endif %}{% if overwrite %} --overwrite{% endif %}{% if append %} --append{% endif %}{% if overwrite_layer %} --overwrite-layer{% endif %} {{ input | safe_path | quote }} {{ output | safe_path | quote }}

REM Done
