{# @id gdal_proximity #}
{# @name 栅格像元邻近距离计算 #}
{# @description 生成栅格邻近距离图，计算每个像元中心到最近目标像元中心的距离 #}
{# @keyword raster #}
{# @keyword proximity #}
{# @keyword distance #}
{# @keyword buffer #}
{# @keyword band #}
{# @keyword pixel #}
{# @keyword geotiff #}
{# @keyword 分析 #}
{# @keyword 距离 #}
{# @keyword 邻近 #}
{# @concept "gdal_proximity 生成邻近距离栅格图，表示每个像元中心到最近目标像元中心的距离，目标像元由源栅格的像素值指定" #}
{# @concept "该工具常用于创建欧式距离图层、成本距离分析前准备、或从二值化栅格（如建筑物、道路掩膜）生成距离场" #}
{# @note 默认情况下，所有非零像素被识别为目标像元。指定 -values 参数可精确控制哪些像素值被视为目标 #}
{# @note 输出栅格的空间分辨率和范围与源栅格一致。如目标文件不存在将自动创建，否则覆盖现有文件 #}
{# @common_error "Access window out of range" — 源文件坐标范围与目标文件不匹配，或目标文件尺寸与源文件不一致 #}
{# @common_error "Unable to open datasource" — 指定的源文件路径不存在，或 GDAL 无法识别该文件格式 #}
{# @param srcfile file_path required 源栅格文件路径，用于识别目标像元 #}
{# @param dstfile file_path required 输出邻近距离栅格文件路径 #}
{# @param of format optional 输出格式名称，如 GTiff、PNG 等 default=GTiff options=GTiff,ENVI,HFA,JPEG,PNG #}
{# @param srcband integer optional 源文件使用的波段编号（默认1） default=1 #}
{# @param dstband integer optional 目标文件使用的波段编号（默认1） default=1 #}
{# @param values string optional 目标像元值列表，逗号分隔（如 1,2,255）。未指定时所有非零像素均为目标像元 #}
{# @param distunits enum optional 距离单位，PIXEL 表示像素单位，GEO 表示地理坐标单位 default=PIXEL options=PIXEL,GEO #}
{# @param maxdist float optional 最大生成距离，超过此距离的像元使用 nodata 值填充 #}
{# @param nodata float optional 指定 nodata 值用于标记超出最大距离的像元 #}
{# @param ot enum optional 输出数据类型 default=Float32 options=Byte,UInt16,Int16,UInt32,Int32,Float32,Float64 #}
{# @param co boolean optional (auto-completed) #}
{# @param fixed_buf_val boolean optional (auto-completed) #}
{# @param use_input_nodata boolean optional (auto-completed) #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_proximity

gdal_proximity {{ srcfile | quote }} {{ dstfile | quote }}{% if srcband %} -srcband {{ srcband }}{% endif %}{% if dstband %} -dstband {{ dstband }}{% endif %}{% if of %} -of {{ of | quote }}{% endif %}{% if co %} -co {{ co | quote }}{% endif %}{% if ot %} -ot {{ ot }}{% endif %}{% if values %} -values {{ values | quote }}{% endif %}{% if distunits %} -distunits {{ distunits }}{% endif %}{% if maxdist %} -maxdist {{ maxdist }}{% endif %}{% if nodata %} -nodata {{ nodata }}{% endif %}{% if use_input_nodata %} -use_input_nodata {{ use_input_nodata }}{% endif %}{% if fixed_buf_val %} -fixed-buf-val {{ fixed_buf_val }}{% endif %}

REM Done
