{# @id gdal_vector_layer_algebra #}
{# @name 矢量图层代数运算 #}
{# @description 对两个矢量图层执行并集、交集、对称差集、裁剪、擦除等几何代数运算 #}
{# @keyword overlay #}
{# @keyword intersection #}
{# @keyword union #}
{# @keyword clip #}
{# @keyword erase #}
{# @keyword vector #}
{# @keyword gdal #}
{# @keyword 图层运算 #}
{# @concept "矢量图层代数运算是空间叠加分析的核心操作，通过对两个矢量图层进行几何集合运算生成新图层" #}
{# @concept "clip（裁剪）保留输入图层中被方法图层覆盖的部分；erase（擦除）保留被方法图层覆盖的部分；union（并集）保留两者合并的所有区域" #}
{# @note intersection（交集）只保留两个图层重叠区域；sym-difference（对称差集）保留非重叠区域 #}
{# @note identity（身份）运算会将输入图层每个要素与方法和重叠区域拆分为独立要素 #}
{# @seealso ogr2ogr_convert #}
{# @common_error "Layer does not exist" — 指定的输入图层或方法图层名称不存在，请使用 --input-layer 和 --method-layer 参数指定正确的图层名 #}
{# @common_error "Unsupported geometry type between layers" — 两个图层的几何类型不兼容或运算不支持该组合，例如对点图层执行 clip 运算 #}
{# @param operation enum required 代数运算类型 options=union,intersection,sym-difference,identity,update,clip,erase #}
{# @param input file_path required 输入矢量数据集（第一个图层） #}
{# @param method file_path required 方法矢量数据集（第二个图层，用于执行运算） #}
{# @param output file_path required 输出矢量数据集 #}
{# @param of format optional 输出格式名称 options=GeoJSON,ESRI Shapefile,GPKG,KML,GML #}
{# @param overwrite_layer boolean optional 允许覆盖已存在的输出图层 #}
{# @param append boolean optional 追加到已存在的图层 #}
{# @param input_layer string optional 指定输入数据源中的图层名称 #}
{# @param method_layer string optional 指定方法数据源中的图层名称 #}
{# @param output_layer string optional 指定输出图层名称 #}
{# @param geometry_type string optional 指定输出图层的几何类型（如 POINT, LINESTRING, POLYGON） #}
{# @param all_input_field boolean optional 将输入图层的所有字段添加到输出 #}
{# @param all_method_field boolean optional 将方法图层的所有字段添加到输出 #}

@echo off
REM Generated by GIS Agent
REM Template: gdal_vector_layer_algebra

gdal vector layer-algebra {{ operation | quote }} {{ input | safe_path | quote }} {{ method | safe_path | quote }} {{ output | safe_path | quote }}{% if of %} --format {{ of | quote }}{% endif %}{% if overwrite_layer %} --overwrite-layer{% endif %}{% if append %} --append{% endif %}{% if input_layer %} --input-layer {{ input_layer | quote }}{% endif %}{% if method_layer %} --method-layer {{ method_layer | quote }}{% endif %}{% if output_layer %} --output-layer {{ output_layer | quote }}{% endif %}{% if geometry_type %} --geometry-type {{ geometry_type | quote }}{% endif %}{% if all_input_field %} --all-input-field{% endif %}{% if all_method_field %} --all-method-field{% endif %}

REM Done
